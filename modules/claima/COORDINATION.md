# Claima — parallel session coordination
Three sessions build three features at once. Each session has a TAG (the amendment number it is applying, e.g. A25).

## Ownership (who may write where)
- Each session writes ONLY inside modules/claima/features/<its-feature>/ and its own data/seed/<entity>.js + data/repositories/<entity>.js listed in its amendment.
- SHARED files (append-only, one-line edits, never rewrite): modules/claima/module.js (add your nav entry + routes inside the block marked with your TAG), shared/config.js (add keys under CONFIG.claima, never edit others'), CLAUDE.md (append your ### entry only), data/store.js (register your entity line only), this file.
- Never edit another session's feature folder or entity files. If you need something changed there, write a request in the Requests section below and stop that part of your work until it is answered.
- data/repositories/claims.js is shared: add helpers in a section headed by your TAG; never modify an existing helper's signature or behaviour.

## Contract (interfaces every session may rely on)
- claims repository helpers listed in amendment 24 + any helper published below under "Published helpers".
- Cross-feature links go through claim ids / claimNo, batch ids, remittance ids — never by importing another feature's files.
- All dates ISO YYYY-MM-DD; ids not labels; every screen uses ctx.onData; pickers use findActive() helpers.

## Protocol
1. On start: read this file fully, append "TAG started <time>" under Status.
2. Before editing any SHARED file: re-read it from disk, make the one-line/one-block change under your TAG, save immediately. Never hold a shared file open across other work.
3. When you add a helper others may use: list it under "Published helpers" with its signature and one-line semantics.
4. When you need something from another feature: write it under "Requests" as "TAG → TAG: need <helper/field>, signature <…>"; poll this file every few minutes; the owner answers under the request and implements it in their own files.
5. If you find a conflict, a broken import, or a failing selfCheck caused by another session: write it under "Conflicts" with file + line; do not fix files you don't own.
6. On finish: run node --check on the whole tree and load every Claima route, then append "TAG done <time>, routes: …" under Status. The last session to finish runs the full-tree check once more.

## Status
- A24 done 2026-09-10, routes: #/claima (home placeholder). Module registered, `claims` owned by Claima, repository extended, `CONFIG.claima` in place.
- A25 started 2026-09-10 (session harp-7a).
- A25 done 2026-09-10 (harp-7a), routes: #/claima/charges, #/claima/charges/health. Helpers published above; `### 25` appended to CLAUDE.md; full-tree node --check clean at the time.
- A27 started 2026-09-10 (session for amendment 27: claims assembly + portfolio).
- A26 started 2026-09-10 (harp-17): coding, code-sets, clinical-docs.
- A26 done 2026-09-10 (harp-17), routes: #/claima/coding, #/claima/coding/<encounterNo>, #/claima/coding/queries, #/claima/coding/metrics. Helpers published below; `### 26` appended to CLAUDE.md; every module in the tree imported in the browser (282 files) — the two failures are pre-existing in Frontis accounts (`emptyHtml` declared twice in account-receipts.js and account-view.js at HEAD), not Claima's.
- A27 done 2026-09-10, routes: #/claima/claims (portfolio: by payer / by status / flat, bulk scrub + finalize), #/claima/claims/<claimNo> and /<claimNo>/<lines|coding|attachments|scrub|history>. Helpers published below; `### 27` appended to CLAUDE.md; Pactum gained the Documentation Required section on the Pre-Auth tab. Full-tree `node --check` clean (0 failures) as the last to finish; `store.resetToSeed()` overflow and the Frontis `emptyHtml` duplicates stand as harp-17 recorded them.
- A28 started 2026-09-10 (harp-76): submission — modules/claima/features/submission/, data/repositories/batches.js, data/seed/batches.js, data/seed/rejection-reasons.js, the A28 section of data/repositories/claims.js, CONFIG.claima.submission.
- A28 done 2026-09-10 (harp-76), routes: #/claima/submission (workbench: ready queue by payer + batches), #/claima/submission/<batchNo> (+ tab id), #/claima/submission/rejections. Helpers published below; `### 28` appended to CLAUDE.md; every module in the tree imported in the browser (337 files, 0 failures) and create → generate → submit → acknowledge (with a rejection) → fix & resubmit → re-finalize → cycle 2 joined the open batch, driven live.
- A30 started 2026-09-10 (harp-e7): lifecycle — modules/claima/features/lifecycle/, data/repositories/followups.js, data/seed/followups.js, data/engines/claim-events.js, data/engines/payer-stats.js, CONFIG.claima.lifecycle, an A30 block in module.js. Does not touch claims.js.

- A29 started 2026-09-10 (harp-bb): remittance, denials (stub), unapplied, posting-engine; A29 section of claims.js; CONFIG.claima.posting; ledger tx type PortionShift.
- A30 done 2026-09-10 (harp-e7), routes: #/claima/pipeline, #/claima/aging, #/claima/timeline/<claimNo>, #/claima/followups, #/claima/payer-stats. Helpers published below; `### 30` appended to CLAUDE.md; every module in the tree imported in the browser (337 files, 0 failures) with A28 and A29 as they stood at the time; `[payer-stats] self-check pass`. Open request to A27 (Timeline tab) under Requests.
- A32 started 2026-09-10 (harp-da): nullification — modules/claima/features/nullification/, data/repositories/nullifications.js, data/seed/nullifications.js, data/seed/nullification-reasons.js; A32 sections (new helpers only) in claims.js (`nullify`), batches.js (`removeClaim`), charges.js (`returnFromClaim`); CONFIG.claima.nullification; roles canNullifyClaim / canApproveNullification; an A32 block in module.js.
- A33 started 2026-09-10 (harp-31): write-offs — modules/claima/features/writeoffs/, data/repositories/writeoffs.js, data/seed/writeoffs.js, data/seed/writeoff-reasons.js, data/engines/writeoff-tiers.js; an A33 section (new helpers only) in claims.js (`adjust`, `reverseAdjustment`, `openBalance`); CONFIG.claima.writeoffs; roles canApproveWO1 / canApproveWO2 / canApproveWO3; an A33 block in module.js; the `writeoffs` lookup branch in shared/activity-claima.js. Patient-side postings are ledger `Adjustment` rows (origin = the write-off) and one branch for that type in data/engines/account-engine.js `effect()`, the A29 PortionShift shape.
- A31 started 2026-09-10 (harp-0b): denial triage & routing — modules/claima/features/denials/, data/repositories/denials.js + data/seed/denials.js (taken over from A29's stub, its helpers kept), data/engines/denial-router.js, data/seed/root-causes.js, an A31 section (new helpers only) in charges.js and claims.js, CONFIG.claima.denials, canResolveDenial in roles.js, an A31 block in module.js, the `denials` lookup in shared/activity-claima.js.

- A29 done 2026-09-10 (harp-bb), routes: #/claima/remittances (workbench), /remittances/upload (ERA stepper), /remittances/new (manual entry; `?no=` continues an unposted one), /remittances/unapplied (unapplied cash), /remittances/<remittanceNo> (+ tab id: reconcile | exceptions | postings | file | history). Helpers published below; `### 29` appended to CLAUDE.md; every module in the tree imported in the browser (340 files, 0 failures); driven live: sample ERA uploaded and posted (4 claims, a denial, a shift, a hand-off, unapplied cash), ambiguous row matched and posted, overpayment resolved, posting reversed and reposted, remittance closed, manual entry saved and posted, unapplied cash applied and refunded.
- A33 done 2026-09-10 (harp-31), routes: #/claima/writeoffs (worklist + Pending my approval), #/claima/writeoffs/new (?denial=&claim=&mrn=&rerequest=), #/claima/writeoffs/<id> (+ /context|chain|history), #/claima/writeoffs/analytics (?period=). Helpers published below; `### 33` appended to CLAUDE.md; every module in the tree imported in the browser (374 files, 0 failures) with A31 and A32 as they stood; request → self-approval refused → approve → post → Frontis account page reads the Adjustment → reverse, driven live on both sides; `[writeoffs] self-check pass`. One conflict recorded below (performance self-check, A29's).
- A32 done 2026-09-10 (harp-da), routes: #/claima/nullifications (the log: MTD rail, twelve-column table, filters, CSV), #/claima/nullifications/<no> (the record, the family strip, the charge round trip, the timeline excerpt); Nullify on the claim page at #/claima/claims/<no> (the dialog, or the routing card on a paid or denied claim). Helpers published below; `### 32` appended to CLAUDE.md; every module in the tree imported in the browser (374 files, 0 failures); nullified a Ready claim live with a two-approver signature and a fresh replacement (NUL-2026-000019 → CLM-2026-000615), the routing cards checked on a Paid and a Denied claim, and Reset demo data re-seeds the six records.
- A31 done 2026-09-10 (harp-0b), routes: #/claima/denials (worklist: rail, filters, bulk triage, assign), #/claima/denials/<id> (+ /history), #/claima/denials/analytics. Helpers published below; `### 31` appended to CLAUDE.md; every module in the tree imported in the browser (374 files, 0 failures after a fresh load); `[denials] self-check pass`; driven live: triage → route (all seven kinds, Recode and Charge correction on a claim finalized, submitted and refused in-session), re-route with a reason, a Refresh that went out on cycle 2 and was paid back by a posted remittance (Recovered), a second refusal on the same line (repeat, no duplicate row), a hand-off marked Recovered on Pactum's side, a manual close behind its role gate, a write-off route through A33, bulk triage, Reset demo data.
- A29 closed 2026-09-10 (ruling, recorded by harp-da — harp-bb had gone): cash-only control block accepted; the PortionShift balance effect in account-engine.js kept; the thin seeded amounts fine; the Secondary Refresh error and the missing `unapplied` branch in shared/activity-claima.js stand under Conflicts for the consolidation pass.
- A34 started 2026-09-10 (harp-9b): daily transaction report — modules/claima/features/dtr/, data/repositories/business-days.js + data/seed/business-days.js, data/repositories/cash-sessions.js + data/seed/cash-sessions.js, data/engines/dtr-engine.js, CONFIG.claima.dtr, roles canCloseDay / canReopenDay / canDocumentException / canCountersign, an A34 block in module.js. Reads (never writes) ledger, charges, batches, nullifications, remittances, unapplied, denials, writeoffs, handoffs; subscribes to `ledger.afterAppendHooks` (harp-31, A22, owns ledger.js and is adding it).
- A35 started 2026-09-10 (harp-01): home — modules/claima/features/home/ only, an A35 block in module.js replacing the A24 placeholder route (first nav entry "Home", badge = escalated + untriaged), `### 35` in CLAUDE.md. Reads published helpers only; feature-detects `dtr.today()` (A34).
- A32 closed 2026-09-10 (ruling): the $1,000 threshold accepted; the wrong-payer seed on the ISF visit accepted, the live path being enough; the seed trade-offs (MOPH out of the ready queue, no Fail scrub) accepted; Void claims in Performance gross logged under Conflicts for the consolidation pass.
- Full-tree check for the A31–A33 set (harp-0b, last to finish): `node --check` clean over 375 modules, every module imported in the browser on a fresh load (375 files, 0 failures), all 19 Claima routes rendered with no console error; `[denials]`, `[writeoffs]` and `[payer-stats]` self-checks pass; `[performance]` fails as recorded under Conflicts (pre-existing, diagnosed there).
- A35 done 2026-09-10 (harp-01), routes: #/claima (and /home) — the dashboard replacing the A24 placeholder (six KPI cards, four attention panels, Claima-scoped recent activity, five quick actions, badge = escalated + untriaged). Reads published helpers only; `dtr.today()` feature-detected (A34 in flight — the card reads "DTR arriving" until `data/repositories/business-days.js` is on disk; `#/claima/dtr` falls back to home through the shell until then). `shared/activity-trail.js` gained an additive `entities` scope on `recent`/`activityHtml`. `### 35` appended to CLAUDE.md; every card, quick action and View all loaded once and reconciled; full-tree `node --check` 378 files, 0 failures (A34 and Frontis A22 still in flight, so not the last to finish).
- A34 done 2026-09-10 (harp-9b), routes: #/claima/dtr (today's report, live), #/claima/dtr/<date> (another day's — frozen once closed), #/claima/dtr/archive (every version + the month-to-date roll-up), #/claima/dtr/archive/<date> (?v=) (the frozen snapshot with Export PDF / Excel). Helpers published below; `### 34` appended to CLAUDE.md; every module in the tree imported in the browser (403 files, 0 failures); driven live: the seeded cash-integrity RED fixed from its own row, four sessions closed through the dialog (one short, with a reason and a countersignature), the day closed as v1, a payment recorded after the close tagged prior-day onto tomorrow, the day reopened with a reason and re-closed as v2, both versions read from the archive, the CSV built, Reset demo data re-seeding. Frontis A22 still in flight, so not the last to finish.
- Full-tree check 2026-09-10 (harp-9b, last Claima session to finish — A22 Frontis still in flight): `node --check` 403 files, 0 failures; every module imported in the browser after a sessionStorage-cleared reload, 0 failures; all 27 Claima routes loaded once, none failed; no control characters in any module. Console on load: `[payer-stats] self-check pass`, `[writeoffs] self-check pass`, and the standing `[performance] self-check FAIL` recorded under Conflicts (A29's).
- Final tree check 2026-09-10 (harp-31, last to finish, after A22 landed on top of A31–A35): `node --check` over 403 files, 0 failures; every module imported in the browser (403, 0 failures); every Claima nav route and #/frontis/accounts loaded. Still standing under Conflicts: the `[performance] self-check FAIL` on load (A29's).

## Published helpers
A24 — `data/repositories/claims.js` (the amendment-11 helpers are unchanged: `all`, `get`, `list`, `byPayer`, `byContract`, `byServiceGroup`, `trackedPayerIds`, `trackedContracts`, `itemOf`, `itemName`, `counts`, `isAdjudicated`, `isPaid`, `statusTone`, `denialLabel`, `DENIAL_REASONS`).
- Shape: `claim = { id ("CLM-0001"), claimNo ("CLM-2026-000001"), patientMrn, encounterNo|null, payerId, planId, policyId|null, contractId, status, dateOfService, createdAt (ISO timestamp), submittedAt|null, batchId|null, remittanceId|null, lines[], totals, denialReasonCode|null, scrubberFindings[], attachments[], + legacy flat fields serviceGroup, category, itemId, qty, grossBilled, allowedExpected, allowedPaid, paidAt, appealed }`. There is no `history` array: the trail is the audit entity `claims` keyed on `claim.id` — read it with `history(id)`.
- `lines[i] = { id ("L1"), itemId, qty, grossBilled, allowedExpected, payerShare, patientShare, isOverage, ledgerTxIds[], status (LINE_STATUSES: Open | Paid | Partially Paid | Denied | Void), denialReasonCode|null }`.
- `totals = { gross, allowedExpected, payerShare, patientShare, paid, adjusted, balance }` — the first four are sums over the lines; `paid` is the payer's cash, `adjusted` what was written off the payer share, `balance = payerShare − paid − adjusted`. Never write totals by hand: they and the flat fields are re-derived by `data/engines/claim-totals.js` (`lineOf`, `totalsOf`, `syncClaim`, `cents`) on every write. Legacy `allowedPaid = paid + patientShare` once `paidAt` is set, else 0 — which is what Performance reads.
- `STATUSES` = Draft | Ready | Submitted | Acknowledged | Paid | Partially Paid | Denied | Rejected | Appealed | Closed | Void; `ADJUDICATED` = Paid, Partially Paid, Denied, Appealed, Closed; `IN_FLIGHT` = Submitted, Acknowledged; `UNSUBMITTED` = Draft, Ready.
- `isDenied(claim)` — Denied, or Appealed with nothing paid (Performance's denial rate reads this, so an appeal does not lift a denial). `isPending(claim)` — in flight. `isEditable(claim)` — Draft or Ready.
- `search(q, { status, payerId, planId, contractId, patientMrn, encounterNo, batchId, remittanceId, from, to })` → rows, newest date of service first; `q` matches claimNo, id, encounterNo, MRN and the patient's name.
- `byStatus(status)` → rows; `byStatus()` → `{ [status]: rows }`. `byPatient(mrn)`, `byEncounter(no)` → rows. `counts()` gained `status: { [status]: n }`.
- `nextClaimNo(year = this year)` → `CLM-YYYY-NNNNNN`, read off the table.
- `create({ patientMrn, payerId, planId, policyId, encounterNo, contractId?, dateOfService, lines, status? })` → the stored claim, Draft unless `status: 'Ready'`; lines arrive priced (`{ itemId, qty, grossBilled, allowedExpected, payerShare, patientShare, isOverage, ledgerTxIds }`); `contractId` defaults to `contracts.contractForService(payerId, planId, dateOfService)`. Audited "Created".
- `update(id, patch, { details })` → the claim or null unless Draft/Ready; patch may carry lines, cover, encounter, dateOfService, scrubberFindings, attachments. Audited "Updated".
- `setStatus(id, status, payload = { submittedAt, paidAt, paid, adjusted, batchId, remittanceId, denialReasonCode, reason, details })` → the claim; the one way a claim moves. Submitted stamps `submittedAt` today when none is given; Paid / Partially Paid stamp `paidAt` the same way; Appealed sets the legacy `appealed` flag. Audited "Status: from → to — reason — details".
- `recomputeTotals(id)` → re-derives totals and flat fields from the lines and commits; not audited.
- `history(id)` → `audit.forEntity('claims', id)`, newest first.
- Seed: 600 generated claims (`data/seed/claims.js`), one line each, every one already Submitted / Acknowledged / Paid / Partially Paid / Denied. `patientMrn` is a holder of a policy on the plan 40% of the time (then `policyId` is set) and any registered patient otherwise (`policyId` null); `encounterNo`, `batchId`, `remittanceId` are all null — a feature that seeds drafts, batches or remittances writes its own rows and links them by id.
- `CONFIG.claima = { submissionDeadlineDays: 30, scrubberRules: [], ackSlaDays: 3 }` in `shared/config.js`.

A25 — `data/repositories/charges.js` (charge capture; exists now, seeded on first read from the ledger + `data/seed/charges.js`).
- Shape: `line = { id ("CHG-0001"), encounterNo, patientMrn, itemId, qty, dateOfService, doctorId|null, consumption[], source { type (Order|Event|Procedure|Manual), ref, capturedBy ("System"|user), reason|null }, pricing { gross, allowed, payerShare, patientShare, undecided, isOverage, componentId|null, overageRows, status (Priced|Held for approval|Not billable), trace }, ledgerTxIds[], status (Unreleased|Held|Released|Reversed), holdReason|null, flags[] (Late|ZeroPrice|Manual|CdmGap|Disputed), releasedAt|null, releasedBy|null, late { window, since, approvedBy|null, reason|null }|null, reversesId|null, reversedAt|null, reversalReason|null, createdAt, updatedAt }`. A package captured live is one line carrying every ledger row it produced (`overageRows` counts the overage ones); `pricing.trace` is a compact `{ contractNo, version, methodology, coverage, preAuth, rules[], overrides[] }` or `{ selfPay: true }`.
- `byEncounter(no)` → all lines on the visit, date of service then arrival order. `releasedByEncounter(no)` → Released only. `lateCharges(no)` → lines flagged Late, not Reversed. `isUnreleased(line)` — Unreleased or Held. `get(id)`. `history(id)` → `audit.forEntity('charges', id)`.
- `release(no, lineIds|null, user)` → `{ released[], excluded[{ line, why }], at }`; null releases every clean unreleased line on the visit, a list releases those it names. Clean = Unreleased with no ZeroPrice/CdmGap/Disputed flag and no late charge awaiting approval (`releaseBlocker(line)` → '' or the reason). Audited "Released" per line, one commit, then every function in `afterReleaseHooks` is called with `{ encounterNo, lineIds, at }`.
- `afterReleaseHooks[]` — push a function; called after every release (A26). `afterCorrectionHooks[]` — push a function; called with `{ encounterNo, lineIds, kind ('cancel'|'hold'), at }` when a Released line is cancelled or put on hold (A27 marks the claim Stale). A hook that throws is logged and skipped.
- `capture(encounterNo, { itemId, qty, dateOfService, doctorId, consumption }, { type, ref, capturedBy, reason }, { lateReason, role, hold })` → `{ error, line, posted }`; prices through `account-engine.postCharge`, posts through `accounts.postCharges` (ledger rows, `chargesPosted`, upfront settlement applied), flags Manual/Late/ZeroPrice (allowed === 0), holds a feed's late line beyond the window for approval. After a late capture it calls `claims.onLateCharge?.(encounterNo, [lineId])` and, when a coding record already exists for the visit, `coding.requestRecode(encounterNo, { source: 'LateCharge', ref: lineId, reason })` — both through dynamic imports, since both files import this one statically.
- Corrections: `edit(id, { qty, dateOfService, doctorId })` and `replace(id, newItemId)` → reversal + fresh capture with `reversesId` (unreleased lines only); `cancel(id, reason)`, `hold(id, reason)`, `unhold(id)` (`unholdBlocked(line)`), `approveLate(id, reason, role)` (needs `role.canLateCharge`).
- Reads for screens: `unbilledGrouped(q, { department, status, flag, source, from, to })` → `[{ encounterNo, encounter, lines, gross, allowed, held, flags, oldestAt, closed, releasable }]`, closed visits first then oldest line; `counts()` → `{ encounters, lines, gross, held, late, zeroPrice }`; `zeroPrice()`; `gaps()` → `[{ encounterNo, encounter, nights, captured, missing, item, signal, missingLabel }]`; `lateness(enc)` → null or `{ since, days, window, why }`; `dosBounds(enc)`; `captureBlocked(enc, spec, opts)`; `preview(no, spec)`; `sourceTypeOf(category)`; `needsDoctor(item)`; `roomItemFor(enc)`; `statusTone`, `flagTone`, `flagTitle`, `zeroPriceReason`, `HOLD_REASONS`, `FLAG_LABELS`.
- `data/repositories/feeds.js` — feed events `{ id ("FE-0001"), type, ref, encounterNo, itemId, qty, dateOfService, trigger (OnOrder|OnCompletion|Midnight), status (Pending|Captured|Failed), capturedLineId|null, at, capturedAt|null, error|null }`: `all`, `pending()`, `failed()`, `pendingFor(no)`, `bySource()`, `counts()`, `lastRun()`, `runs()`, `run(by = 'System')` → `{ at, captured[], failed[], run }` (every Pending event through `charges.capture`), `retry(id)`, `triggerOf(category)`.
- `CONFIG.claima.capture = { lateWindowDays: 7, triggers: { Lab, Radiology, Pharmacy, Procedure, 'Room & Board' } }`; `role.canLateCharge` on `shared/roles.js` (coder and CMO).
- Seed: every Charge ledger tx is a Released line (one Reversed, two Manual), plus 12 unreleased lines over four visits and 6 Pending feed events (one fails on run: an inactive item). `store.js` is untouched — both tables build on first read.

A26 — `data/repositories/coding.js` (encounter coding; two tables, `coding` and `cdiQueries`, both seeded on first read from the board and the released lines — `data/seed/coding.js`).
- Shape: `codingRecord = { id (= encounterNo), encounterNo, status (STATUSES: Unassigned | Assigned | In Progress | Query Pending | Coded | Recode Requested), assignedTo (coder id: 'coder' is the demo RCM role, 'CD-0002'/'CD-0003' the seeded colleagues — `coderName(id)`, `coders()`), versions[], releasedAt, completedAt|null, slaDays, assignments[{ by, to, at, reason }], recodeRequests[{ id ("RCR-0001"), source (Denial | LateCharge | Manual), ref, reason, at, by, status (Open | Accepted | Done | Dismissed) }] }`.
- `versions[i] = { version (1..n), diagnoses[{ code, desc, principal: bool, poa: 'Y'|'N'|'U'|null }], procedures[{ code, desc, date (ISO), doctorId, chargeLineIds[] }], warningsAcknowledged[{ code (warning key), reason }], codedAt|null, codedBy (coder id)|null, reason|null (the recode reason) }`. The newest version without `codedAt` is the draft under work; a recode appends a copy and never edits the version that stands. `chargeLineIds` are `charges.js` line ids (`CHG-…`) — the ledger's `LDG-…` ids only where the capture register has no rows for the visit.
- `byEncounter(no)` → `{ status, version, diagnoses, procedures, codedAt, codedBy }` of the newest **coded** version (empty arrays and version 0 when none) — what a claim is assembled from; a chart under recode reports the standing version with status 'In Progress'.
- `afterCodedHooks[]` — fired with `{ encounterNo, codingVersion }` on Mark Coded. `afterRecodeHooks[]` — fired with `{ encounterNo, oldVersion, newVersion, reason }` when a recode opens (newVersion is the uncoded copy); `recode()` also calls `claims.markStale?.(encounterNo, reason)`.
- `requestRecode(no, { source, ref, reason })` → the request (`{ id, source, ref, reason, at, by, status: 'Open' }`) or null; flags a Coded chart 'Recode Requested', is just recorded on an open one. A25 calls it on a late charge, A27 on a denial routed back.
- Reads: `releasedLines(no)` → `[{ id, itemId, chargeCode, description, category, qty, amount (allowed), at, dateOfService, doctorId, isOverage }]`; `worklist()` / `search(q, { view, status, coder, department, type, band, from, to, beyondSla, awaiting, working })` / `counts()`; `validate(no, draft?)` → `{ blocking[{ key, label, jumpTo }], warnings[…], acknowledged: Set, ok }`; `metrics()`; `history(no)` (audit entity `coding`, keyed on the encounter number).
- Queries: `queriesFor(no)`, `openQueries(no)` (Open + Answered), `queriesForPhysician(doctorId?, { openOnly })`, `awaitingAnswer()`, `getQuery(id)`, `queryTat(q)` (days); writes `raiseQuery`, `answerQuery` (isPhysician), `resolveQuery`, `reopenQuery`, `withdrawQuery`. `cdiQuery = { id ("CDQ-0001"), encounterNo, type, physicianId (DR-…), question, refs { chargeLineIds[], docIds[] }, status (Open | Answered | Resolved | Withdrawn), raisedBy, thread[{ at, by, role, text }], raisedAt, answeredAt|null, resolvedAt|null }`.
- Also `data/repositories/clinical-docs.js`: `byEncounter(no)` → `[{ id ("DOC-0001"), encounterNo, patientMrn, type (TYPES: Discharge Summary | Operative Note | Progress Note | Lab Report | Imaging Report | Consent), title, fileName, date (ISO), author, authorId, textPreview }]`, newest first; `get(id)`, `byPatient(mrn)`, `add({ encounterNo, type, title, … })`. Seeded from `data/seed/clinical-docs.js` on first read (8 documents over 5 charts).
- Also `data/repositories/code-sets.js`: `icd.search(q)` / `proc.search(q)` → up to 12 `{ code, desc, sex, ageMin, ageMax, (proc) category }`, `icd.get(code)`, `proc.get(code)`, `icd.all()`, `proc.all()`, `label(row)`, `isUnspecified(row)`, `sanity(row, patient)` → reasons. Static (no store table).
- Roles (`shared/roles.js`): `canCode` (coder, exec), `canAssignCoding` (exec), `canRecode` (coder, exec), `isPhysician` (physician). `CONFIG.claima.coding = { slaDays: { OP: 3, IP: 5, ER: 3 }, linkedCategories: ['Procedure', 'Surgery', 'Radiology'], ageBands: [1, 3, 7] }`.
- Recent activity (`shared/activity-claima.js`, asked for by the user after A26): audit entity → screen for every Claima entity. Log under these keys and ids and your rows link with no further work: `charges` (line id) → capture worklist grouped under the visit; `feeds` → `#/claima/charges/health`; `coding` / `clinicalDocs` (encounter no) → `#/claima/coding/<no>`; `claims` (claim id) → `#/claima/claims/<claimNo>`; `batches` (batch no) → `#/claima/submission/<no>`; `remittances` (no) → `#/claima/remittances/<no>`; `denials` (id) → `#/claima/denials/<id>`; `writeoffs` (id) → `#/claima/writeoffs/<id>`; `nullifications` (no) → `#/claima/nullifications/<no>`. The last five link by id alone until their repository exists — the owning session upgrades its branch to a lookup (`describeClaima`) when it lands.

A27 — `data/repositories/claims.js` (the A27 section) + `data/repositories/claim-attachments.js` + `data/engines/claim-assembler.js` / `claim-scrubber.js` (both leaves).
- Claim shape gains: `kind (Primary|Secondary|Supplementary)`, `parentClaimNo|null`, `childClaimNos[]`, `stale { flag, reason, at }|null`, `scrubRuns[{ id, at, by, contractNo, contractVersion, findings[], result (Pass|Warnings|Fail), acknowledgments[{ findingId, reason, by, at }] }]`, `scrubValid` (the latest run stands; refresh / stale / reopen / attachment change void it), `coding { version, status, codedAt, codedBy, principal {code, desc, poa}|null, principalCount, secondaries[], procedures[{ code, desc, date, doctorId, chargeLineIds[] }] }|null`, `attachments[{ id ("A1"), docId|null, fileId|null, fileName, type, origin (Auto|Upload), lineIds[], required }]`, `docRequirements[{ docType, lineIds, reason }]`, `refreshes[{ at, by, diff }]`, `lateLineIds[]`, `excluded[{ chargeLineId, itemId, reason }]`, `contractNo`, `contractVersion`, `snapshotRef`, `referralNo`, `encounterType`, `department`, `doctorId`, `dateOfServiceTo`, `assembledAt`, `finalizedAt|null`, `reopenedAt|null`, `activatedAt|null`. A claim with no `kind` is a legacy row and reads as Primary.
- Line shape gains: `chargeLineId` (charges.js id, or the ledger tx id when assembled before the register loaded), `chargeCode`, `description`, `dateOfService`, `preAuthRequired`, `authNo|null`, `authNumber|null`, `late`. `data/engines/claim-totals.js` `lineOf()` now spreads unknown fields through, so these survive `syncClaim` — no existing field or behaviour changed. Lines are matched across sources by `sameLine(a, b)` (same chargeLineId, or a shared ledger tx).
- Published for peers: `markStale(encounterNo, reason)` → n flagged (open claims only; a Ready claim goes back to Draft, audited). `onLateCharge(encounterNo, lineIds)` → the claim the lines landed on (a Draft primary is refreshed and the lines flagged Late; a Ready+ primary spawns or extends a Supplementary chained to it). `assembleFor(encounterNo)` → `{ claim, created, secondary, reason }` (idempotent; Self-Pay or no released lines → no claim with the reason; defines the Secondary from `policies.chain()` behind the classified policy). `readyForSubmission()` → Ready and not stale. `peersReady` (a promise: charges / coding / clinical-docs loaded via dynamic import, feature-detected — `peerStatus()` says which).
- Hooks consumed: `coding.afterCodedHooks` (no claim → assemble; Draft → refresh; Ready+ → stale), `charges.afterCorrectionHooks` (→ markStale), `charges.afterReleaseHooks` (→ onLateCharge when the visit already has a claim). `assembleCoded()` runs once the peers settle: every Discharged/Completed insured visit whose chart is Coded and has no primary.
- Reads: `inAssembly()`, `portfolio(q, { view ('assembly'|'all'), status, payerId, scrub (Pass|Warnings|Fail|none|voided), band, age, kind, stale, from, to })`, `assemblyStats()` → `{ count, value, readyCount, readyValue, stale, avgAge }`, `chainOf(claim)` → `{ parent, children }`, `kindOf`, `isStale`, `isLocked`, `ageDays`, `latestScrub`, `scrubResult` (null when none valid), `scrubLabel`, `unacknowledged`, `canFinalize(claim)` → `{ ok, why }`, `VALUE_BANDS` / `AGE_BANDS` / `valueBandOf` / `ageBandOf`, `releasedLines(no)`, `clinicalDocsOf(no)`, `KINDS`, `SCRUB_RESULTS`, `SCRUB_CATEGORIES`, `scrubTone`, `groupedFindings`, `diffSummary(diff)`.
- Writes: `refresh(id)` → `{ claim, diff, error }` (Draft only; clears stale, voids scrub, audited with the diff summary), `runScrub(id)` → the run, `acknowledge(id, findingId, reason)`, `finalize(id)` → Ready (gate: Pass, or Warnings all acknowledged, not stale), `reopen(id, reason)` → Draft, `addUpload(id, { fileName, type, size, lineIds })`, `attachDoc(id, docId)`, `removeAttachment(id, attId)`, `linkAttachment(id, attId, lineIds)`.
- Scrub finding: `{ id ("F01"), category (Completeness|Financial|Authorization|Referral|Documentation|Code Logic), severity (Error|Warning), lineId|null, code, message, jumpTo { tab, lineId?, anchor?, docType? } }`. Result: any Error → Fail, any Warning → Warnings, else Pass.
- `data/repositories/claim-attachments.js` — the upload register (`claimAttachments` table, seeded through `data/store.js`): `all`, `get`, `byClaim(claimNo)`, `record(claimNo, file)`, `remove(id)`, `requirementStatus(claim)`, `missingRequirements(claim)`, `requirementSummary(claim)`, `TYPES`, `MAX_BYTES`.
- Pactum: `contract.documentationRequired[{ id, scopeLevel (Contract|Service Group|Category|Item), scopeValue, docTypes[], thresholdAmount|null }]` with `DOC_LEVELS`, `DOC_TYPES`, `documentationRows`, `documentationLabel`, `documentationThresholdLabel`, `documentationOverlap`, `documentationFor(contract, lines[{ id, item, amount }], total)` → `[{ docType, sources, lineIds, reason }]` (rows add up; a Contract-level threshold reads the claim's payer share, a narrower one the line), `saveDocumentationRequired`, `removeDocumentationRequired` in `data/repositories/contracts.js`. Seed: NSSF — Discharge Summary on the whole contract, Operative Note on Surgery above $2,000; AXA — Imaging Report on Imaging.
- `CONFIG.claima.assembly = { valueBands: [0, 500, 2000, 10000] }`; `role.canReopenClaim` and `role.canAcknowledgeScrubWarning` on `shared/roles.js` (coder and CMO).
- Seed: `data/seed/claim-assembly.js` — eleven claims over the nine closed, insured, charged encounters (numbered off the table after the 600 generated rows), assembled by the engine and scrubbed by it at seeded times: 3 plain Drafts (one missing the NSSF Discharge Summary), 3 with Warnings, 2 Ready, 1 Stale, 1 Fail (the ISF plan with no contract), 1 Supplementary chained to the Bupa Ready claim, 1 Secondary defined on the NSSF emergency visit. Seeded lazily on the first assembly read after the peers settle, so the lines carry `CHG-` ids; `store.resetToSeed()` re-seeds on the next read.

A28 — `data/repositories/batches.js` (submission batches; `batches` table, seeded on first read from `data/seed/batches.js`), `data/engines/submission-files.js` (the files a batch generates — pure, reads patients/payers/policies/contracts/cdm), `data/seed/rejection-reasons.js` (static, like reference.js) and the A28 section of `data/repositories/claims.js`.
- Batch shape: `{ id (= batchNo), batchNo ("BAT-2026-000142"), payerId, mode (Electronic|Manual), status (STATUSES: Open | Generated | Submitted | Acknowledged | Partially Rejected | Rejected | Closed), claimNos[], claimCycles { [claimNo]: cycle }, exclusions[{ claimNo, reason, by, at }], ejected[{ claimNo, cause, at }], files[{ version, kind (Submission|Manifest|Forms|CoverSheets|BatchCover), generatedAt, by, fileName, content }], submission { at, by, method, reference, fileVersion } | null, acknowledgment { at, payerRef, receivedCount, acceptedCount, rejectedCount, document, by } | null, rejections[{ claimNo, code, reason, document, at, by }], createdAt, createdBy, closedAt | null }`. `OPEN_STATUSES` = Open, Generated; `ANSWERABLE` = Submitted, Acknowledged, Partially Rejected, Rejected (a rejection may still be recorded). Audit entity `batches`, entityId = the batch number; the batch page is `#/claima/submission/<batchNo>` (+ `/claims|files|submission|acknowledgment|rejections|history`), the workbench `#/claima/submission` (?status=open|<status>&payerId=&due=1), the worklist `#/claima/submission/rejections` (?route=Policy|Refresh|Recode|other).
- Published for peers: `byClaim(claimNo)` → `[{ batchNo, cycle, status, submittedAt, method, reference, acknowledgedAt, rejection }]`, oldest cycle first (the same claim number sits in one batch per cycle). `afterSubmitHooks[]` `{ batchNo, claimNos, at }`, `afterAckHooks[]` `{ batchNo, accepted[], rejected[], at }`, `afterRejectHooks[]` `{ claimNo, code, at }` — push a function; a hook that throws is logged and skipped. `latestGeneration(batch)` → `{ version, at, by, files[] } | null`; `generations(batch)` newest first.
- Reads: `all`, `get(batchNo)`, `isOpen`, `statusTone`, `profileOf(payerId)` / `modeOf` / `methodsFor` / `cycleLabel(cycle)` / `dueToday(cycle, on)` off `CONFIG.claima.submission`, `readyQueue(payerId?)` (Ready, not stale, in no open batch), `readyQueueByPayer()` → `[{ payerId, payer, claims, count, value, mode, cycle, cycleLabel, dueToday, oldestAge, openBatch }]`, `openBatchFor(payerId)`, `claimsOf(batch)`, `valueOf(batch)`, `search(q, { payerId, status ('open' | one of STATUSES), from, to })`, `counts()` → `{ readyValue, readyCount, dueToday, open, rejected }`, `history(batchNo)`, `nextBatchNo()`, `reconcile(batch, ack)` → `{ ok, included, received, accepted, rejected, ejected, problems[] }`, `submitBlocker(batch)`, `closeBlocker(batch)`, `fixPlan(claim)` → `{ route, label, href, hrefLabel, immediate, note, code, reason }`.
- Writes: `create(payerId)` (fills from the queue; null when nothing is Ready), `addReady(batchNo)`, `exclude(batchNo, claimNo, reason)`, `validate(batchNo)` → `{ ok, ejected[{ claimNo, cause }], blockers[] }` (stale, no longer Ready, or in another open batch → ejected), `generate(batchNo)` → `{ batch, generation, ejected, error }` (validates first; each generation is the next version, earlier ones kept), `markSubmitted(batchNo, { method, reference, at })`, `acknowledge(batchNo, { payerRef, at, document, receivedCount, acceptedCount, rejectedCount, rejections[{ claimNo, code, reason, document }], notReceived[] })` → `{ batch, error }` (blocked unless included = accepted + rejected + not received; accepted → Acknowledged, rejected → `reject`, not received → ejected back to Ready), `reject(batchNo, claimNo, code, reason, document)`, `close(batchNo)`, `fixAndResubmit(claimNo)` (async) → `{ claim, plan, error }`.
- Claims (A28 section of `claims.js`): the claim keeps its number across cycles — one record. Fields: `cycle (1..n)`, `previousCycleNo` (the number of the cycle before the current one, null on cycle 1 — a cycle number, not a claim number), `nextCycleNo` (null on the live record; set on a closed cycle's entry), `cycles[{ cycle, batchNo, submittedAt, acknowledgedAt, rejection, nextCycleNo }]` (the closed cycles), `submission { batchNo, at, method, reference } | null`, `acknowledgedAt | null`, `rejection { code, reason, at, batchNo } | null`, `rejectedCycle { cycle, batchNo, code, reason, at } | null` (the rejection a draft under fix answers). Helpers: `cycleOf(claim)`, `cycleChain(claim)` → `[{ cycle, batchNo, submittedAt, acknowledgedAt, rejection, status, current }]`, `assignBatch(id, batchNo | null)`, `markSubmitted(id, { batchNo, at, method, reference })`, `markAcknowledged(id, { batchNo, at, payerRef })`, `markRejected(id, { batchNo, code, reason, at })`, `unsubmit(id, cause)` (back to Ready), `openForFix(id)` (Rejected → Draft, out of the batch, `rejectedCycle` kept), `resubmit(claimNo)` (a Ready claim answering a rejected cycle closes it into `cycles[]`, `cycle += 1`, `previousCycleNo` set; audited "Resubmitted"), `rejectionsWorklist()` → Rejected claims, oldest rejection first, `rejectionAge(claim)`; `REJECTION_REASONS` / `rejectionReason` / `rejectionLabel` / `fixRouteOf` re-exported from `data/seed/rejection-reasons.js` (R01 Member ID invalid → Policy, R02 Missing or invalid data → Refresh, R03 Coding format → Recode, R04 Duplicate → Review, R05 Filing limit → Review, R06 Provider not recognised → Contract, R07 Attachment unreadable → Refresh, R08 Plan not covered on DOS → Policy, R99 Other → Review).
- The chain runs through the store: `batches.js` subscribes to `claims.status` commits and, when a claim carrying `rejectedCycle` becomes Ready (A27's `finalize`, or Fix & Resubmit's "as filed" path), calls `claims.resubmit` and includes it in the payer's open batch if there is one. A claim assembled from a visit is fixed on the portfolio (Refresh re-assembles the draft, Recode raises `coding.requestRecode(encounterNo, { source: 'Denial', ref: claimNo, reason })` by dynamic import); a legacy claim with no visit is resubmitted as filed on the next cycle.
- `CONFIG.claima.submission = { methods: { Electronic[], Manual[] }, defaultProfile, profiles: { [payerId]: { mode, cycle: { kind: Daily | Weekly (day) | Monthly (day) }, methods[] } } }` — NSSF Electronic Daily, AXA Manual Weekly Tuesday, MOPH Manual Monthly 5th, Bupa Electronic Weekly Wednesday, the rest mixed.
- Seed: six batches over NSSF, Bupa and MOPH (AXA and Bankers have too few in-flight claims to fill one) — Acknowledged fully (Bupa), Partially Rejected with three rejections (NSSF: R01, R05 open; R02 already resubmitted as cycle 2 and riding the Open batch), Submitted manual with forms (MOPH), Submitted electronic (Bupa), Generated v1 + v2 (MOPH), Open with two exclusions (NSSF) — numbered BAT-2026-000137…142. The claims are the generated in-flight rows, newest `submittedAt` first per payer, each stamped `batchId`, its status and a trail dated after its `createdAt`; the remittance seed skips anything carrying a `batchId`.

A30 — `data/engines/claim-events.js` (import it as `lifecycle`; reads claims, audit, handoffs, followups and — by dynamic import, feature-detected — batches, remittances, denials; read by no repository) + `data/engines/payer-stats.js` + `data/repositories/followups.js` (`followups` table, seeded on first read from `data/seed/followups.js`).
- Event (derived, never stored): `{ at, type, actor, summary, link: { label, href } | null, sourceId, status? (the status a lifecycle event put the claim in), derived? (read off the claim's fields, not a recorded event) }`. `TYPES` = Assembled | Refreshed | Scrubbed | Finalized | Reopened | Batched | FileGenerated | Submitted | Acknowledged | Rejected | Resubmitted | RemittancePosted | Denied | HandedOff | PatientShift | FollowUp | Escalated | Closed; `PAYER_TYPES` = Acknowledged, Rejected, RemittancePosted, Denied.
- `byClaim(claimNo, index?)` → the events in order. Sources: the claim's audit trail (`Created`, `Refreshed`, `Late charge added`, `Scrubbed`, every `Status: from → to` line), the claim's own stamps where the trail is silent (`createdAt`, `submittedAt`/`submission`, `acknowledgedAt`, `rejection`, `nextCycleNo`, `paidAt`, a denial, `Closed`), `batches.byClaim` + `get` + `latestGeneration` (Batched, FileGenerated, Submitted, Acknowledged with links to `#/claima/submission/<batchNo>`), `remittances.byClaim` (RemittancePosted with the money, PatientShift from `lines[].patientShift`, links to `#/claima/remittances/<no>`), `denials.byClaim` (Denied, linked to the remittance's exceptions tab), `handoffs.forClaim` (HandedOff → `#/defensio`), `followups.byClaim` (FollowUp), plus a computed Escalated. A seeded Acknowledged claim with no stamp reads its acknowledgment off `CONFIG.claima.ackSlaDays` and says so (`derived: true`); a seeded denial with no answer date is shown at submission, flagged the same way.
- `lastPayerEvent(claimNo, events?)`, `statusSince(claim, events?)`, `daysInStatus(claim, events?)`, `silentDays(claim, events?)` (since the last payer event, else submission), `isSilent`, `isEscalated`, `thresholdFor(claim, 'silentDays' | 'escalationDays')` (per payer via `CONFIG.claima.lifecycle`), `bucketOf(days)` / `BUCKETS` (0–15, 16–30, 31–60, 60+), `daysTone(days)` (warning > 15, critical > 30).
- `family(claimNo)` → `{ original, cycles[], supplementary[], secondary[], members[], current }` — walks `parentClaimNo` up, `previousCycleNo` back, `nextCycleNo` forward and `childClaimNos` down; `familyHead(claimNo)` is the newest cycle; `kindLabel(claim)` → '' | 'Cycle n' | 'Supp' | 'Secondary'.
- `visible(filters)` → the claims on the board (`search()` filters plus department / encounterType / value band / age band; Void never); `annotate(rows, index?)` → `[{ claim, events, days, tone, bucket, silent, isSilent, escalated, kind }]`; `auditIndex()` → claim id → trail, one pass for a whole board; `board(filters, { collapse, bucket })` → `{ columns: [{ status, count, value, cards }], total }` over `BOARD_COLUMNS`; `aging(filters)` → `{ buckets, rows, totals, grand }` over days in status — the same `visible()` rows, so the two reconcile.
- `silent(payerId?)` → annotated Submitted/Acknowledged claims past their payer's silence threshold, longest silence first; `escalated(payerId?)` → those past the escalation threshold (the Claima home reads this); `queueStats(payerId?)` → `{ silentCount, silentValue, escalated, escalatedValue, dueToday }`. `peersReady` (a promise) / `peerStatus()`.
- `data/engines/payer-stats.js`: `summary(period)` → per tracked payer `{ payerId, name, type, submitted, acknowledged, submitToAckDays, submitToPayDays, rejected, rejectionRate, resubmitted, resubmissionRate, denialRate, silentCount, silentValue, escalated, sparkline, metrics }` — denial rate and days to pay are `performance-engine.rollup()` over the same rows; rejection rate = Rejected ÷ ever submitted; resubmission rate = `previousCycleNo` / `cycle > 1` ÷ submitted; `trend(payerId, months)` → monthly `{ key, label, submitted, adjudicated, denied, denialRate, daysToPay, paid, rejected }`; `exportCsv(period)`; `selfCheck(period)` → `{ pass, failures }` (asserts against `performance-engine.payerRollup` on load and logs one `[payer-stats] self-check` line); `PERIODS` / `periodRange` are Performance's; `COLUMNS` names and explains each metric.
- `data/repositories/followups.js`: `followUp = { id ("FU-0001"), claimNo, at (ISO timestamp), by, method (METHODS: Phone | Portal | Email | Visit), contact, note, nextDueAt (ISO date) | null }`, append-only. `all`, `get`, `byClaim(claimNo)` (oldest first), `latest(claimNo)`, `nextDue(claimNo)` (off the latest call), `isDue`, `dueOn(on)` → claim numbers, `counts()`, `log({ claimNo, method, contact, note, nextDueAt })` → the row (audited on the claim: entity `claims`, action `Follow-up`), `logMany(claimNos, fields)` → the rows in one commit.
- `CONFIG.claima.lifecycle = { silentDays: { default: 30, byPayer: {} }, escalationDays: { default: 60, byPayer: {} }, boardColumns: [Draft … Closed], hiddenColumns: [] }`.
- Routes: `#/claima/pipeline` (?status=&bucket=&payerId=&department=&type=&collapse=1), `#/claima/aging` (?payerId=), `#/claima/timeline/<claimNo>`, `#/claima/followups` (?payerId=&slice=escalated|due), `#/claima/payer-stats` (?payerId=&period=). `modules/claima/features/lifecycle/timeline-tab.js` exports `render(host, { claimNo })` for the claim page's Timeline tab (see Requests).

A29 — `data/repositories/remittances.js` (`remittances` table, seeded on first read by driving its own writes from `data/seed/remittances.js`), `data/repositories/unapplied.js` (`unapplied` table), `data/repositories/denials.js` + `data/seed/denials.js` (the stub A31 took over), `data/engines/posting-engine.js` (pure: reads CONFIG only) and the A29 section of `data/repositories/claims.js`.
- Shape: `remittance = { remittanceNo ('RMT-2026-000071'), payerId, payment { reference, date, method (EFT|Cheque|Transfer), total }, capture { mode (File|Manual), fileName, storedFile, parseReport { read, failed[{ row, reason, raw }] }, by, at }, claims[], status (STATUSES: Unposted | Posted with Exceptions | Posted | Closed), exceptions[], unapplied, postedAt, closedAt, postings[], createdAt, updatedAt }`. A claim row is `{ claimNo|null, payerClaimRef, memberId, dateOfService, billed, matchStatus (Matched|Ambiguous|Unmatched), candidates[], posted, postingId, lines[{ lineRef, chargeCode, claimLineId|null, matchStatus, billed, expected (the claim line's stamped payerShare), paid, adjustment, adjCode, denied, denialCode, outcome|null, variance|null, patientShift }] }`; an exception `{ id ('EX-1'), kind (AmbiguousMatch|Unmatched|Residue|Overpayment), ref, amount, postingId, at, resolution { kind, ref, reason, by, at }|null }`; a posting `{ id ('PST-001'), at, by, reversal, reverses?, reversedBy, reason?, txIds[], claimNos[], claims[{ claimNo, previousStatus }], denialIds[], handoffIds[], unappliedIds[], secondaryClaimNos[], summary }`.
- `byClaim(claimNo)` → `[{ remittanceNo, at, payerId, paid, adjusted, denied, patientShift, lines[{ lineId, lineRef, outcome, paid, adjustment, adjCode, denied, denialCode, variance, patientShift }] }]`, oldest first — posted rows only.
- `afterPostHooks[]` — push a function; called with `{ remittanceNo, claimNos, at }` after every posting and `{ …, reversed: true }` after every reversal. A hook that throws is logged and skipped.
- Reads: `all`, `get(no)`, `search(q, { payerId, status, from, to, unapplied })` (Unposted first, then oldest payment date), `counts()` → `{ total, unposted, unpostedValue, exceptionsOpen, withExceptions, unappliedCash, unappliedRows }`, `inFlightClaims(payerId)` (the match pool: Submitted/Acknowledged), `history(no)` (audit entity `remittances`, keyed on the number), `planFor(rem)` (the engine's plan — what Post would do), `control(rem)` → `{ total, paid, adjusted, denied, entered, unapplied, over, balanced }`, `openExceptions`, `livePostings`, `unpostedRows`, `closeBlocker(rem)`, `nextNo(year)`, `statusTone`, `matchTone`, `outcomeTone`, `toleranceOf(payerId)`, `toleranceLabel`, `adjCodes()`, `adjLabel`, `evaluateLine(line, expected, tol)`, `keyOf(row)`, `rowFromClaim(claim)`.
- Capture: `createFromFile({ fileName, text, parsed?, at, by })` → `{ error, remittance }` (parses through `posting-engine.parseEra`, matches every row against the payer's in-flight claims, keeps the file verbatim); `createManual({ payerId, payment, at, by })`; `updateHeader(no, patch)` (Unposted only); `addClaim(no, claimNo, { payerClaimRef })` → `{ error, row }` (exists, this payer's, in flight, not already on it); `removeClaim(no, key)`; `updateLines(no, key, lines[])`; `resolveMatch(no, key, claimNo)` → `{ error, row }` (re-pairs the lines, resolves the row's open exception).
- Posting: `post(no, { at, by })` → `{ error, result: { posting, claimNos, denials, shifts, shiftAmount, handoffs, secondaries, unapplied, exceptions, summary } }`. Every Matched row whose lines all match posts; per line the outcome is Paid in full | Adjusted (a PR-* shift, or a coded CO/OA shortfall inside the payer's tolerance) | Underpaid | Overpaid | Denied; the claim moves through `claims.applyRemittance` (→ `setStatus` Paid / Partially Paid / Denied, `remittanceId` = the number, `paidAt` = the payment date); a denied line → `denials.create` (one per line); a PR-* line → ledger `PortionShift` on the patient's account (`detail { payerShare: −x, patientShare: +x, adjCode, origin { remittanceNo, claimNo, lineId } }`) and `settlement-engine.reconcile(encounterNo)` when that engine exists; an Underpaid line past tolerance → `handoffs.create` (one per claim, note names the remittance, the lines and the tolerance); an Overpaid line → exception Overpayment; a primary with a cover behind it (`claims.secondaryPolicyOf`) and something left → `claims.activateSecondary`; residue (`payment.total − Σ paid` on every row, less what standing postings already hold) → `unapplied.create` and a resolved Residue exception. Ambiguous / unmatched rows → open exceptions, nothing written. Blocked when the rows claim more cash than the payment carries. Status: no live posting → Unposted; open exceptions or unposted rows → Posted with Exceptions; else Posted.
- `reversePosting(no, postingId, reason, { at, by })` → `{ error, reversal }`: ledger rows reversed as pairs, claims back through `claims.revertRemittance` to their previous status, denials marked Reversed, held unapplied rows Adjusted, secondaries emptied (`claims.deactivateSecondary`), the rows reopened for correction; a hand-off stays for Defensio. `resolveException(no, id, { kind ('Refund to payer'|'Apply as credit'|'Adjust'), reason, reference })` — Overpayment only (Apply as credit creates an unapplied row). `close(no)` — needs a live posting, no open exception, no unposted row.
- `data/repositories/unapplied.js`: `all`, `get`, `open()`, `byPayer({ status })` → `[{ payerId, rows, held, count }]`, `byRemittance(no)`, `counts()` → `{ total, held, heldAmount, overdue, payers }`, `ageDays(row)`, `isOverdue(row)` (past `CONFIG.claima.posting.unappliedAgeWarnDays`), `warnDays()`, `history(id)`; writes `create({ payerId, remittanceNo, postingId, amount, at, by })`, `apply(id, claimNo, { reason })` (this payer's in-flight or Partially Paid claim, through `claims.setStatus`), `refund(id, { reason, reference })`, `adjust(id, { reason })`. Row: `{ id ('UC-0001'), payerId, remittanceNo, postingId, amount, since, status (Held|Applied|Refunded|Adjusted), resolution { kind, ref, reason, by, at }|null }`. Audit entity `unapplied`. `seedHooks[]` — the remittance repository pushes its seeder; an empty table runs it.
- A29 section of `claims.js`: `secondaryPolicyOf(claim)` → the cover behind the claim's (`{ id, payerId, planId, claimNo|null }` — the A27-defined secondary's when the claim has an encounter, else the patient's chain), `applyRemittance(id, { remittanceNo, lines, status, paid, adjusted, paidAt, denialReasonCode, details, at, by })` (stamps `line.remittance { … }`, re-splits a PR shift onto `patientShare`, accumulates `totals.paid/adjusted`, sets `remittances[]`, `statusBeforeRemittance`), `revertRemittance(id, { remittanceNo, status, reason })`, `activateSecondary(primaryId, { remittanceNo, policy, lines[{ lineId, remaining }], primaryPayment })` (fills the defined secondary, or defines one for a generated primary; stamps `activatedAt` and `primaryPayment`; chains both ways; audited 'Activated'), `deactivateSecondary(id, reason)`.
- `data/engines/posting-engine.js`: `OUTCOMES`, `MATCH`, `EXCEPTION_KINDS`, `METHODS`, `ERA_COLUMNS`, `parseCsv`, `parseEra(text)` → `{ header, claims, read, failed }`, `failuresCsv`, `evaluateLine`, `matchClaim(row, pool)`, `matchLines(lines, claim)`, `control`, `plan(rem, { claimOf, nextPolicyOf, handoffExists, unappliedHeld })`, `summarize`, `toleranceOf`, `withinTolerance`, `isPatientResponsibility`, `adjCodes`/`adjCode`/`adjLabel`, `outcomeTone`.
- `CONFIG.claima.posting = { unappliedAgeWarnDays: 30, toleranceDefault: { pct: 0.05, amount: 25 }, tolerance: { 'PY-0001': …, 'PY-0008': … }, adjCodes: [{ code, label, group }] }`. Ledger `TYPES` gained `PortionShift` and `account-engine.effect()` reads it (payer share −x, patient share +x, charges unchanged). `data/seed/remittances.js` also exports `sampleEra(payerId = 'PY-0025')` → `{ fileName, text, claims }` (the upload screen's sample, built on demand off the claims table) and `eraCsv`.
- Seed: six remittances (RMT-2026-000071 … 76) over the generated in-flight claims — largest payer share first per answer, never a claim carrying a `batchId`, always submitted before the payment date — driven through `post()` with the dates the events happened on: NSSF Closed (four paid, one inside tolerance, $40 residue held 43 days), Bankers file Posted with Exceptions (an ambiguous row matched by nothing, a $35 overpayment, $25 residue), NSSF Posted with a PR-2 shift that activates the AXA secondary and a $75 residue applied three days on, Bupa Posted with a CO-197 denial, a PR-1 shift and a $224 hand-off, the Army fund's file Unposted with two failed rows, Allianz manual Unposted half entered. Next number RMT-2026-000077.

A33 — `data/repositories/writeoffs.js` (`writeoffs` table, seeded once the denials feature has loaded — `peersReady` — from `data/seed/writeoffs.js`) + `data/engines/writeoff-tiers.js` (a leaf: config and the static reason list) + `data/seed/writeoff-reasons.js` (static, like reference.js) + the A33 section of `data/repositories/claims.js`.
- Shape: `writeoff = { id ("WO-2026-000058"), source { kind (SOURCE_KINDS: Denial | ClaimResidual | PatientBalance | AgedItem), ref (denial id, claim no or MRN), encounterNo|null, claimNo|null, mrn|null }, side (Payer | Patient), amountRequested, amountPosted|null, reasonCode (W01…W07, W99), classification (Contractual | Discretionary — read off the reason), justification, hardshipEvidence { fileName, size }|null, status (STATUSES: Pending Approval | Approved | Rejected | Posted | Reversed), tier { required, mode (Single | Sequential), steps[{ tier, approver|null, decision (Approved|Rejected)|null, note|null, at|null }] }, requestedBy, at, decision { by, at, note, outcome }|null, posting { txIds[], at, by, cappedNote|null }|null, reversal { by, tier, reason, txIds[], at }|null, rerequestOf|null, rerequestId? }`. Audit entity `writeoffs`, keyed on the id; the decision page is `#/claima/writeoffs/<id>` (+ `/context|chain|history`), the request screen `#/claima/writeoffs/new?denial=&claim=&mrn=&rerequest=`, the worklist `#/claima/writeoffs` (?status=&source=&classification=&view=mine&kpi=), the analytics `#/claima/writeoffs/analytics` (?period=MTD|QTD|YTD).
- Published for peers: `requestFromDenial(denial, amount = denial.amounts.open ?? denial.amount, { justification, mode })` → the row or `{ error }` (a live request already on that denial refuses; the reason is read off the denial's Performance key — LATE_FILING → W02, COV_RULE → W01, else W06); `byClaim(claimNo)`, `byAccount(mrn)` (oldest first), `pendingFor(role = current)` (waiting, at a tier the role signs, not its own), `postedTotals(period | { from, to })` → `{ count, total, payer, patient, denialSourced, contractual, discretionary }`, `analytics(period)` → `{ totals, split, byReason, byPayer, byDepartment, byServiceLine, byRequester, trend, approvals, repeats, reconciliation }`, `reconciliation(range)` → `{ mine, theirs, pass, unresolvable, source }` (posted denial-sourced write-offs against `denials.amounts.writtenOff` over the denials they name; `selfCheck()` logs one `[writeoffs] self-check` line on load), `periodRange('MTD'|'QTD'|'YTD')`, `exportCsv(period)`, `peersReady` / `peerStatus()`.
- Reads: `all`, `get`, `search(q, { status, source, reasonCode, classification, band, side, from, to, mine })`, `counts()` → `{ pending, pendingValue, mine, approved, approvedValue, postedMtd, postedMtdValue, discretionaryPct, … }`, `outstandingFor(source, side)` (patient side → `accounts.balances(mrn).outstanding`; payer side → the denial's `amounts.open` when one is named and the feature holds it, else `claims.openBalance`), `agingOf`, `contextFor(source, side)` → `{ claim, patient, encounter, denial, payer, outstanding, agingDays, balances, prior[], priorValue }`, `sourceOptions(kind)` (the picker), `validate(data)` → problems[], `sideOf(kind)`, `denialOf(w)`, `payerOf`, `payerName`, `statusTone`, `bandOf` / `bandLabel` / `AMOUNT_BANDS`, `tierLabel`, `canDecide(w, role)` / `canReverse(w, role)` / `currentStep(w)`, `WRITEOFF_REASONS` / `CLASSIFICATIONS` / `reason` / `reasonLabel` / `classificationOf` / `EVIDENCE_REASONS` / `MODES` / `PERIODS` / `SOURCE_LABELS` / `SIDES`, `history(id)`.
- Writes: `request(data, { at, by, commit })`, `decide(id, { approved, note }, { role, at, by, commit })` (requester ≠ signer at every step; a refusal needs a note and ends the ladder; the last approval is Approved and posts at once when `CONFIG.claima.writeoffs.autoPostOnApproval`), `post(id)` (re-validates against what is outstanding now — capped with a note when the balance shrank, refused when nothing is left; payer side → `claims.adjust` + `denials.resolveWrittenOff` when denial-sourced; patient side → a ledger `Adjustment` with `detail.origin { writeoffId }` and `detail.allocations` oldest charge first, and an `account` trail entry), `reverse(id, { reason }, { role })` (a role at the request's tier or higher; payer side → `claims.reverseAdjustment` + `denials.reopenFromWriteoff` when published; patient side → `ledger.reverse` on each posted row).
- Claims (A33 section of `claims.js`): `openBalance(claim)` (`totals.balance`, never below zero), `adjustmentsOf(claim)`, `adjust(claimNo, { amount, reasonCode, writeoffId, note, at, by, commit })` (adds to `totals.adjusted`, keeps the entry under `claim.adjustments[]`, audits "Adjusted"; the status is not moved), `reverseAdjustment(claimNo, { writeoffId, reason, at, by, commit })` (audits "Adjustment reversed"). `data/engines/account-engine.js` gained an `adjustments` bucket: an `Adjustment` row answers the patient's balance (`outstanding`, `unpaid`, `patientDelta`) without being money, and `charges = payer + patient + undecided` still holds.
- Roles: `canApproveWO1` (coder, exec), `canApproveWO2` / `canApproveWO3` (exec). `CONFIG.claima.writeoffs = { tiers: [{ tier: 1, max: 500, role: 'canApproveWO1' }, { tier: 2, max: 5000, role: 'canApproveWO2' }, { tier: 3, max: null, role: 'canApproveWO3' }], mode: 'Single', autoPostOnApproval: false }`.
- Seed: eleven requests found on the registers by rule (`data/seed/writeoffs.js`), numbered WO-2026-000047…057 so the first live request is 000058 — a posted timely-filing write-off on the denial the triage seed classes as a Write-Off Candidate (the reconciliation's one figure), two denial requests waiting at tier 1 and tier 2 (Denied claims with no denial record stand in when the register has no candidate left), an approved residual to post, a posted residual, a small balance posted on the ledger, a sequential hardship write-off with tier 1 signed and tier 2 waiting, a refused bad debt and its re-request, a posted-then-reversed administrative error, and an aged item on the oldest claim the payer has gone quiet on. Requesters are two clerks who never sign in, so both demo signers have work.

A32 — `data/repositories/nullifications.js` (`nullifications` table, seeded once the claim repository's peers have settled — `ready` — from `data/seed/nullifications.js`; re-seeds after a reset) + `data/seed/nullification-reasons.js` (static, like reference.js) + A32 sections of `claims.js`, `batches.js`, `charges.js`.
- Shape: `nullification = { no ("NUL-2026-000019"), claimNo, claimId, patientMrn, encounterNo|null, payerId, planId, policyId|null, statusAtNullification, path (PATHS: Direct | PayerNotified | EndChain), reasonCode (N01 Wrong payer, N02 Duplicate, N03 Wrong encounter, N04 Coding error requiring full rebuild, N05 Payer instruction, N06 Internal test / erroneous assembly, N99 Other — text required), reasonText|null, justification, payerNotification { method (Portal|Email|Phone|Letter), reference, date, note|null }|null, disposition { kind (ReturnToUnbilled | ReturnAndHold), reason|null, lineIds[] (register CHG- ids returned), skipped[{ id, why }] }, lines[{ chargeLineId, itemId, chargeCode, description, qty, amount }], batchRemoved { batchNo, at }|null, reclassification { from { policyId, payerId, planId, label }, to { … } }|null, replacement { claimNo|null, kind (Fresh | WrongPayerReclass), at, reason ('' or why none) }|null, chainEnded, actors { requestedBy, approvedBy|null }, valueAtNullification, at }`. Immutable: `nullify` is the only write. Audit entity `nullifications`, keyed on the number; the log is `#/claima/nullifications` (?q=&reasonCode=&path=&disposition=&payerId=&from=&to=&replacement=1|0), the record `#/claima/nullifications/<no>`.
- Published for peers: `byClaim(claimNo)` → the records on that claim, oldest first; `replacedBy(claimNo)` → the record whose replacement this claim is, or null; `log(q, { reasonCode, path, disposition, payerId, from, to, replacement })` → rows newest first; `stats({ from, to })` → `{ count, value, replaced, replacementRate, topReason { code, label, count }|null }` (`monthStart()` for MTD); `pathFor(claim)` → `{ path, blocked, why, route { label, href }|null, batch (the open batch it sits in)|null, note }` — Draft/Ready → Direct, Submitted/Acknowledged → PayerNotified, Rejected → EndChain, Paid/Partially Paid/Closed → blocked with the remittance route, Denied/Appealed → blocked with the denial route (`denials.byClaim` by dynamic import, feature-detected), Void → blocked with the record; `validate(claim, form, actors, role)` → problems[]; `needsSecondApprover(claim)`, `threshold()`, `approversFor(requesterName)`, `reclassOptions(claim)` (the patient's other active covers plus Self-Pay), `valueOf(claim)`, `nextNo()`, `history(no)`, `ready`, the reason/path/disposition vocabularies re-exported.
- Write: `nullify(claimNo, form, { at, by, actors, role, no })` → `{ record, replacement (the fresh claim or null), error }`. `form = { reasonCode, reasonText, justification, notification { method, reference, date, note }, disposition, holdReason, replace (bool), reclassPolicyId (N01: a policy id, null for Self-Pay, undefined when not chosen) }`. In order: `batches.removeClaim` when the claim sits in an open batch; `claims.nullify` (Void, lines Void, an unactivated secondary defined beside it voided too); `charges.returnFromClaim` on the register lines behind the claim's lines; for a wrong payer `encounters.reclassify` (Frontis's helper) with the nullification as the reason; then `charges.releaseForReplacement` + `claims.assembleReplacement` — always a fresh assembly of the visit as it stands, never a copy, linked both ways.
- Claims (A32 section of `claims.js`): `nullify(claimNo, { nullificationNo, reasonCode, reasonLabel, justification, path, at, by })` → the claim Void (audited "Status: from → Void — Nullified NUL-… — N0x label — justification · path"); fields `nullification { no, at, path, via? }`, `statusAtNullification`, `replacedBy|null`, `replaces|null`; `isNullified(claim)`. `assembleReplacement(nullifiedClaimNo, { at, by })` → `{ claim, created, secondary, reason }` (the same assembler run `assembleFor` does, on a visit whose primary is void; self-pay, no released lines or a live primary → nothing, with the reason).
- Batches (A32 section of `batches.js`): `removeClaim(batchNo, claimNo, reason, { at, by })` → the batch or null — Open or Generated only, the claim unbatched, a Generated batch back to Open, recorded in `exclusions[]` and audited "Claim removed".
- Charges (A32 section of `charges.js`): `returnFromClaim(lineIds, { nullificationNo, claimNo, hold, reason, at, by })` → `{ returned[], skipped[{ id, why }], at }` (Released → Unreleased, or Held with the reason; each line keeps `returnedFrom { claimNo, nullificationNo, at }`; audited "Returned from claim"; no correction hook fires — the claim is already void); `releaseForReplacement(lineIds, { nullificationNo, claimNo, at, by })` → `{ released[], excluded[] }` (release() without the hooks, so the coding clock does not restart); `linesForClaim(claim)` → the register lines behind a claim's lines, by `chargeLineId` or a shared ledger row.
- Roles: `canNullifyClaim` and `canApproveNullification` (coder, exec — each can sign for the other). `CONFIG.claima.nullification = { secondApproverThreshold: 1000, roles: { request: 'canNullifyClaim', approve: 'canApproveNullification' } }` — the amendment's $2,000 was lowered because the largest claim in the dataset bills $1,161.
- Seed: six records, NUL-2026-000013…018 (the first live one is 000019): an end-of-chain on a generated AXA claim rejected twice (its two cycles written onto the claim, no batch — the generated rows' own convention); the two-approver withdrawal of the largest acknowledged claim still with its payer (Bankers, $1,120.50, notified by email); a duplicate in the acknowledged Bupa batch, withdrawn on the portal; the ISF clinic claim the scrub could never pass, re-classified to Self-Pay through `encounters.reclassify` with no claim to assemble (the fund confirms cover and holds no direct-billing agreement); the finalized MOPH clinic claim added to BAT-2026-000141 and removed two hours later, assembled fresh — its replacement fails the scrub on "no coding" because the chart is in the coding backlog, and A27's coded hook refreshes it when the chart is marked coded; and the Allianz clinic claim whose blood count was captured on the wrong visit, returned on hold. Picks are by state, never by number, and never a claim with a trail another seed wrote.

A31 — `data/repositories/denials.js` (the entity, taken over from A29's stub; every helper the stub published is kept: `create`, `reverse`, `seedHooks`, `byClaim`, `byRemittance`, `worklist`, `counts`, `history`, `DENIAL_CODES` / `denialCode` / `denialCodeLabel` / `reasonOf`) + `data/engines/denial-router.js` (a leaf: reads `data/seed/root-causes.js`, `data/seed/denials.js` and config only) + `data/seed/root-causes.js` (static, like reference.js).
- Shape: `denial = { id ("DN-0001"), claimNo, claimId, lineId|null, remittanceNo|null, payerId, patientMrn, encounterNo|null, contractId|null, scope ('Line'|'Claim'), code, reason, reasonCode (Performance's key), payerReason { code, text }, amount, amounts { denied, recovered, writtenOff, lost, transferred, open }, class (Corrigible | Appealable | Write-Off Candidate)|null, rootCauseId|null, triageNote, route { kind, ref, at, by, reason|null, active, answer? }|null, routeHistory[{ …route, endedAt, endedReason }], status, assignee|null, deadline { appealBy, passed }, repeatCount (1..n), previousDenialId|null, repeats[{ remittanceNo, code, amount, at }], resolvedAt|null, resolution { kind (Remittance | Handoff | WriteOff | Manual), ref, reason, manual, by, at, successorId? }|null, createdAt, createdBy, updatedAt }`. Invariant, asserted by `selfCheck()` on load: denied = recovered + lost + writtenOff + transferred + open per row, and the four-bucket sum over a chain equals the first denied amount.
- `STATUSES` = Untriaged | Triaged | Routed | In Progress | Recovered | Partially Recovered | Written Off | Lost | Deadline Passed | Manually Resolved (+ A29's `Reversed` for a withdrawn posting); `OPEN_STATUSES` = the first four plus Deadline Passed; `RESOLVED_STATUSES` the five answers. `ROUTES` = Recode | ChargeCorrection | AuthRework | Refresh | DefensioHandoff | PayerReconsideration | WriteOff; `routeLabel`, `ROUTE_HINTS`, `ROUTE_ICONS`, `classTone`, `statusTone`, `isOpen`, `isResolved`, `AMOUNT_BANDS` / `bandOf`, `payerReasonLabel`.
- Reads: `all`, `get(id)`, `byClaim(claimNo)` (oldest first), `byRemittance(no)`, `worklist(filters?)` → the OPEN denials, Untriaged first then largest open amount (the A29 meaning — the write-off seed reads it), `search(q, { payerId, status, class, code, rootCauseId, route, band, from, to, deadline ('week'|'passed'|'open'), assignee ('me'|name), scope, repeat, open, resolvedFrom })` → the whole register in the same order, `counts()` → `{ total, open, amount, openValue, untriaged, inProgress, recoveredMtd { count, amount }, writtenOffMtd { count, amount }, deadlinePassed, nearDeadline }`, `nearDeadline(days = 7)` → open denials whose appeal window closes inside `days`, soonest first (the home), `claimOf`, `lineOf`, `chargeLineOf` (through the stamped id or a shared ledger row), `facts(denial)`, `suggest(denial)` → `{ rootCauseId, class, route, fallbackFrom, why }`, `routeOptions(denial)` → every route with `{ ok, why }`, `deadline(denial)` → `{ appealBy, daysLeft, passed, warn, binding }`, `ageDays`, `chainOf(denial)`, `linkOf(denial)` → `{ label, href }` of the route's work item, `progressOf(denial)`, `analytics({ from, to })` → `{ totals, months, reasons[], rootCauses[], byPayer[], byDepartment[], byServiceLine[], byDoctor[], repeats[], prevention[] }`, `filterOptions()`, `history(id)`, `peersReady` / `peerStatus()`, root causes re-exported (`ROOT_CAUSES`, `GROUPS`, `OWNERS`, `rootCause`, `rootCauseLabel`, `groupedRootCauses`).
- Writes: `create({ claimNo, claimId, lineId, remittanceNo, payerId, code, amount, scope, at, by })` → the row (one denial per open claim line: a repeat reopens the record with `repeatCount + 1` and the route ended; a line denied again after resolution starts a new record chained through `previousDenialId`); `reverse(id, reason, { at, by })` (pops the last repeat, else withdraws); `assign(id, name | 'me' | null)`; `triage(id, { class, rootCauseId, note })` → the row or `{ error }`; `triageMany(ids, { class, rootCauseId, note, route? })` → `{ done[], problems[] }`; `route(id, kind, { reason })` → the row or `{ error }` (triage first; one active route; a second needs a reason) — creates the work item through `coding.requestRecode`, `charges.requestCorrection`, `preauth.resubmit` / `renew` / `create`, `claims.openForDenial` (+ `refresh`, or Ready as filed with no visit), `handoffs.create`, `claims.setStatus(Appealed)` + `followups.log`, `writeoffs.requestFromDenial`; `resolveFromRemittance(claimNo, lines, { remittanceNo, at, by })` (subscribed to `remittances.afterPostHooks` — paid ≥ open → Recovered, part → Partially Recovered + successor); `resolveWrittenOff(denialId, writeoffId, amount, { at, by, reason })` (A33 calls); `reopenFromWriteoff(denialId, writeoffId, amount)` (A33's reversal calls); `resolveFromHandoff(handoffId | denialId, 'Won' | 'Lost' | 'Settled', { amount })` (also fired by a `handoff.status` commit that marks the hand-off Recovered); `recordHandoffOutcome(id, outcome, { amount })` (moves the hand-off, then the denial); `recordPayerAnswer(id, 'Overturned' | 'Upheld', { note })`; `resolveManual(id, { result: 'recovered' | 'lost', reason })` (needs `role.canResolveDenial`); `sweep()` (Deadline Passed on an unrouted denial past its window, In Progress on a routed one whose work item moved — runs on the first read and after a reset).
- A31 sections elsewhere: `claims.openForDenial(id, { denialId, code, reason, at, by })` → the claim as a Draft with `rejectedCycle` set from the denial (Denied / Appealed / Partially Paid only), so A28's chain carries it out on its next cycle; `charges.requestCorrection(lineId, { source, ref, reason })` → the line Held, flagged Disputed, with `corrections[{ id ("CCR-0001"), source, ref, reason, at, by, status }]` appended.
- `CONFIG.claima.denials = { appealWindowDays: { default: 30, byPayer: { PY-0001: 45, PY-0002: 60, PY-0025: 21 } }, deadlineWarnDays: 7 }`; `role.canResolveDenial` (coder, CMO). Audit entity `denials`, entityId = the id; `shared/activity-claima.js` resolves it to `#/claima/denials/<id>`.
- Routes: `#/claima/denials` (?status=&payerId=&deadline=week|passed|open&rootCauseId=&route=&assignee=me&class=&code=&slice=open|untriaged|progress|recovered|writtenOff), `#/claima/denials/<id>` (+ `/history`), `#/claima/denials/analytics` (?period=YTD|lastQuarter|last90|last30).
- Seed: 16 intents over the generated Denied claims plus A29's Bupa CO-197 — every status but Written Off is seeded here; the two Write-Off Candidates are left unrouted for A33's seed, which posts the LATE_FILING one and calls `resolveWrittenOff`. `store.resetToSeed()` re-seeds on the next read.

A34 — `data/repositories/business-days.js` (the business day and the daily transaction report) and `data/repositories/cash-sessions.js` (the cashier's drawer). Engine `data/engines/dtr-engine.js` is a leaf: `compute(world)`, `select(world, drill)`, `compact(report, day)`, `csvOf(snapshot, meta)`, `systemTotals(session, ledgerRows)`, `cashEffect(row)`, `isCashRow(row)`, `methodOf(row)`, `ledgerDay(row)`, plus `SECTIONS`, `CATEGORIES`, `CHECKS`, `METHODS` — the repository builds the world (every register's rows, never a stored total) and hands it over.
- Shape: `businessDay = { date, status (Open|Closed|Reopened), openedAt, closedAt|null, closedBy|null, versions: [{ version, dtrNo ("DTR-2026-0253-v1" — year, day of year, version), snapshot (the full report JSON), closedAt, closedBy, reopenReason|null, exceptions: [{ checkKey, note, by, at }] }], reopenEvents: [{ at, by, reason }], exceptions: [...] (the working list; copied into the next version) }`. `cashSession = { id ("CS-2026-000871"), date, cashier, openedAt, closedAt|null, status (Open|Closed), systemTotals {Cash, Card, Transfer, Cheque}|null (frozen at close; live before), declared|null, variance { per method, total }|null, varianceReason|null, countersign { by, at }|null, txIds[], receiptNos[] }`. A ledger row written after its day closed carries `priorDay: { postedOn, originalDate }` (stamped by the ledger afterAppend hook this repository registers; the row's own `at` never changes).
- Report: `{ date, computedAt, sections: [{ key (charges|claims|payerCash|patientCash|adjustments), label, totalLabel, rows: [{ key, label, group, category|null, inTotal, count, amount, priorDayCount, priorDayAmount, drill: { repo, filter: { kind, date, value?, priorDay? } } }], categories: [{ key, label, opening, movement, closing }], total, count, priorDayAmount, opening, closing, unposted? (payerCash: { count, amount }) }], checks: [{ key (cashIntegrity|payerPostingEquation|openingMovementClosing|writeoffReconciliation|allSessionsClosed), label, hint, status (OK|RED), summary, figures: [[label, value, 'count'?]], offenders: [{ id, at, label, sub, amount, href, priorDay, action? ('attach'|'close-session'), actionLabel? }], documented: exception|null }], sessions: [summaries], priorDate, redCount, undocumentedReds }`. A closed day's report is its stored version, never recomputed; `report(date)` says `frozen: true` and carries `dtrNo`, `version`, `closedAt`, `closedBy`, `reopenReason`.
- `dtr.today()` (named export `today`) → `{ date, status, dtrNo|null, version, sections: { charges: { total, count, label, totalLabel }, claims, payerCash (+ unposted: { count, amount }), patientCash (+ byMethod: { Cash, Card, Transfer, Cheque }), adjustments }, checks: [{ key, label, status, count, documented, summary }], redCount, undocumentedReds, sessions, openSessions, computedAt }` — always answers, computed live for today, before and after the seed.
- Reads: `all`, `get(date)`, `list()` (newest first), `isClosed(date)`, `current()` (today's row, opened on first touch without a commit), `nextOpenDay(date)` (the earliest not-closed day on or after it; opens tomorrow when today is closed), `priorSnapshotOf(date)`, `world(date)`, `compute(date)` → the live report, `report(date, version?)`, `drill(drill)` → the transactions behind a row (`[{ id, at, repo, amount, label, sub, href, priorDay }]`), `archive()` (days with a version, newest first, each with `latest`), `mtd(on?)` → `{ month, from, to, days, sections: [{ key, label, totalLabel, total, count, categories: [{ key, label, movement, count }] }] }`, `latestVersion(day)`, `dtrNoFor(date, version)`, `history(date)`, `closeBlocker(date, role?)`, `reopenBlocker(date, role?)`; `ready` (the seed, behind `claims.peersReady`, `nullifications.ready`, `denials.peersReady`, `writeoffs.peersReady` — await it before reading a day).
- Writes: `closeDay(date, { at, by, role })` → `{ error, day, version }` (every session closed and zero undocumented REDs; freezes `compute(date)` as the next version, locks the day), `reopenDay(date, reason, { at, by, role })` → `{ error, day }` (role `canReopenDay`), `documentException(date, checkKey, note, { at, by, role })` → `{ error, day }` (role `canDocumentException`; one note per check, replaces an earlier one; refused on a closed day).
- Sessions: `onDate(date)`, `get(id)`, `openFor(cashier, date)`, `receiptsOf(session)`, `systemTotals(session)`, `varianceOf(session, declared)`, `totalOf(totals)`, `unattached(date)` (cash rows on the day in no session), `counts(date)` → `{ total, open, closed, withVariance, unattached }`, `countersigners(exclude[])`, `threshold()` ($50, `CONFIG.claima.dtr.sessionVarianceCountersignThreshold`), `openBlocker(cashier, date)`, `openGuards[]` (a function `(date) → '' | why`; the day repository pushes the closed-day guard), `history(id)`. Writes: `open(cashier, date, { at, by })` → `{ error, session }`, `attach(ref (tx id or receipt no), cashier?, date?)` → the session or null (called from the ledger hook on every cash row; keyed on the ledger's `by` and the business day the row is reported on), `attachOrOpen(txId)` → `{ error, session }` (the cash-integrity fix), `closeBlocker(session, { declared, reason, countersignBy }, closer)`, `close(id, { declared, reason, countersignBy }, { at, by })` → `{ error, session }` (final: totals frozen, receipts locked; a non-zero variance needs a reason, one past the threshold a countersignature from a `canCountersign` role who is neither the cashier nor the closer).
- Roles: `canCloseDay` (coder, CMO), `canReopenDay` (CMO), `canDocumentException` (coder, CMO), `canCountersign` (coder, CMO); `CONFIG.claima.dtr.roles` names them. Audit entities `businessDays` (entityId = the date) and `cashSessions` (entityId = the session id); neither has a branch in `shared/activity-claima.js` yet — the day is read at `#/claima/dtr/<date>`, a session on that day's page.
- Seed: five days ending today (D−4 v1, D−3 v1 → reopened → v2, D−2 v1, yesterday v1 over one documented cash-integrity exception, today Open) computed by the engine over the registers as they stand; three sessions today (Rima Nassar closed $78.75 short and countersigned, Maya Zgheib open with two receipts, Nadine Rizk open with none); two `Payment` rows pushed into the ledger by the sessions seed (tagged `detail.seedTag` `A34:yesterday` / `A34:prior-day`, against the two untouched accounts owing the least) — the second carries `priorDay` and is today's prior-day row. The A24-era $264 card settlement Tarek Solh took with no session is today's deliberate RED. `store.resetToSeed()` re-seeds once the peers have settled.

## Requests
- A28 → A27: the claim page at #/claima/claims/<no> has no tab registry, so the cycle strip ("Cycle 1 (Rejected R01) → Cycle 2 (Ready)") and the batches a claim went out in are shown on the batch page and the rejections worklist only. If claim-view.js gains a Submission tab (or the Timeline tab above lands, which reads `batches.byClaim` already), it can draw `claims.cycleChain(row)` and `batches.byClaim(row.claimNo)` — both published above, no import from the submission feature needed. Link only until then.
- A30 → A27: the claim page at #/claima/claims/<no> wants a Timeline tab (amendment 30 names #/claima/claims/<no>/timeline). In claim-view.js add `{ id: 'timeline', label: 'Timeline' }` last in TABS and, in drawPanel, `else if (state.tab === 'timeline') import('../lifecycle/timeline-tab.js').then((m) => { if (host.isConnected) m.render(host, { claimNo: row.claimNo }); });` — A30 exports `render(host, { claimNo })` from modules/claima/features/lifecycle/timeline-tab.js (read-only: the family strip and the events, no listeners to retire). Until then the page is #/claima/timeline/<claimNo>. (harp-93 acknowledged; deferred to the consolidation pass, which adds the claim-view tab registry and takes this with the Submission tab request.)
- A22 (Frontis, harp-31) → A29 / A33 / A34, for information — nothing to change on the Claima side: `data/engines/settlement-engine.js` now exists. `reconcile(encounterNo)` is pure (it returns `{ actualShare, paid, difference, outcome, drivers, … }` and writes nothing), so the dynamic call in `data/repositories/remittances.js` `shift()` fires harmlessly; the settlement stamp and the account flags are recomputed by `data/repositories/accounts.js` off `ledger.afterAppendHooks` on every appended row, which is why a PR-* PortionShift (verified: RMT probe on ENC-2026-000401 moved the actual share 8.48 → 13.48) and a patient-side write-off Adjustment (verified: WO-2026-000053 posted on ENC-2026-000411 settled it) both update the visit's outcome without `writeoffs.js` or `posting-engine.js` calling anything. The engine counts PortionShift rows into the actual share and Adjustment rows (any origin) as reductions. Ledger row shapes are unchanged; the two new row types are `Settlement` (a marker: amount = the share settled at, `detail { estimatedShare, actualShare, paid, difference, reconcilingTxIds, manual, mode, consent }`) and `Refund` (`detail { sourceTxId, sourceReceiptNo, method, reference, purpose: 'Refund' }`, a receipt issued as the voucher). `ledger.afterAppendHooks` is the array A34 asked for, called with the row after every append.

## Conflicts
- (harp-17, A26) Not Claima's: `store.resetToSeed()` throws "Maximum call stack size exceeded" at committed HEAD too (the derived seeds — encounters, eligibility, estimates, pre-auth — re-enter each other while every table is empty, because the 'reset' commit runs subscribers before anything is rebuilt). Reproduced on a clean checkout of f86282c served separately, so it predates amendments 24–27.
- (harp-17, A26) Not Claima's: `modules/frontis/features/accounts/account-receipts.js` and `account-view.js` fail to import in the browser ("Identifier 'emptyHtml' has already been declared") at HEAD.
- (harp-bb, A29) For the consolidation pass: A27's Refresh on an activated secondary that has no encounter (one `claims.activateSecondary` defined for a generated primary — `encounterNo` null) fails in `claims.refresh()` with "Encounter null is gone"; the claim page and the portfolio row should disable Refresh when `claim.encounterNo` is null, the way they already do for an unactivated secondary (modules/claima/features/claims-assembly/claim-view.js `actionsHtml`, portfolio-rows.js `rowHtml`).
- (harp-bb, A29) For the consolidation pass: `shared/activity-claima.js` has no branch for the `unapplied` audit entity (unapplied cash, ids `UC-0001`…, actions Held / Applied / Refunded / Adjusted), so those rows fall through to the bare fallback in Recent activity. The rows have no page of their own — every one is read on `#/claima/remittances/unapplied` — so the branch is a label ("Unapplied cash") and a link to that list, ignoring the id. harp-17, who wrote the file, has gone.
- (harp-31, A33) Not A33's: `[performance] self-check FAIL — PY-0004 grossBilled: parts 72131.25 vs whole 507.5; … PY-0008 …` on a clean load of the current tree. In node it flips the moment `data/repositories/remittances.js` is imported (passes with claims.js alone, fails right after the import, with nothing else read in between — whatever the module runs on load does it); afterwards `claims.byPayer('PY-0004')` returns one claim with `contractId` null while the contract rollups under PY-0004 still sum to ~$72k. harp-bb (A29) has gone; for the consolidation pass.
- (harp-da, A32) Not Claima's — for the consolidation pass: Pactum's Performance counts a Void claim's gross in "Gross billed" (`claims.list()` and `data/engines/performance-engine.js` `rollup()` have no status exclusion, so a nullified claim still bills its payer there); the consolidation pass adds a Void/status exclusion to performance-engine.js. Ruled by the user on A32's close.
- (harp-0b, A31) Diagnosis of the `[performance] self-check FAIL` above, for the consolidation pass: `claims.list({ contractId })` treats a null id as "no filter", so `contractRollup(null)` sums every claim in the period and the payer whose claim set carries a `contractId: null` row fails to reconcile — two such rows sit in the YTD range, A27's ISF claim CLM-2026-000610 (no agreement covers the plan; Void since A32's seed) under PY-0004 and the Secondary CLM-2026-000612 defined behind CLM-2026-000603 under PY-0008. Fix in `selfCheck()` (skip a null contract id, or roll it up as "no contract") and in `list()` (a null passed on purpose should match null); nothing in A31 writes `contractId`.
- (harp-01, A35) For the consolidation pass: four deep links the home amendment names are not parameters the target screens read, so the dashboard links to the whole list and names the slice in the sub-line. `modules/claima/features/coding/coding-worklist.js` `applyQuery` reads `?beyondSla=1`, not `?sla=breached`; `modules/claima/features/lifecycle/followups.js` reads `?slice=escalated`, not `?escalated=1`; `modules/claima/features/charge-capture/unbilled-worklist.js` `applyQuery` reads no `?action=manual` (the Manual charge quick action lands on the worklist, whose header holds the button — `openManualCharge` in `manual-charge.js` is what an `action=manual` branch would call on mount); `modules/claima/features/submission/submission-workbench.js` `applyQuery` reads no focus parameter (the rail's Ready value card already focuses `#sw-queue-panel` — a `?focus=queue` branch would call the same). When those land, `modules/claima/features/home/home-kpis.js` and `home-quick-actions.js` carry the hrefs to update.
