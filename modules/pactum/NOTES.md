# Pactum — notes
- Dependency (A41, 2026-09-11): denial figures source = defensio A41 — `data/engines/denial-analytics.js` `getDenialMetrics(payerId?, period)` is the definitions-table object Performance's denial columns read from here on (no Pactum code edited by that amendment).
- A44 (harp-d0, 2026-09-11): Pactum owns the standard code systems — `codeSystems`, `codeSystemVersions`, `standardCodes` (data/repositories/code-systems.js, code-system-versions.js, standard-codes.js; seeds in data/seed/ of the same names, registered in data/store.js; audit keys are the table names). Screens under modules/pactum/features/standard-codes/: `#/pactum/standard-codes` (?systemType=&status=&noCurrent=1), `/<id>` (+ `/versions|codes|history`, `?version=`), `/<id>/import?version=`. Published lookup, registered in modules/claima/COORDINATION.md too: `lookupCodes({ systemType | codeSystemId, query, atDate, limit })` → Active codes of the version whose validity covers `atDate` (default the current version; no current → newest active version + one console.warn; nothing active → `[]` + a warn, never silent), rows carrying `systemName` / `versionLabel` / `fallback` beside the flattened attributes; `resolveVersions()` names the version each system answered on. Invariants: at most one current version per system, `setCurrent()` swaps atomically and asserts it; terms of one system never overlap (a new release closes the open-ended one before it, the day before it starts); a code is unique in its version; nothing is deleted. Claima takeover: `data/seed/code-sets.js` and `data/repositories/code-sets.js` are thin adapters over the lookup (A26 import paths unchanged, each read taking an `atDate`), and the coding pickers + validation read on the visit's date of service — logged under A44 in the Claima file.
