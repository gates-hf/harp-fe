# HARP prototype

High-fidelity clickable prototype for client demos. Plain HTML + CSS + vanilla JS ES modules, no framework, no build step. Serve with `python serve.py` from the repo root — http://127.0.0.1:8000. Not `python -m http.server`: on Windows it serves `.js` as `text/plain`, Chrome refuses every module and the page renders blank.

## Structure
```
index.html            thin shell: loads app/, nothing else
app/                  router (hash-based, deep-linkable), layout, nav from module manifests
shared/               toast, modal, formatting, mock roles
data/store.js         in-memory store, sessionStorage persistence, resetToSeed()
data/repositories/    one file per entity — the only way modules read/write data
data/engines/         pure logic shared by screens: evaluate, price, summarize. No DOM, no writes
data/seed/            one file per entity
modules/<module>/module.js            manifest: id, name, nav, routes -> lazy import() of features
modules/<module>/features/<feature>/  <feature>.html + <feature>.js (one feature per folder)
modules/<module>/components/          shared within that module only
modules/_template/                    copy to start a module, then register in app/modules.js
```

## Hard rules
- No monolith: one feature per folder, one entity per seed/repository file, ~300-line max per file. One entity, one repository file overrides the 300-line cap, and so does one entity, one seed file. Engines in `data/engines/` are exempt too — a set of definitions two screens read from is one file or it is not a single source of truth.
- Modules connect only via routes and repositories — never by importing each other's files.
- Layering is one-way: modules may import from `data/`, `data/` never imports from `modules/`. Inside `data/`, an engine may read repositories; a repository may import only a leaf engine — one that imports nothing from `data/` itself — so the two never form a cycle. Pure logic two screens share is an engine, not a copy in each feature.
- `design-system/` is the single source of truth. Use its classes as-is; never write CSS in a module. `app/app.css` holds shell glue only — link resets, popover placement, page rhythm — never module or component styles.
- KPI footer lines use `.metric-rail-card__sub`; a rail sizes its own columns and never wraps — only a rail inside a narrow column names a track count (`.metric-rail--N`).
- Every KPI card is a control, built by `shared/metric-card.js` and never by a local helper: it selects the rows it counts on the screen it sits on — the card's number is the row count under it, and `aria-pressed` says that slice is on screen — or it links to the screen that holds them. A card with nothing to select stays a plain number.
- Navigation is registry-driven: `app/modules.js` lists the manifests, the topbar `.mod-tabs` pick the module, the sidebar shows only that module's screens under `.mod-side-head`. Adding a module is one line in the registry; the shell does not change.
- One owner per entity; cross-module references share the same IDs. Ownership is a note in the owning module's manifest or README — the repository and seed stay in `data/`, they never move. `patients` is shared scaffolding until an amendment names its owner.
- Seed data: realistic Lebanese context (names, cities, +961 phones, LBP/USD, current-year dates); 30–60 rows per main entity. `data/seed/claims.js` is the one seed that derives from repositories rather than being written out, so `data/store.js` does not import it: `data/repositories/claims.js` generates on first read of an empty table, and `handoffs.js` does the same off the result. Any future generated dataset follows that shape.
- Page actions go in the panel header, not `ctx.actions`. A screen's create-actions may instead ride the floating action bar — `mountFab` from `shared/fab.js`, styled by `.fab` — which is the design system's and open to every module, not one screen's furniture.
- Everything clickable: every button navigates, opens a modal/drawer, mutates the store with feedback, or is disabled with a tooltip saying why. Forms validate and save.
- Voice: sentence case, second person, present tense, no exclamation marks, no emoji.
- Claima is built by parallel sessions under modules/claima/COORDINATION.md; those rules override the single-session norm for Claima only.

## Consistency rules
- IDs everywhere in data; labels only at render time.
- All dates stored as ISO `YYYY-MM-DD`; compare with the one shared helper (`iso`, `compareDates`, `withinDates`, `todayIso` in `shared/format.js`); display with the one shared formatter (`date`, `dateTime`).
- Every list subscribes to the store and re-renders on change (`ctx.onData(draw)`); no screen relies on a reload.
- Every picker of payers/plans/items/bundles/contracts calls the repository's `findActive()`-style helper; no local copies of lists.

## Speed
- Read only this file and the files the amendment names. No re-reading the design system, other features, or the repo.
- Purely additive: never refactor an existing feature unless the amendment says so.
- Seed data: hand-write 8–10 rows, generate the rest by combining short lists. No large literal arrays.
- Verify with `node --check` on new JS and one load of each new route. No end-to-end walkthroughs; the user tests.
- Verification must load each new route in the browser; `node --check` does not catch duplicate declarations.
- Copy the patterns in `features/payer-master` (list, modal, importer) instead of designing new ones.
- Every screen must use `ctx.onData` for store subscriptions; copy `_template` if unsure.
- Reply: route list + open questions, nothing else.

## Applying an amendment
Read it, reuse existing entities and repositories, implement, load each new screen once to confirm it renders, append a 2–3 line changelog entry below — numbered after the amendment file, not after the entries already there (`amendment7.md` → `### 07`). Nothing else.

Ambiguous data model or navigation → ask. Ambiguous UI → design-system default, note it in the changelog.
Replies: what was built, open questions. No summaries.

## Changelog

### 01 — Pactum: Payer Master
Added module `pactum` (owner of `payers`) with Payer Master at `#/pactum/payers`: list with search, type/status filters, sorting, paging and row actions; 4-tab add/edit modal; history drawer; bulk import at `#/pactum/payers/import`. New shared `audit` entity (append-only) plus `isEmail`/`fileSize` in shared/format.js.
UI calls: the tab error dot is a filled critical `error` icon; documents are transactional — the modal edits a draft and the repository writes the Document added/removed entries on save, so Cancel discards.
Shell: `app/main.js` now replaces the `#page-body` node on every route render, so listeners a feature binds to its mount retire with it — features bind delegated listeners the ordinary way.
Added `shared/drawer.js`, the right-side sheet (title, sub, icon, body, foot; Escape, backdrop and X close it; it shares the modal's `.scrim` stack and focus trap). View history opens in it. Its placement lives in `app/app.css` in design-system tokens, because the design system ships no drawer and one module is below its bar for graduating a component — move that block to `design-system/components.css` when a second module needs a sheet.

### 02 — Pactum: CDM
Added the charge master under `pactum`: `#/pactum/cdm` (items and bundles in one list), `/import` (4-step importer, items only), `/bundles` (expandable component trees, flags, clear flag) and `/bundles/new|<id>` (3-step builder with a cycle-blocking picker). New entity `cdm` — one collection, `kind: 'item' | 'bundle'` — with `findActive`, `componentSum`, `parentsOf`, `wouldCreateCycle`/`cyclePath`, `flagParents`, `clearFlag` and `expireBundles()` on load.
Bundle routes sit under the CDM screen, so a nav entry may now carry a deeper path (`cdm/bundles`) and `app/layout.js` highlights the deepest nav path the route sits under.
UI calls: `componentSum` counts a nested bundle at its own price, not at the sum inside it; the builder's running totals sit under the step body (the design system has no sticky footer); the component tree indents with `--space-4` since the system ships no tree.
View history is `cdm-history.js`, the payer trail copied for the `cdm` entity (items and bundles alike) in the shared drawer; the payer version is untouched.

### Pactum: page header removed (no amendment — asked for directly)
The topbar breadcrumb already names the screen, so every Pactum screen dropped its `ctx.actions` block and the shell's header bar collapses when a screen leaves it empty (`app/app.css`) — the `<h1>` stays in the document, clipped, so the page keeps its heading. Other modules that still fill `ctx.actions` (`_template`) keep the bar; nothing in `app/layout.js` or the breadcrumb changed.
Each screen's actions moved to the panel header on the right: Bulk import beside Add new payer on Payer Master, Bundles and Bulk import beside Add item on the CDM, Back to CDM beside New bundle on Bundles.
UI call: the importers and the bundle builder have no panel to name, so their Back link sits at the end of the step panel's header after the hint rather than in a row of its own.

### 03 — Pactum: Contracts (part A)
Added the contract lifecycle under `pactum`: `#/pactum/contracts` (every active contract, metric rail, payer-type and expiring-within filters, amber "expires in N days" chip ≤90 days), `#/pactum/payers/:payerId/contracts` (one payer's agreements, versions of a lineage grouped under a "v1, v2…" expander, add/edit/terminate/history row actions) and `#/pactum/contracts/:id` (the contract page: banner, version switcher, General + History, and Methodologies / Overage / Coverage / Pre-Auth / Rules as counted placeholders). New entity `contracts` — versioned by `lineageId` + `version` — with `byPayer`, `activeOnly`, `versionsOf`, `nextVersion`, `isContractNoUnique`, `planOverlap`, `daysLeft`, `plansOf` and `expireContracts()` on load. Payer Master gained a Contracts row action and the payer editor a "Contracts (n)" link on General; `payer-list.js` hands the mount to `payer-contracts.js` on the deeper path, the way the CDM hands off to bundles.
Data model calls: versions of one agreement share the contract number, so `isContractNoUnique` and `planOverlap` skip the row's own lineage; audit entries are keyed on `lineageId` with the version in `details` ("v2 · …"), so one query returns the whole history and the History tab splits a corrective edit's `;`-separated diff into a line each. `create` also stamps `createdBy`/`createdAt` — the versions timeline names who drafted each version.
UI calls: the lists have filters but no paging or sortable headers (the amendment names neither; the global list sorts soonest-expiry first, which is the reason to open it); in-modal section headings are `.toolbar` + `.t-title-sm`, since `.panel-header` belongs to a panel; plans are checkbox rows in `.rule-child-row` because the design system ships no checkbox class of its own; and the design system gained `.field--area`, the multi-line variant of `.field` (the plain row is fixed at 32 px, so a taller textarea spilled out of its box) — the termination reason wears it.

### 04 — Pactum: Rate Methodologies & Overage (part B)
Filled the contract page's Methodologies and Overage tabs and added the fee schedule report at `#/pactum/contracts/:id/fee-report` (`contracts-global.js` hands the mount over on the deeper path, the way it already does for the contract page). Methodologies are nested on the contract: `scopeLevel`/`scopeValue`, `method`, `params`, effective dates, with `methodologyOverlap`, `hasDefaultMethodology`, `resolveMethodology`, `resolvedPrice`, `bundlePricedRows`, `bundleRowsNeedingOverage` and `activationBlockers` in `data/repositories/contracts.js`; `askActivate` opens a blocking dialog listing those blockers instead of the date prompt. Fixed Amount rows carry a fee schedule edited in the modal (`fee-schedule.js` — table and file format) with a nested 4-step importer (`fee-schedule-import.js`) whose rows are merged into the draft, so Cancel discards them. CDM bundle components gained `limitType`/`limitQty`/`limitAmount`, shown in the builder, the review step and `component-tree.js`.
Data model calls: a fee schedule prices the lines it names and no others, so a Fixed Amount row that does not name the charge is skipped and resolution carries on to the next-widest scope — the standard price is reached only when nothing covers the line at all; `resolveMethodology` takes admission type as an optional fourth argument, since it is a claim-time fact no other screen knows, and the fee report's Admission type filter (default none) is what supplies it; the report lists every active charge line, bundles included, and reads rates on a date the contract actually covers (clamped to its term), so a future draft reports the rates it will bill under; and removing a methodology removes the overage policy hanging off it.
UI calls: the two configured tabs render into a node contract-view.js creates for them (the shell's freshBody rule one level down) and the page's own `[data-act]` handler is scoped to `#cv-actions`, since a tab now owns buttons inside the panel; the methodology modal validates into one error box under the body rather than per field, because its fields swap with the chosen method; tolerance pairs and the fee schedule's item picker sit in `.toolbar` rows, the only design-system primitive that caps a field's width (a bare select sizes to its longest option and overflowed the dialog); a quantity limit reads through the component's UoM ("2 nights", "1 test") and says "included" for Each and Package.
Print: `app/app.css` gained an `@media print` block — the shell's chrome and anything marked `data-print="hide"` leave the printed page, which is what Export PDF (`window.print()`) hands the printer. Export Excel builds the CSV client-side and downloads it as `.csv`.

### 05 — Pactum: Coverage (part C)
Filled the contract page's Coverage tab: a plan selector (each plan shows ✓ or "Default missing"), the split table for the selected plan with add/edit/remove, Copy from plan… (Replace all rows or Merge — keep existing) and a "Try it" strip that runs an allowed amount through the resolved row and shows the patient and payer share live. Coverage is nested per plan on the contract — `coverage: [{ planId, rows: [{ scopeLevel, scopeValue, covered, shareType, shareValue, deductible, ceiling }] }]` — with `coverageFor`, `coverageOverlap`, `hasDefaultCoverage`, `plansMissingDefaultCoverage`, `resolveCoverage`, `patientShare`/`payerShare`, `shareSummary`, `copyCoverage`/`coverageCopyCount` and the writes `saveCoverageRow`/`removeCoverageRow` in `data/repositories/contracts.js`. The activation gate gained "Plan <name> has no Default coverage row" per plan, which `contract-actions.js` renders unchanged — it lists whatever `activationBlockers` returns. Seed: NSSF (CTR-0001) carries the worked split on both plans, AXA (CTR-0005) a $10 fixed co-pay on one of its two.
Data model calls: coverage carries no effective dates, so a plan holds one row per scope and `coverageOverlap` is a same-scope check rather than a date test; the ceiling caps the patient share after the share type is applied, and the share never exceeds the allowed amount; a charge no row covers leaves the whole allowed amount with the payer (`patientShare(x, null)` is 0), which the preview says in words; `copyCoverage` takes a contract or its id and re-ids every copied row, so ids stay unique within the contract.
UI calls: the plan selector is `.segmented` with `aria-pressed`, the design system's primitive for picking one of a few; the selected plan is the tab's own state and a save redraws the tab rather than the page, so the plan under edit stays selected — the tab never calls the `refresh` contract-view.js hands it; Covered is a Yes/No select, since the design system ships no checkbox and the methodology modal already asks its questions with selects; the table reads "Full amount" for an uncovered scope and "—" for a None share, and the preview line says "no patient share" instead of that bare dash.

### 06 — Pactum: Pre-Authorization (part D)
Filled the contract page's Pre-Auth tab: an info banner naming the precedence, the matrix with add/edit/remove, a subtle "Exemption" tag on a "No" row a broader row would have required, and a "Check an item" strip (item picker + amount) whose chip answers with the winning rule. The `preAuth{}` placeholder is now an array nested on the contract — `preAuth: [{ id, scopeLevel (Service Group|Category|Item), scopeValue, required, threshold|null, updatedAt }]` — with `preAuthRows`, `preAuthLabel`/`preAuthScopeName`, `thresholdLabel`, `preAuthOverlap`, `isPreAuthExemption`, `resolvePreAuth` and the writes `savePreAuth`/`removePreAuth` in `data/repositories/contracts.js`. Nothing here gates activation, so `activationBlockers` is untouched. Seed: NSSF (CTR-0001) carries the worked ladder — Inpatient always, Radiology above $300, MRI Brain always, Chest X-ray exempt inside it, Pharmacy above $1,000 — and AXA (CTR-0005) requires it on Surgery.
Data model calls: `resolvePreAuth(contract, item, amount)` walks Item → Category → Service Group and the narrowest row wins outright, so an item row with `required: false` exempts a charge a wider row would have held; a threshold only ever narrows a requiring row, so a "No" row clears and disables it; with no amount the check is `0 > threshold`, which reads as not required. The rule engine's conditional pre-auth merges into `resolvePreAuth` in part F — the hook is a comment on its doc block.
UI calls: the required answer is a Yes/No `.segmented` toggle (the design system ships no switch, and Coverage already picks one of a few this way) while the rest of the modal stays selects; the exemption tag is a plain `.badge` beside the tinted Yes/No one, the subtlest pairing the system offers; the check strip mirrors Coverage's "Try it" panel and defaults the amount to the item's standard price, so it answers before anything is typed.

### 07 — Pactum: Rule Engine (part E)
Added the rule engine under a feature folder of its own, `modules/pactum/features/rules/`: the Rules tab on the contract page (evaluation toggle First match / All match saved and audited on change, amber conflict banner that expands to name each pair, table in priority order with Edit · Deactivate/Activate · Duplicate · View history) and the wizard at `#/pactum/contracts/:id/rules/new` and `/rules/:ruleId` — a 4-step page (Details, Conditions, Action, Review & test) copied from the bundle builder. A rule is `{ id, name, description, priority, status, conditions, action }` nested on the contract, with `contract.ruleEvaluation`. `rule-attributes.js` is the static catalog (24 attributes over Patient / Item / Encounter / Financial, each with a type that filters the operator list and swaps the value input); `rule-evaluator.js` is the engine — `evalCondition`, `evalGroup`, `evaluate`, `summarize`, `findConflicts`; `condition-builder.js` is the AND/OR tree; `rule-simulator.js` is step 4's panel, which auto-generates an input per referenced attribute, fills a firing case from Load example and runs the whole contract in priority order. `data/repositories/contracts.js` gained the storage side (`rules`, `activeRules`/`activeRuleCount`, `nextRulePriority`, `getRule`, `saveRule`, `setRuleStatus`, `duplicateRule`, `setRuleEvaluation`, `ruleHistory`) and thin wrappers over the engine; "No. of rules" on the global list is now the Active count.
Architecture call: the engine is pure and ships with the feature that owns the rule shape, so `data/repositories/contracts.js` imports `rule-evaluator.js` — the one arrow from `data/` into a module. It stays acyclic because `rule-attributes.js` imports no repository: enum options that live in one (`plans`, `categories`, `serviceGroups`, `admissionTypes`, and the action's `shareTypes`/`overageActions`) are named by source in the catalog and resolved by the screen drawing the picker, where the contract is known.
Data model calls: a context is flat and keyed by attribute (`{ 'item.category': 'Consumables' }`), and an attribute the claim does not carry never matches, so a missing value fails a condition rather than passing it; `patient.plan` stores the plan id and `summarize` takes a resolver that reads it back as the plan name; conflict detection is the amendment's cheap heuristic — equal literals or intersecting numeric ranges on a shared attribute, plus contradictory actions — so it flags pairs worth a look rather than proving anything; rule audit entries carry the rule id (`v2 · RL-005 · name — change`), which is how `ruleHistory` reads one rule's trail back out of the contract's.
UI calls: `#/pactum/contracts/:id/rules` on its own opens the contract page on the Rules tab (any tab id in the path does now), so the wizard's Save and its Back link return where the user came from; the tree indents a nested group with `--space-4` and a left rule as its bracket, the inline-token approach the CDM component tree already uses; `in`/`not_in` over an enum is chips plus a picker, and over free text one comma-separated field; Co-pay offers only the two share types that take a single value (`Co-pay %`, `Fixed Co-pay`), since a rule action carries no deductible; the wizard's step markup is `rule-wizard-steps.js`, the bundle-builder split, to keep each file near the line cap.
Seed: NSSF (CTR-0001) carries six rules — a nested group, an encounter rule, a patient rule, an overage override and the deliberate Pharmacy pair the conflict banner catches — and AXA (CTR-0005) one ceiling rule. The amendment's header says six rules across both contracts while its list names seven, so the list won; "Gold" and "Silver" are the NSSF first- and second-class plans, the closest thing the payer seed has to tiers.

### 08 — Pactum: Billing Evaluation (part F)
Added the evaluation engine and the screen that drives it. `modules/pactum/features/billing-eval/` holds `billing-engine.js` (`evaluateCharge` → a five-step trace: methodology, overage, coverage, pre-auth, rules; `evaluateEncounter` → the contract version plus totals; `invoiceRows`, `totalsOf`), `overage-engine.js` (`limitRows`, `evaluateOverage` — per component included / consumed / overage / tolerance / action / source, and overage lines flagged `isOverage`), `breakdown-panel.js` (the trace, the totals rail and the invoice table as markup a claims screen can reuse), `scenarios.js` (3 canned encounters) and the Billing Simulator at `#/pactum/billing-simulator` — `/<contract id>` opens it pre-filled, which is the contract page's new Simulate billing link. Both engines are pure (no DOM, no writes) and the simulator never touches the store. `data/repositories/contracts.js` gained `contractForService(payerId, planId, dateOfService)` and two activation blockers — an empty charge master, and a case rate whose bundle has components without limits — and the four configuration tabs now show "Populate the CDM before configuring contracts" (`features/contracts/cdm-gate.js`) instead of an empty form.
Data model calls: rules run last and every override is kept as "was X → now Y (rule name)" — Not Billable zeroes the line, % Discount / Fixed Rate / Ceiling recompute the allowed amount and re-run coverage, Co-pay replaces the split with a synthetic row, Route to Payer/Patient forces 100/0, and Override Overage Action re-runs the overage engine with that action; a line's status is Not billable, then Held for approval, then Priced, in that precedence. A tolerance is read in its own unit (a % of what the bundle includes, an Amount against what the overrun is worth), so a component inside it is "Absorbed (tolerance)" whatever the policy says and a rule override never reaches it. `contractForService` takes the version whose term covers the date and that was live then — active today, or a version since closed, never a draft — so a 2025 date of service prices through version 1. `financial.overageAmount` is the line's whole overage, which is what fires the seeded RL-004 on the package scenario: the trace shows the per-component answers at step 2 and the rule replacing them at step 5.
UI calls: `.split` puts the inputs in the rail and the result beside them, so the consumption sub-table folds its Included column into the component name ("includes 2 nights") to fit; each breakdown is a `<details>` whose `<summary>` is the panel header — the collapsible the design system needs no CSS for; a nested bundle keeps the parent's limit and lists its own items underneath as read-only detail, because consumption is entered where the limits are written; and the simulator imports `optionsHtml`/`csvLine`/`download` from `features/contracts/fee-schedule.js` rather than keeping a second copy of them.
Seed: the NSSF lineage gained version 1 (CTR-0010, Expired, the 2025 term, the same configuration five points cheaper) so a scenario can show version selection, and CTR-0001 is now version 2; PKG-APP-001's overage override moved from Bill Patient on PHA-0002 to Split per Coverage on CNS-0002, so one component absorbs by tolerance, one bills the payer and one splits; and "CT chest with contrast" is hand-written at $450 (RAD-0003) so the radiology threshold has something to clear.

### Pactum: engines moved to data/engines/ (no amendment — asked for directly)
`rule-evaluator.js` and `rule-attributes.js` (amendment 07) and `billing-engine.js` and `overage-engine.js` (amendment 08) moved out of their feature folders into `data/engines/`, and every import was updated: `data/repositories/contracts.js` now reads `../engines/rule-evaluator.js` instead of reaching up into `modules/`, and the rules and billing-eval features reach down for all four. Nothing else moved — `condition-builder.js`, `rule-simulator.js`, the wizard, `breakdown-panel.js`, `scenarios.js` and `simulator.js` are screen-side and stayed put.
The layering is now a hard rule above: modules import from `data/`, `data/` never imports from `modules/`. Inside `data/`, the two billing engines read repositories and are read by no repository, while `rule-evaluator.js` stays a leaf (`shared/` and its own catalog only) — which is what lets `contracts.js` wrap it without a cycle.

### 09 — Pactum: integration audit
A consistency pass, no new features. Fixed: lists went stale because nothing subscribed to the store, so `app/main.js` now hands every screen `ctx.onData(fn)` — a store subscription retired when the route changes, the way the body node's listeners are — and the payer, CDM, bundles, contracts and payer-contracts lists redraw on it; `expireContracts()` was defined and never called, so it now runs on load beside `cdm.expireBundles()`; the simulator warned "No active contract for this plan" before anything was chosen, silently swapped a prefilled inactive plan for another contract's plan, and lost its contract when the date field was cleared; and the simulator's layout put a whole encounter form in a 360px rail with the scenario picker reading as a selected card.
The simulator is rebuilt on `.split--40-60` (glue in `app/app.css`, with the metric rail filling that column instead of overflowing its four fixed 260px tracks): the scenario dropdown and Load sit in the panel header beside Run and Clear, field rows match the contract forms, a charge line is a sunken card with the picker and quantity on one row and the consumption sub-table only under a bundle, and the empty result is a tall centred state view. `simulator-inputs.js` holds the rail's markup. `prefill` now clamps the date of service to one the opened version actually billed on — inside its term, on or after it went live, on or before it closed or was terminated — so Simulate billing lands on that contract from a draft, an expired version or a terminated one; a plan the contract names stays in the picker labelled inactive; and when nothing resolves the screen names the contracts on that plan with their status and term instead of one flat warning.
Consistency: `shared/format.js` gained `todayIso`/`iso`/`compareDates`/`withinDates` and the contract and CDM date comparisons run through them (a blank date now reads as today rather than as before everything); the Pre-Auth "Check an item" strip defaults to the allowed amount, so the fee report, Coverage "Try it", Pre-Auth, rule simulation and the billing simulator all answer for the same money on the same charge; `SERVICE_GROUPS` is derived from the category map, so a picker can no longer scope a rate to a group no charge lands in; the sidebar's CDM badge counts what the CDM list holds (items and bundles); "Contracts (n)" in the payer editor counts agreements the way the screen it opens does; and the version switcher keeps the tab you were reading.

### 10 — Pactum: Home Screen
Added the module's landing dashboard under `modules/pactum/features/home/`: `#/pactum` (and `#/pactum/home`) is now the first nav entry, with six KPI cards (`home-kpis.js`), three attention panels (`home-attention.js`), the last five audit entries (`home-activity.js`) and five quick actions in the panel header (`home-quick-actions.js`, which reuses the payer, item and contract modals and the bundle builder rather than writing anything itself). "View all" opens the whole trail at `#/pactum/activity` (`activity-list.js` — search, entity-type filter, paging). Everything is computed live and redraws on `ctx.onData`; the only new data-layer code is two read helpers, `contracts.expiringWithin(days)` and `cdm.flaggedBundles()`, plus `flaggedAt`/`flaggedSince` on the flag helpers so a flagged bundle can say how long it has been waiting.
Shell: `app/router.js` splits a `?key=value` query off the hash into `route.query` (the path is unchanged, so nav highlighting and the `ctx.params` handoffs never see it) and `app/main.js` passes it to every screen as `ctx.query` and now redirects a bare module path to its first nav screen — the sidebar highlights the deepest nav path a route sits under, and `#/pactum` sat under none of them. `shared/format.js` gained `relativeTime`.
Lists read those params on mount and move the matching control, so a card lands on a list that says what it is filtered by: `payers?status=&type=`, `contracts?status=&expiring=&payerType=`, `cdm?kind=&status=&category=`, `cdm/bundles?flagged=&type=`, `activity?entity=`. Three filters were added to carry them — a Status select on the global contracts list (defaulting to Active, which is what the screen showed before), a line-type select on the CDM list, and a review-flag select on the bundles list — and the CDM list gained the payer list's deep link, so `#/pactum/cdm/CDM-0001` opens that charge line (the item modal, or the builder for a bundle) and Recent activity can link to it.
Data model calls: a card's headline and the row count of the list it opens are the same figure, computed from the same helper, so Payers opens the whole payer list and CDM items opens `cdm?kind=item` rather than the filtered targets the amendment sketched — the sub-line is what names the slice (its number is the `?status=Active` / `?flagged=1` row count). Contract audit entries are keyed on the lineage, so an activity link resolves through `currentOf` to the live version, and an entry naming a rule that still exists opens the rule wizard rather than the contract page.
UI calls: the design system's metric card is one line high and says so — "anything a KPI footer line carried belongs in the card's title attribute" — so the six sub-lines ride in `title` rather than a second row, and the amber left bar is what makes Expiring soon and Bundles visible at a glance; the attention panels are `.worklists` + `.panel`, the module-dashboard vocabulary the system already ships, each holding a five-row `.tbl` or a positive `.state-view`; a draft's blockers are whole sentences, so each chip is a short name carrying the sentence in its tooltip, capped at two plus "+n"; Recent activity is the `.journey` strip the history drawers use, with the record label and the entry's own details on one line and the duplicate dropped when they say the same thing; and the Bulk import chooser is the system's `.menu` listbox rather than two cards, since the system ships no card control.

### Pactum: KPI footer line and rail track counts (no amendment — asked for directly)
`design-system/components.css` gained two things the dashboard wanted and the system did not ship: `.metric-rail-card__sub`, the muted one-line footer under a card's label (the element switches the layout through `:has()`, so there is no modifier class to forget — the card becomes a two-row grid 62px high with the value spanning both rows), and `.metric-rail--2` / `--3`, which name a rail's track count and let its cards fill the tracks instead of holding the fixed 260px width. Four tracks stay the default and need no modifier.
The home KPI cards now show all six sub-lines and keep them in `title` as well, and the rail is `--3` so six cards fill the panel in two rows of three. The `.split--40-60 .metric-rail` block amendment 09 put in `app/app.css` is gone: the billing simulator's totals rail says `metric-rail--3` itself, which is the same three tracks with the same 1400/700px reflow, now read off the component rather than off an ancestor.
Superseded in part by “Pactum: metric rails never wrap” below, written the same day: a rail counts its own cards now, so four tracks are no longer the default, Home carries no modifier, and the reflow is 900/700px. What still stands from this entry is `.metric-rail-card__sub` and the billing simulator's `--3`.

### 11 — Pactum: Performance
Added read-only analytics under `pactum`: `#/pactum/performance` is the payer table (worst calibrated score first, colour-coded denial bars, score chips whose tooltip names the three inputs and their weights, monthly-denial-rate sparklines, sortable headers, and an Underpayments flagged card that filters the table to payers with flags) and `#/pactum/performance/contracts/<id>` is one contract version — the payer screen hands the mount over on the deeper path the way the contracts list does. The contract page carries a payer-grouped version picker, five cards, monthly charged/paid columns beside ranked denial reasons, a service-line table whose footer equals the cards, and the flagged underpayments with **Hand off to Defensio** (`perf-defensio-handoff.js`). Two entities Pactum holds for now: `claims` (a future claims module takes it) and `handoffs` (Defensio takes it) — the note is on the manifest, the repositories and seeds stay in `data/`.
`data/engines/performance-engine.js` is the only place a performance definition lives — `CONFIG` (weights, the 25% / 90-day / 10% ceilings, the 30-day target, the $25 flag floor, the denial bands), `rollup`, `isFlagged`, `calibratedScore`/`scoreInputs`, `payerRollup`/`allPayers`/`contractRollup`/`serviceLines`/`monthlyBilled`/`denialReasons`/`sparkline`/`underpayments`/`globalRollup` and `periodRange` — so the payer row, the contract cards, the service-line footer and the underpayment list reconcile because the same function ran over a subset of the same claims. `selfCheck()` asserts exactly that on load (contract rollups sum to the payer, service lines sum to the contract, on billed, allowed, variance and denied count) and logs one `[performance] self-check pass` line. It is over the file cap on purpose — splitting it would be splitting the definitions, which is now the hard rule above.
Seeding call: the claims dataset has to read the contracts and the charge master to exist, so `data/store.js` does not import it — `data/repositories/claims.js` generates on first read of an empty table, and `handoffs.js` does the same off the generated claims. Nothing was added to the store, `resetToSeed()` empties both tables and the next read rebuilds them identically (fixed-seed mulberry32), and a hand-off written in the browser persists for the session like any other row. `data/seed/claims.js` therefore imports repositories, which is new for a seed and is what keeps the cycle out of the store.
Data model calls: the knobs are spent, not sampled — exactly the denial share a payer's knob names is denied and the trend picks which claims carry it, so five payers separate on a few hundred rows instead of blurring into each other's noise (NSSF 19.6% denied over 69 days scores 23; AXA 2.3% over 19 days scores 89). A claim still inside its own turnaround is Pending and counts in no rate but its own, and both `effectiveRate` and its contracted target read adjudicated claims only, since a claim in flight is not evidence about the rate a payer pays; `variancePct` divides by what the *paid* claims were expected to allow, the only rows variance is measurable on. The score's penalty is scaled by 100 (the amendment's weights sum to 1, so an unscaled penalty could never move a score more than a point). A payer shaves the lines worth shaving, so underpayments land on claims expected above $100 — below that the shortfall never clears the $25 flag floor, and the table would have had four rows. `denialRate` is over adjudicated claims, `lastQuarter` is the last full calendar quarter, and a claim stores the contract version live on its date of service, so the Bankers lineage reports v1 and v2 separately.
Seed: `data/seed/contracts.js` gained the five straightforward 2026 agreements every remaining payer needed to appear at all — the Army fund, the civil servants' cooperative, Allianz SNA, Arope and Cigna (CTR-0011…CTR-0015), written as a table rather than five blocks: a default percentage, one narrower rate row and the same 20%-capped-at-$400 split on each linked plan. MOPH, both Bankers versions and Bupa gained the rates they were signed without, so no contract reports a 100% target any more and Bankers v2 pays three points less than v1. Performance now tracks ten payers over ~600 claims, scoring 16 (NSSF: a fifth denied, 67 days, 17% variance) to 91 (Bupa). The variance ceiling is 10%, not the amendment's 20% — at 20% the axis moved a score by a point or two and the weight it carries was fiction.
UI calls: `perf-charts.js` draws the four shapes in inline SVG over `--viz-*` tokens (colour-coded bar, delta arrow, sparkline, month column pair) — no chart library, and no CSS in a module. Both rails carry five cards and name no track count, which is now the rail's own job; the two KPI footer lines that overran one card are trimmed with the full sentence kept in `title`, which is what the system says to do. The contract picker sits in the panel body's `.toolbar` rather than the panel header, since that is the one primitive that caps a field's width and a bare select sized itself to "NSSF hospitalization 2026 · v2"; the whole header row is drawn at once so its parts are flex children and the toolbar's gap applies. The payer screen's filter banner needed a plain wrapper to carry `hidden` — `.toolbar` sets `display:flex`, which outranks the attribute. `app/app.css` gained `a.badge { text-decoration: none }` beside the other link resets, for the hand-off chip: it reads `Handed off · Defensio ↗` and points at `#/defensio`, which the shell redirects to Pactum home until Defensio exists.

### Pactum: metric rails never wrap (no amendment — asked for directly)
`.metric-rail` dropped its fixed 260px tracks for one row of equal columns — `grid-auto-flow: column` over `grid-auto-columns: minmax(0, 1fr)`, with `min-width: 0` and `overflow: hidden` on the card so it can shrink below its own text. The rail counts its own cards, so `--3` came off Home, Payer Performance and Contract Performance; every rail on a page is one row at 1280px and 1440px (Home six cards at 156/183px, the two performance rails five at ~190/225px, Payers, Contracts, CDM and Bundles four at 247/287px), and the reflow is 900px → two columns, 700px → one. This supersedes the track-count design in “KPI footer line and rail track counts” above.
`.metric-rail--2` / `--3` survive with a narrower job: a rail inside a column rather than across the page. The billing simulator's totals sit in the 60% half of a split, where six fluid cards are 87px each and the labels disappear entirely, so that one rail keeps `--3` and wraps inside its column. Nothing was left to remove from `app/app.css` — the `.split--40-60 .metric-rail` block went with the entry above, and `a.metric-rail-card` there is the link reset, not sizing.
UI call: a wide money value and its label cannot both fit a five-card row at 1280px, so Contract Performance shows “Gross billed” as “G.” — the label truncates and the sub-line ellipsises, the value is never cut, and the full text stays in `title`, which is what the system already says to do.

### Pactum: floating action bar (no amendment — asked for directly)
`design-system/components.css` gained `.fab`, the round button docked bottom-right that opens the system's `.menu` listbox, and `shared/fab.js` builds one: `mountFab(mount, { actions, label, icon })`, where an action is `{ act, label, icon }` for a row the screen's own `[data-act]` handler picks up or `{ href, … }` for one that navigates. It is open to every module — `preview/components-fab.html` is its card in the gallery.
It is draggable because a fixed control covers something different on every screen, so the reader moves it rather than the design guessing: the button is its own drag handle, a press that does not travel 4px is a click, the position is clamped into the window and kept for the session, and the menu opens away from whichever edge the button was dragged to (`--down`, `--start`), so it never opens off-screen.
Home is the first screen on it: the panel header is just "Pactum" again, Bulk import is gone from the dashboard entirely (both importers keep their own buttons on the CDM and Payer Master panel headers), and the four create-actions are the bar's rows.
Two things the component had to settle. Opening is bound to `click`, not `pointerup`, or a keyboard Enter would never open it — the click a drag ends in is suppressed by a flag instead. And `.fab.fab { margin: 0 }` is written with the class doubled because the bar mounts inside the screen's node, where `app/app.css`'s page-rhythm rule matches at the same specificity and loads later; a margin on a fixed element offsets it from the position it was dropped at.

### Pactum: KPI cards select the rows they count (no amendment — asked for directly)
Every metric-rail card on every screen is a control now, and one file builds them all — `shared/metric-card.js` (`metricCardHtml`, `metricRailHtml`, `metricKey`, `kpiFilter`) — instead of the eight copies of a local `metric()` helper the screens carried. A card either opens the screen holding the rows it counts or acts on the page it sits on. `design-system/components.css` gained the pressed state a filtering card needs — `button.metric-rail-card[aria-pressed="true"]` tints the card and paints the three plain edges accent, never the severity bar, the same line hover holds — and the gallery card documents it.
On a list screen the card sets the filters, so the number on the card and the row count under it are one figure; a second click on the pressed card clears them, and a control moved by hand redraws the rail so no card claims a filter that left the screen. The slices sit beside each list in `payer-kpis.js`, `cdm-kpis.js`, `bundle-kpis.js` and `contracts-kpis.js` — the `home-kpis.js` split, which also keeps the list files under the line cap — as a `KPI` map of card key → the filter state it selects, read by `kpiFilter`. Two slices have no select of their own, so the card is its own control and Clear filters clears it with the rest: Payer Master's "With active plans" (a `plans` filter and a `withPlans` count added to the payers repository) and Bundles' "Nested".
Two metrics were rewritten to be a slice of their own list rather than a number beside it: Payer Master's "Active plans" is now "With active plans" — the payers carrying one, with the plan count in its tooltip — and the contracts rail gained "All contracts" as the total that clears the filters, so it carries five. "Payers covered" counts payers rather than contracts, which is why it is the one card that navigates: it opens Payer Master.
Analytics and the transient rails answer the same way in their own terms. Payer Performance's cards filter (Payers tracked shows every payer, Variance recovered lists the payers a recovered hand-off came from, Underpayments flagged as before — `state.onlyFlagged` is now `state.filter`, and the banner names whichever set is on screen) or sort by the column they summarise (denial rate, days to pay, worst first, reversing on a second click like the header). Contract Performance has no list of claims, so its five cards jump to the panel that breaks the number down — the panels carry an id and `tabindex="-1"`, and focus is moved *before* `scrollIntoView`, because moving it after cancels a smooth scroll already under way. The billing simulator's totals select the invoice lines behind them (`INVOICE_FILTERS` in `breakdown-panel.js`), and a card whose total is zero draws as a plain number rather than a control that would empty the table. The three importers' preview rails filter the preview to the valid or the error rows, each row still numbered by the line it came from in the file.
`simulator.js` was already 338 lines and this would have made it 365, so the result half moved to `simulator-result.js` — the `simulator-inputs.js` split — which is also what lets the totals rail redraw the invoice without collapsing a breakdown the reader has opened. `modules/_template` carries the pattern, so a new module copies a rail whose cards already work.

### 12 — Frontis: Module Bootstrap + Patient Master
Added module `frontis` (Patient Access & Eligibility), owner of `patients` and the new `duplicates` entity, with Patient Master under one nav entry and six routes: `#/frontis/patients` (list with search over every identifier, Status/Gender/Nationality filters, a merged-records select, sorting, paging and a five-card rail), `/new` and `/<mrn>/edit` (full-page register form, three tabs, live age readout, photo, VIP toggle), `/<mrn>` (record page: banner, role-aware actions, summary, Documents and History tabs), `/duplicates` (worklist with Review and Dismiss), `/merge` (three steps: choose, field by field, re-link and confirm) and `/import` (the CDM importer's four steps with the registration columns). The scaffolding `patients` seed and repository were replaced: 60 patients (10 hand-written — a VIP, a deceased, a blocked, a merged pair and two near-duplicate pairs — the rest combined from short lists with a fixed-seed PRNG), and `search`/`nextMrn`/`isIdentifierUnique`/`findDuplicates`/`resolve`/`merge`/`setStatus`/`setVip`/`canOpenEncounter`/`view` in `data/repositories/patients.js`. `shared/roles.js` gained `canBlockPatients` and `canViewVip` per role (Maya Zgheib has neither, which is the demo's masked reader), and `shared/format.js` gained `ageFrom`, `maskName` and `maskId`.
Data model calls: the MRN is the entity's id — there is no second key — and `nextMrn()` reads the sequence off the register itself, so a reset and a reload produce the same numbers; masking is a read-time rule (`view(patient, role)`) that every screen renders through, while `search` still matches the real values, since a register that cannot be searched cannot be trusted — a restricted record is found and then read masked; a merged record keeps its MRN and points at the survivor, so an old wristband still resolves, and `resolve()` follows the chain; `merge()` moves documents itself and calls `relinkHooks`, the array a future encounters or policies module pushes into, which is also what the merge screen's preview reads (Encounters, Policies and Accounts show "none yet"); and the duplicates worklist is never deleted from — Dismissed and Merged are answers.
Two kinds of closed, which is the one place the record page departs from the amendment's "read-only": a merged record is read-only outright, while a deceased one keeps Edit and its Documents tab — the certificate and the corrections that matter most arrive after the death — and loses only what assumes a living patient (Block, VIP, New encounter). Both are out of the encounter door through `canOpenEncounter`.
`data/engines/name-match.js` is new and is the only place two names are compared: `normalizeName`, `normalizePhone`, `levenshtein`, `similarity` (the kinder of a token-set and an edit-distance reading) and the 0.8 threshold. It is a leaf — it imports nothing — which is what lets `data/repositories/patients.js` read it.
UI calls: the design system ships no checkbox, so "Include merged" is a two-option select and the merge screen's Keep column is bare radios in the table cell; the register form is a full page with `.sections` tabs rather than a modal, since registration is the longest form in the platform; the photo preview is the system's `.avatar avatar--lg`, written into its span rather than switched off with `[hidden]`, because `.avatar` and `.btn` set their own display; the record page is a `.split` with the summary in the bounded pane, so who the patient is stays on screen while the tabs are read; a masked record's actions are all disabled with the reason in the tooltip, and its banner is `.perm-banner`; and registering does not check identifier uniqueness in the form, because that clash is the duplicate check's first answer and its dialog names the record and offers to open it — an edit, which has no such dialog, still shows the field error.
Documents are written as they are uploaded rather than on save (the payer modal is transactional; a record page has no Cancel), and `patient-import-csv.js` copies the CDM importer's CSV helpers rather than importing them, since a module never reaches into another module's files. Patient entries appear in Pactum's Recent activity through the shared trail, where its `describe()` fallback labels them without a link.
Pactum's Recent activity and its full trail resolve a `patients` entry to the Frontis record page — the trail is shared, and a row belongs to whoever owns the entity, so `home-activity.js` reads the patient through `view()` (a restricted record reads masked there too) and the entity filter gained Patients. A `policy` entry joins the same branch when Frontis owns policies.
Seed: the worklist opens on three pairs and one already merged — two Name+DOB near-duplicates, and a Phone pair between two unrelated people, which needed the register's one hand-set generated value (`SHARED_PHONE`): Ahmad Al-Sayed was registered on the line of the household he works for.
`_template` was pointed at the new API: the example list reads `search`/`counts`/`view`, its columns are MRN, nationality, city and phone, and the detail dialog's action is Block / Unblock gated on `canBlockPatients` — the discharge action went with the old placeholder entity.

### 13 — Frontis: Insurance & Plan Capture
Added the `policies` entity (Frontis owns it; the note is on the manifest) and the Insurance tab on the patient record at `#/frontis/patients/<mrn>/insurance`: the coverage chip ("Primary: <payer> · <plan>", or Self-Pay when the chain is empty), Add policy and Reorder in the tab's toolbar, the chain in priority order, everything out of it below, and the fixed Self-Pay fallback row last. `modules/frontis/features/insurance/` holds `tab-insurance.html`/`.js` (the frame, its rows, and the tab's `pol-*` actions), `policy-form.js` (the add/edit modal), `policy-actions.js` (suspend / reactivate / cancel, the reorder sheet, and the shared one-field reason dialog) and `policy-history.js` (the patient trail bound to the `policy` entity). `data/repositories/policies.js` carries `byPatient`, `chain`, `canAddToChain`, `isMemberIdUnique`, `overlaps`, `planHasActiveContract`, `orderLabel`, `create`/`update`, `setStatus`, `reorder` and `expirePolicies()` on load, and pushes the re-link hook into `patients.relinkHooks` — loading `modules/frontis/module.js` is what registers it, so the merge preview counts policies whether or not the tab was opened.
Data model calls: only Active policies carry a priority, so leaving the chain always nulls it and what is left is renumbered 1..n — compaction is a reorder in the trail because that is what it is, and it is logged on each policy that actually moved, which is how one policy's history shows a change made beside it. `chain()` is what eligibility and billing will consume and Self-Pay is never a row in it — the fallback is a fact about an empty chain, and a caller appends it. `overlaps` ignores cancelled and expired rows (they cannot conflict) and `planHasActiveContract` resolves the payer off the plan id and asks Pactum's `contractForService` on the policy's start date, so the warning is about the day the cover begins. Reactivating appends at the end of the chain rather than restoring the old seat, and is refused outright when the validity has lapsed ("Renew dates first") or the chain is full. A merged patient's policies move to the survivor, all of them out of the chain, and the survivor's chain is refilled in order up to three.
UI calls: Insurance is the first tab and the one the record opens on — who pays comes before what was filed — and a tab id in the path still deep-links to any of the three. The tab is read-only on Merged alone, the rule Documents already wears: a deceased record's paperwork keeps arriving after the death, policies included. The tab lives in the narrow half of the record's `.split`, so the amendment's nine columns are six: payer over plan, member ID over policy no., and status over the last-verified line; the relationship rides under the plan ("Child of Bilal Baalbaki") since a Self policy has nothing to say there. The design system ships no sortable list, so Reorder is up/down buttons over `.rule-child-row` with the order read back as one line under them — the same line the trail records. The two save warnings are raised together in one confirm step rather than one dialog each. The policy holder row is a `dt`/`dd` pair switched by `hidden` on both halves: a wrapper div around them is one cell of the `.dl` grid and throws every row below it out of its column. And a card is a plain `t-body-sm` line with its icon, not a `.badge` — the badge is 20px high and an icon inside one spills out of it.
Seed: 12 policies over 8 of the 60 patients — a three-policy chain with a dependant and a Bankers cover expiring inside the month (the amber chip), a chain with a suspended and an expired policy beside it, an expired-only record that falls back to Self-Pay, an ISF plan no Pactum contract names (the eligibility warning), and four single-policy records including the VIP and the blocked patient. Encounters do not exist yet, so three primaries are seeded `usedInEncounters: true` — otherwise the edit guard that asks for a reason before a payer or plan moves could not be reached in the demo.

### 14 — Frontis: Eligibility Check
Added the `eligibility` entity (Frontis owns it; the note is on the manifest) and the second Frontis nav entry: `#/frontis/eligibility` is the worklist (five-card rail, search over patient/MRN/reference, Result / Payer / date-range filters, paging, and View result · Re-check · View history row actions), `#/frontis/eligibility/new` the check screen and `#/frontis/eligibility/<ref>` the printable snapshot. `modules/frontis/features/eligibility/` holds `eligibility-worklist.html`/`.js` (which hands the mount over on both deeper paths, the way Patient Master already does), `eligibility-kpis.js`, `eligibility-check.html`/`.js` + `eligibility-check-inputs.js` (patient picker, chain radios, service rows) + `eligibility-check-result.js` (the attempt stack and the cascade prompt), `eligibility-panel.js` (the steps, conditions, coverage table and override block both screens draw), `eligibility-result.html`/`.js`, `eligibility-override.js` and `tab-eligibility.js`. The patient record gained an Eligibility tab after Insurance and a Check eligibility header action; `shared/roles.js` gained `canOverrideEligibility` (the RCM coder and the CMO have it, so the default demo role sees the disabled button and its reason); and `data/repositories/policies.js` gained `markVerified(id, at, ref)`, the only thing that stamps `lastVerifiedAt` — a passing check calls it and the policy trail names the check that did.
`data/engines/eligibility-engine.js` is the only place the ladder lives — `verify({ patient, policy | SELF_PAY, date, visitType, services })` over five steps in a fixed order, plus `RESULTS`, `VISIT_TYPES`, `resultTone` and `isPass` — so the desk check and the one Encounter Registration runs inline cannot disagree. It is pure: no DOM, no writes. `eligibility-check.js` also exports `runAutoCheck({ mrn, policyId, encounterId, visitType, services })`, which runs it, writes an Auto-Registration snapshot against the encounter and hands it back; `data/repositories/eligibility.js` carries `create`, `byPatient`, `cascadeOf`, `list`, `counts`, `latestValid(mrn, policyId, days = 7)`, `nextRef`, `attachEncounter` and `applyOverride`, and nothing else writes.
Data model calls: only the first three steps can refuse cover — a closed record, a lapsed policy or a missing contract is Not Eligible — and the last two raise conditions instead, so an excluded charge or a pre-auth requirement never turns the cover down; a step below a failed one carries `skipped: true` rather than a bare `pass: false`, because "never asked" is a different answer from "failed", and a refused check carries `coverageSummary: null` for the same reason (an empty summary would read as "nothing is covered"). Visit type and the contract's admission type are two vocabularies that share two words, so only Emergency and Day Case are passed through to `resolvedPrice` — guessing that an inpatient visit is Elective would price the encounter off a methodology nobody chose. A snapshot stores what the engine answered and is never recomputed; `override` sits alongside `systemResult` and `finalResult` is the override's result or the system's, which is what the worklist, the tab and the page all read. References are sequential per year (`ELG-2026-000123`), read off the trail itself the way `nextMrn()` is, so a reset reproduces them. `attemptOf` names the first attempt of a cascade and `cascadeOf(ref)` reads the whole run back off it.
UI calls: the check screen is `.split--40-60`, the question in the rail and the answer beside it, with each attempt a `<details>` whose `<summary>` is the panel header — the collapsible the design system needs no CSS for, the one the billing breakdown already uses. The design system ships no checkbox or radio row, so a cover is a bare radio inside a `.rule-child-row` and the Self-Pay fallback is drawn there rather than read from the repository, because it is never a row in the chain. A step's glyph carries the shape of the answer and a badge beside it the colour, since the system tints a badge and not an icon; a gate step reads Fail in critical, a coverage or pre-auth step reads Condition in warning, because a red Fail beside an Eligible result would say the opposite of what the check decided. The ⚑ marker is the `flag` icon in a `.badge--accent`, and the override dialog's error box is a plain wrapper the alert is written into — `.alert` sets `display:flex`, which outranks `hidden` and would leave an empty box on screen. The charge picker is written here rather than imported from Pactum's fee schedule: a module never reaches into another module's files, and `cdm.findActive()` is the part that has to stay in step. Eligibility is withheld on a restricted record for the reason Insurance is — a check names the payer and the plan.
Seed: `data/seed/eligibility.js` writes the outcomes as a table of intents — this patient, this policy, this many days ago, these anticipated services — and the engine produces each snapshot's steps and coverage, because a hand-written record of something that never ran would drift from the contracts the moment either changed. 15 snapshots over 8 patients: two cascades (a suspended policy then the NSSF cover underneath it; an expired policy then Self-Pay), one overridden (the ISF plan no Pactum contract names, confirmed by phone, ref 4471), two Self-Pay, one Auto-Registration linked to an encounter, and one refused at step 1 on the blocked record. Dates are offsets from today, so the Checked today card and the 7-day reuse window are demonstrable whatever day the demo runs. Like `data/seed/claims.js` it reads repositories, so `data/store.js` does not import it — `data/repositories/eligibility.js` builds on first read of an empty table.

### 15 — Frontis: Encounter / Visit Registration
Added the `encounters` entity (Frontis owns it; the note is on the manifest) and the third nav entry, badge = encounters open right now. `#/frontis/encounters` is the board (Today | All, the rail's four cards, search, Type / Status / Department / Doctor / Financial class / date-range filters, paging, and Open · Edit · Discharge · Cancel · View history on each row); `#/frontis/encounters/new` (and `?mrn=`) is the four-step flow; `#/frontis/encounters/<no>` is the encounter page, with a tab id after it deep-linking to Visit info, Financial, Linked records or History. The patient record gained an Encounters tab and its **New encounter** button is live. `data/seed/reference.js` is new and is the one place departments, doctors, wards and bed classes live — static shared data, no repository, the way `rule-attributes.js` is a catalogue.
`data/repositories/encounters.js` carries the entity and its lifecycle: `today`, `search`, `byPatient`, `activeOfType`, `nextNo`, `create`, `updateVisit`, `activate`, `cancel`/`cancelBlocked`, `discharge`/`lengthOfStay`, `autoCompleteOutpatients()` on load, `reclassify`, `linkRecord`, `setClearance`, `clearanceIndicator`, `financialLabel`/`financialTitle` and `classification`, and it pushes the encounters re-link hook into `patients.relinkHooks`. `shared/roles.js` gained `canReclassifyEncounter` (nurse, coder, CMO) and `canCancelWithCharges` (coder, CMO only — cancelling a billed encounter is a financial correction, not a front-desk one).
Data model calls: status is a fact about the start time and never a field a caller passes — a start in the future is Planned, and activating one moves the start to now so the length of stay is counted from the arrival rather than the booking. The board's Today scope is today's *work*, not today's start times: everything that started today plus everything still open, because a patient admitted on Monday is still on the ward on Wednesday — which is why "Active now" and the OP · IP · ER split are different numbers. "Not cleared" counts Pending and Blocked and not Not started: an outpatient visit that never needed clearing would otherwise make the card a headcount. Registration writes nothing until Create, with the one exception the domain forces — the eligibility check is a record of what the payer answered at that moment, so it is written when it runs and the encounter claims it afterwards through `attachEncounter` (which is write-once, so a reused snapshot stays with the encounter it was first spent on). A re-classification keeps what it replaced in `financialHistory` rather than overwriting it.
Masking: a restricted record's *cover* is withheld on the board, the Encounters tab and the encounter page's Financial tab — the same line the Insurance and Eligibility tabs draw — while the visit itself, its department and its trail are not. That a VIP was seen is not the secret; who pays for them is.
UI calls: the flow is the bundle builder's stepper, and the classification question is one file (`encounter-classification.js`) drawn by both step 3 and the Re-classify dialog, so there is no second set of rules about which policy is asked first. The clearance column is a tinted `.badge` with its dot rather than the amendment's bare icon: the design system's badge is 20px and an icon inside one spills out of it (the policy tab already learned this), so the word carries the state and the tooltip carries the items. The type split card ("4 · 2 · 1") selects nothing and is the plain `<div>` the system reserves for a number nothing can act on — the Type filter beside the table is what narrows to one of them. Register new patient mounts `patient-form.js` itself in the shared drawer with a ctx whose `navigate()` is the return path, so there is no second registration form; its own Cancel link is dropped from that copy, since it would take the page out from under the flow. The Linked records tab lists all five records with disabled Create buttons naming the feature that will fill them.
Seed: 26 encounters — eight hand-written for today (four clinics including one booked for this afternoon, two admissions plus yesterday's ICU stay still open, one emergency arrival; one admission with charges posted so the cancellation guard is reachable, one blocked clearance carrying "Pre-auth pending; Deposit not collected", and the same patient seen in clinic and admitted from it) and seventeen generated over the last thirty days with a fixed-seed PRNG, plus the admission `data/seed/eligibility.js` already points its Auto-Registration snapshot at — that seed's `ENC-2026-000418` is read back rather than assumed, so the two never drift. The seed also writes the register's own trail (Created, Discharged, Completed, Cancelled) with the times those events happened at, because `audit.log()` would stamp every one of them with now and with whoever is signed in; and it stamps `encounterId` back onto each snapshot an encounter consumed, which is what fills the eligibility worklist's Linked encounter column — now a link to the encounter page there, on the record's Eligibility tab and on the snapshot page.

### Frontis: activity links and a live eligibility cascade (no amendment — asked for directly)
`modules/pactum/features/home/home-activity.js` resolves the three Frontis entities that were falling through to its bare fallback: a `policy` entry opens the record's Insurance tab (a policy has no page of its own), an `eligibility` entry opens its snapshot, and an `encounters` entry the encounter page. All three join the entity filter on `#/pactum/activity`. Each carries `withheld`, read off `patients.view()`, because all three name the payer and the plan in their details — the thing the Insurance, Eligibility and Financial tabs already withhold on a restricted record — so a masked row says a policy changed, and whose, and nothing more. The encounter branch was not asked for; it was the only entity left on the fallback once the other two moved, and it leaks the same field.
Seed: `data/seed/policies.js` gained POL-0013 and POL-0014, both Active and in date on MRN-000113 (Marwan Talhouk) — a MEDGULF Comprehensive primary on a plan Pactum holds no agreement for, and an NSSF second-class secondary that it does. Every other chain in the seed is headed by a contracted plan, so this is the only record where a two-policy cascade is reachable live: the check refuses at the contract step and the next position answers. It is what makes the "Try next policy" prompt demonstrable on the check screen and inside step 3 of encounter registration, rather than only readable in the seeded trail.
No change to the visit-type mapping: `ADMISSION_OF` in `data/engines/eligibility-engine.js` already passed through Emergency and Day Case alone and left an inpatient or outpatient visit's admission type null, which is the behaviour that was wanted.

### 16 — Frontis: Pre-Registration
Added the `prereg` entity (Frontis owns it; the note is on the manifest) and the fourth nav entry, badge = today's arrivals still open. `#/frontis/prereg` is Expected Arrivals (four-card rail, Today + upcoming | All, search over number/name/phone/identifier, Status / Visit type / Department / date-range filters, paging, and Open · Convert · Cancel · View history on each row); `#/frontis/prereg/new` and `/<no>` are the one-page form (four panels, one Save, completeness in the rail); `#/frontis/prereg/<no>/convert` is the conversion stepper. `modules/frontis/features/prereg/` holds `prereg-worklist.html`/`.js` (which hands the mount over on both deeper paths, the way the encounter board does), `prereg-kpis.js`, `prereg-form.html`/`.js` + `prereg-form-panels.js` (the four panels) + `prereg-form-rail.js` (the rail, the banners and the ISO ↔ `datetime-local` pair), `prereg-completeness.js` (the bar and the checklist card), `prereg-chips.js` (the arrival line, the patient cell, the pre-check badge and the record's Expected section), `prereg-actions.js` (cancel and reactivate) and `prereg-convert.js`. The patient record gained a **Pre-register** header action and an Expected section at the top of its Encounters tab; Pactum's Recent activity resolves a `prereg` entry to the form and gained the matching entity filter.
`data/repositories/prereg.js` carries the entity and its lifecycle: `search`/`today`/`byPatient`/`expectedFor`, `counts`, `nextNo`, `create`/`update`, `setPrecheck`, `cancel`, `reactivate`, `markConverted`, `expirePreregs()` on load, `coverLabel`, `precheckIndicator` and the two type maps, and it pushes the pre-registration re-link hook into `patients.relinkHooks`.
Architecture call: the amendment puts `computeCompleteness` in the feature folder, but the repository reads it on every save and `data/` never imports from `modules/` — so the definition is `data/engines/prereg-completeness.js` (four groups, eleven items, `{ pct, ready, missing, groups }`) and the feature file of that name is the picture drawn from it, re-exporting the engine so a screen has one import. Completeness is never stored: `create`, `update`, `setPrecheck` and `reactivate` recompute it and set the status from it, so Pending ↔ Ready is a fact about the row and never a field a caller passes, and each flip is its own trail entry.
Data model calls: identity is read from the linked record when there is one, so linking completes those six items at a stroke — and what the call captured is kept beside it rather than discarded, which is what conversion offers to merge into the record. Three vocabularies meet here and each conversion is its own map: the pre-registration has a Day Case type, the board has no such thing (a day case is booked as an outpatient visit), and the eligibility engine keeps it as its own word. `expirePreregs()` measures 48 hours from the expected *time*, not the day, and `reactivate` asks for a new arrival rather than restoring the one that had already passed. Only the encounter flow writes the conversion — `prereg.markConverted` is called from `create()` in `encounter-new.js` and nowhere else — so a pre-registration is closed by the encounter it became.
The two hooks the amendment names, plus the two the entity forced. `policy-form.js` exports `openPolicyModal({ patientMrn, onSave })`, where a null MRN is the pending shape: the same form, validated the same way, resolving with the record instead of writing it (no chain cap, no overlap check — a patient who is not registered has nothing to overlap with). `encounter-new.js` reads `?prefill=<no>`, filling the patient, the visit, the start (the expected time only while it is still ahead — an encounter that opens now started now), the planned procedure as the visit reason and the cover the classification step asks first. `patient-form.js` reads `ctx.prefill` in register mode, which is how the conversion screen mounts the real register form with what the call captured. And `runAutoCheck` gained `pendingPolicy` and `preregNo`: a check may run before the patient exists, so the snapshot stores the pre-registration number instead of an MRN and the ladder is handed a stand-in record that says the patient is registrable — the question being asked is about the cover. `CHECK_TYPES` gained `Pre-Registration`; `coverLabel` names a plan carried without a policy id as "(not yet on file)" rather than reading it as Self-Pay; and the worklist and the snapshot page name the pre-registration where the MRN would be, with Re-check and the patient trail withheld until there is a record.
UI calls: the completeness card sits in the *left* rail, because the design system's `.split` fixes the bounded column first and a module writes no CSS to move it — the record page's summary is in the same place. The bar is inline SVG over `--viz-*` tokens (the system ships no progress bar, and Pactum's performance charts already draw this way), two colours only, because there are two answers and they are the ones the status badge gives. Run pre-check and Convert save the form first: a check is a record of what the payer answered and needs a number to hang off, which is also why both are disabled until a new form has been saved once. The conversion stepper's third step is drawn but never becomes current — creating the encounter happens in the encounter flow — and it stays in the plain "not yet" state rather than `--blocked`, which the system paints critical. The merge checklist is bare checkboxes in `.rule-child-row` (the system ships none, the merge screen made the same call for its radios) applied by one button, so one act is one trail line; it is withheld entirely on a record this role reads masked.
Seed: eight hand-written rows — four expected today (the happy path Ready, a new patient at 82% missing the Arabic name and an identifier, the ISF plan whose pre-check the contract step refuses, and a Self-Pay Ready), two upcoming (one an admission carrying the appendectomy package), one expected three days ago and seeded *open* so `expirePreregs()` is what closes it on load, and one already converted into a visit read back off today's board. Seven pre-check snapshots are produced by the engine the way `data/seed/eligibility.js` produces its own, taking the references after the eligibility seed's, and the register's own trail is written with the times the events happened at rather than through `audit.log()`.

### Frontis: a pre-registration check finds its patient (no amendment — found in review)
`data/repositories/eligibility.js` gained `attachPatient(ref, mrn)` and `data/repositories/prereg.js` calls it from `markConverted`. A pre-check run for someone not yet on the register writes a snapshot with `patientMrn: null` and the pre-registration number in its place, and nothing resolved it when that patient was finally registered: the snapshot stayed orphaned, so `latestValid` could not reuse it at registration (the seven-day window pre-registration exists to fill), `byPatient` never showed it on the record's Eligibility tab, and the worklist read "Not registered" with Re-check disabled for good. Converting now resolves it.
It is the third and last field a snapshot may gain after it is written, and the same shape as `attachEncounter`: write-once, audited, and it changes only which record the evidence hangs off — never the answer. Verified on the converting path: the snapshot re-points, `latestValid` finds it, the record's tab shows it, the trail reads "Linked to patient", a second conversion does not re-point it, and a genuine Self-Pay check (no policy and no plan) still reads Self-Pay rather than a pending cover.

### Frontis: the pre-check the desk paid for is the one the encounter carries (no amendment — the entry above, finished)
Two things were still short of the point of running a check early. `attachPatient` was called from `markConverted`, which runs inside `encounters.create()` — after step 3 has already asked `latestValid` and, finding nothing, run a second check against the same payer for the same visit. And it resolved the patient without the policy: a pre-check for a patient with no record was run against a cover with no id either, so the snapshot's `policyId` stayed null and `latestValid(mrn, POL-00xx)` still missed it — worse, the check read as a Self-Pay answer, which is the wrong classification to offer it to.
So the call moved to `prereg.update()`, which fires the moment an open row gains a `patientMrn` it did not have — the conversion screen registering the patient, or Link on the form itself, both of which happen before anything asks about reuse — and `attachPatient(ref, mrn, policyId)` now resolves both halves in one act, because registration is what creates both. Still write-once per field, still nothing touched but which records the evidence hangs off; `markConverted` keeps its call as the backstop for a conversion that never passed through `update()`. A self-pay pre-registration passes a null policy id and stays self-pay.
Verified end to end on the new-patient path: the conversion screen's step 2 now reads "ELG-2026-000017 · Eligible — offered for reuse", the encounter is created carrying that reference, the eligibility trail does not grow (22 rows before and after, where it used to gain a duplicate), and the record's Eligibility tab names the cover as AXA Middle East · Premium care rather than "(not yet on file)". The matched-patient path is unchanged and still reuses its own check.
The guard is per field rather than on the MRN alone, and gated on `preregNo`. Both halves arrive in one call today, so the looser guard changes nothing now; what it removes is a silent trap — a flow that registered the patient and filed the card second would have been locked out of ever resolving the policy, and would have failed by the check quietly ceasing to be reusable. The `preregNo` gate is what keeps that safe: an ordinary self-pay check also has an MRN and no policy and is meant to, so only a snapshot that named a pre-registration in place of a record can be resolved at all.

### 17 — Frontis: Cost Estimation
Added the `estimates` entity (Frontis owns it; the note is on the manifest) and the fifth nav entry, badge = issued and still valid. `#/frontis/estimates` is the list (four-card rail, search over number, name and phone, Status / Payer / Creator / date-range filters, paging, and Open · Duplicate · Print · Convert · History on each row); `#/frontis/estimates/new?mrn=&encounterNo=&subject=prospect` is the builder, which is also where a Draft opens at `/<no>`; and `#/frontis/estimates/<no>` is the printable document once it is not. `modules/frontis/features/estimates/` holds the list (`estimate-list.html`/`.js` + `estimate-kpis.js`), the builder (`estimate-builder.html`/`.js` + `estimate-inputs.js`, `estimate-lines.js`, `estimate-state.js`, `estimate-result.js`), the document (`estimate-view.html`/`.js`), the markup both priced screens share (`estimate-doc.js`), the chips the four screens read an estimate by (`estimate-chips.js`), the dialogs (`estimate-actions.js`) and the trail (`estimate-history.js`). The patient record gained a **Cost estimate** header action and an Estimates section on its Encounters tab; the encounter page's Linked Records Estimates row is live, listing the ids as links with a Create button pointed at that encounter; and `shared/config.js` is new — `estimateValidityDays` (14) and `estimateDisclaimer`, the two things more than one screen reads.
`data/engines/estimate-pricing.js` is the only place an estimate is priced: it runs `evaluateEncounter` — the same five steps a claim will be evaluated by — and then reads out of the traces the three things an estimate has to say out loud and a claim does not (the pre-auth flags, the exclusions, and where a bundle price runs out), returning the whole frozen-document shape. `data/repositories/estimates.js` carries the entity and its lifecycle: `search`, `counts`, `nextNo`, `create`/`updateDraft` (Draft only), `simulate`, `issue` (runs the engine once more, freezes `result`, stamps the validity, then supersedes), `duplicate`, `linkSubject`, `markConverted`, `cancel`, `expireEstimates()` on load, `byPatient`, `byEncounter`, `validForClearance(mrn)` — which is what F9 reads — and the merge re-link hook.
Data model calls: an issued estimate is never recomputed, so `result` holds the traces, the invoice rows, the totals, the limits, the flags, the exclusions and the disclaimer as they stood — a contract that changes tomorrow changes the next estimate, not this one. Superseding is on the pair (same subject, same *set* of item ids, order-independent), because two live prices for one operation is the thing a desk must never hand out; `duplicate` leaves the source Issued until the copy is issued, since a quotation being revised is still the one in force. Self-Pay is not faked as a contract case — with no plan there is no methodology, no coverage row and no rule, so the standard price is the price, the patient carries all of it, and the document says there are no steps to open rather than showing five empty ones. `evaluateEncounter` needs an admission type and an estimate holds a visit type, so `ADMISSION_OF` is now exported from `data/engines/eligibility-engine.js` and read from there — one copy of the rule that passes through Emergency and Day Case alone — and `encounters.js` gained `TYPE_OF_VISIT`, the way back, with Day Case folded into OP.
Conversion: `#/frontis/encounters/new` now also takes `?estimate=`, which fills the visit from the estimate's context (start now, not the quoted date), asks the classification step for the estimate's own cover first, and hands the estimate's lines to the eligibility check as anticipated services — `classifier` reads `state.services` if the screen carries any, so the check answers about the charges the patient was quoted. Creating the encounter is what writes the conversion and links the estimate back, beside the pre-registration's; a walk-in is registered first through `openRegisterDrawer`, which now takes a prefill, and the document is re-pointed at the record before the flow is handed the visit.
Architecture call: two Pactum files moved rather than being imported across a module boundary, which the hard rule forbids. `modules/pactum/features/billing-eval/breakdown-panel.js` is now `shared/billing-breakdown.js` and the consumption sub-table came out of `simulator-inputs.js` into `shared/consumption-table.js` (with `prefilledConsumption`, which was in `simulator.js`); the simulator imports both back. They read two repositories, which is what sets them apart from the rest of `shared/` — the arrow runs shared/ → data/ and never back, so nothing in `data/` can cycle through them. The repository reads its own engine the way `eligibility.js` and `prereg.js` already read theirs: `estimate-pricing.js` reads contracts and the charge master and never reads estimates.
Masking: an estimate names the payer, the plan and what the patient is expected to find, so a restricted record's document is withheld outright and the list withholds its cover and its money while still showing that a quotation exists — the line the encounter board already draws. `home-activity.js` resolves an `estimate` entry to its document with the same `withheld` flag, and Cost estimates joins the entity filter on `#/pactum/activity`.
UI calls: the builder is `.split--40-60` with the question in the rail and the price beside it, the billing simulator's layout, and Issue is enabled only while the simulation on screen is a simulation of exactly what is in the rail (an input signature, not a dirty bit) — a price nobody has seen is not a price anybody can be handed. The New estimate menu the amendment sketched is two buttons in the panel header instead, since the design system's `.menu` belongs to the floating bar and a two-way choice does not need a listbox. The document's prospect watermark is inline SVG with a rotated `<text>`, the call `perf-charts.js` already made for its shapes: the design system ships no watermark and a module never writes CSS. The totals rail is six cards at `--3`, patient share first, and it is a set of controls on the builder — where they filter the quotation under them through `INVOICE_FILTERS` — and plain numbers on the document, which always prints every line and so has nothing for a card to select.
Seed: 8 estimates written as a table of intents and priced by the engine — a self-pay quotation issued a month ago that `expireEstimates()` retires on load, the superseded pair (the survivor two days off lapsing, which is what the Expiring card counts), one converted into the admission the eligibility seed already points at, the appendectomy package with the overrun its limits block is worth reading for, an MRI carrying both a pre-auth flag and the one line NSSF names and refuses, a walk-in, and today's draft.

### 18 — Frontis: Referral Management
Added the `referrals` and `referralSources` entities (Frontis owns both; the note is on the manifest) and the sixth nav entry, badge = inbound referrals still open. `#/frontis/referrals` is the worklist (four-card rail, Active inbound | All | Outbound, search over number, patient, doctor and facility, Direction / Status / Specialty / Source / date-range filters, paging, and Open · Link to encounter · Reject · Cancel · Extend · View history on each row); `#/frontis/referrals/new?direction=&mrn=&encounterNo=` and `/<no>` are the one-page form; `#/frontis/referrals/<no>/view` is the read-only page, which for an outbound referral carries the printable letter. `modules/frontis/features/referrals/` holds the worklist (`referral-worklist.html`/`.js` + `referral-kpis.js`), the form (`referral-form.html`/`.js` + `referral-form-panels.js`), the read-only page (`referral-view.html`/`.js`), the chips four screens read a referral by (`referral-chips.js`), the dialogs (`referral-actions.js`) and the trail (`referral-history.js`). The patient record's Encounters tab gained a Referrals section with New inbound / Refer out; the encounter page's Linked Records Referral row is live and its header gained **Refer out**; `shared/config.js` gained `referralValidityDays` (30) and `data/seed/reference.js` the specialty list and the map onto departments.
`data/repositories/referrals.js` carries the entity and its lifecycle — `search`, `counts`, `activeInbound`, `nextNo`, `create`/`update`, `validForEncounter`/`fitsDepartment`, `link`, `attachEncounter`, `schedule`, `reject`/`cancel`, `extend`, `expireReferrals()` on load, `byPatient`/`byPhone`/`forEncounter`/`scheduledFor`, `attachMrn`, `referralGap` and the merge re-link hook — and `data/repositories/referral-sources.js` the growing register (`search`, `label`, `upsertFromForm`, `bump`), the one Frontis table plain enough to seed through `data/store.js`.
Data model calls: a referral is spent, not closed — `link` takes one visit off and a referral with none left is Used, which is why the worklist's Visits column only speaks when the total is more than one. `validForEncounter` is the picker's whole rule (open, in date, a visit left, and about this department or a specialty the reference list maps onto it), so an expired referral is absent from step 2 rather than offered and refused. The three ways a referral is spelled meet in `sourceLabel`/`destinationLabel`: `source.doctorId` is a referral-source id on an external inbound referral and one of our own `DR-` ids on an internal or outbound one, because those name a department of ours rather than a clinic. A phone number is compared through the register's own `normalizePhone` with the trunk zero off as well, since 03 447 219 and +961 3 447 219 are one phone. And `patients.afterCreateHooks` is new — the create-time twin of `relinkHooks` — so registering a walk-in resolves the referral the clinic phoned in last week without `data/repositories/patients.js` learning what a referral is.
The referral-required flag runs from Pactum to the board through one function. `contracts.referralRequired(contract, item|null)` reads Item → Category → Service Group → Contract, narrowest first; the eligibility engine's new sixth step calls it and raises "Referral required by payer — missing" as a condition (it never refuses, the way coverage and pre-auth never do); `referrals.referralGap` calls the same function and is what registration stamps on the encounter as `flags.referralMissing`; and `encounters.clearanceIndicator` folds the flag into the items its tooltip lists, so the board says it without a column of its own. `encounters.linkRecord(no, 'referral', …)` clears the flag, which is why it is cleared there and not in the referral feature. An outbound referral is deliberately outside all of this: it names its source visit through `attachEncounter`, spends nothing and is never written into `linked.referralId` — a referral this hospital wrote is not the referral the payer asked for.
Pactum: the Pre-Auth tab gained a Referral Required section below the matrix (`referral-required-section.js`, which owns its own node, markup and listener, plus the add/edit modal `referral-required-form.js` copied from `preauth-form.js`), with a contract-level Yes / No / Not set toggle over a table of scoped exceptions. `contract.referralRequired` is nested on the contract, so a new version clones it with everything else. Seed: NSSF requires one across the contract and exempts Consultation; AXA requires one on Radiology.
UI calls: New Referral is two buttons in the panel header rather than the amendment's menu — the design system's `.menu` belongs to the floating bar and a two-way choice does not need a listbox, the call the estimates list already made. The system ships no combobox, so a facility or a doctor is a select of what the register holds plus one "Type a new name…" option that reveals a field; typing there is what creates the source. The printable letter is the only thing on the read-only page without `data-print="hide"`, so Print hands the printer that and nothing else. And a check on file is not reused when it was run before the referral was chosen: it would still say one is missing, and reuse is about the cover, which a referral is not part of.
Seed: 10 hand-written referrals over 6 facilities and 8 external doctors — five inbound New (one written for three visits, one four days off lapsing, one for a person nobody has registered whose phone matches the seeded pre-registration, one on a specialty the hospital has no department for, one internal from the emergency room), one Scheduled against a seeded arrival, one Used on a visit read off today's board, one seeded open so `expireReferrals()` retires it on load, and two outbound. The NSSF admission on today's board carries `flags.referralMissing`, which is what the clearance tooltip and the encounter's Referral row open on.

### 19 — Frontis: Pre-Authorization Workflow
Added the `preauthRequests` entity (Frontis owns it; the note is on the manifest) and the seventh nav entry, badge = what the desk still owes somebody. `#/frontis/preauth` is the worklist (four-card rail, Needs action | All, search over number, auth number, patient and MRN, Status / Payer / Expiring-within (7/14/30) / date-range filters, paging, and Open · Submit · Capture response · Cancel · View history on each row); `#/frontis/preauth/new?mrn=&encounterNo=&estimate=&snapshot=&item=` and `/<no>` are the one-page form, which a draft opens at; `#/frontis/preauth/<no>` is the request page, with a tab id after it deep-linking to Request, Documents or History. `modules/frontis/features/preauth/` holds the worklist (`preauth-worklist.html`/`.js` + `preauth-kpis.js`), the form (`preauth-form.html`/`.js` + `preauth-form-panels.js` + `preauth-prefill.js`), the request page (`preauth-view.html`/`.js` + `preauth-services.js`), the printable form (`preauth-doc.js`), the chips five screens read a request by (`preauth-chips.js`), the dialogs (`preauth-actions.js`, `preauth-decision.js`) and the trail (`preauth-history.js`). `shared/config.js` gained `preauthExpiryWarnDays` (7).
`data/repositories/preauth-requests.js` carries the entity and its lifecycle — `search`, `counts`, `needsAction`, `nextNo`, `create`/`updateDraft` (Draft only), `submit`, `captureDecision`, `cancel`, `renew`/`resubmit`, `logCommunication`, `addDocument`, `consume`, `expireAuths()` on load, `activeFor`, `chainOf`, `byPatient`/`byEncounter`, `remainingFor`/`usedFor` and the merge re-link hook. The patient record's Encounters tab gained a Pre-Auths section beside the estimates; the encounter page's Linked Records Pre-authorisations row is live, listing each request's status, services and validity with a Create button prefilled from the visit.
Data model calls: a request is a draft until it is submitted and submitting freezes the services and the justification, so a decision is captured against exactly what the payer answered — everything else (documents, calls, the decision itself) is still recorded afterwards. An answer is a quantity, not a permission slip: `activeFor(mrn, itemId, on)` is the one definition of "authorised" — Approved or Partially Approved, `withinDates` the validity, and `approvedQty − usedQty > 0` — and every screen downstream asks it rather than reading the status. A partial decision writes the per-line grid and refuses to be recorded when nothing actually moved (that is an approval) or when every line was refused (that is a denial). `renew` asks for what is left and `resubmit` for the whole thing again, both as a fresh Draft with `predecessorNo`/`successorNo` set both ways, and `chainOf` walks to the first and forward from it.
Flag integration: `data/engines/eligibility-engine.js` gained `authorizationHooks` and `authorizationFor()`, an array the pre-auth repository pushes its lookup into as it loads — the arrow is repository → engine, so the engine stays a leaf and the two cannot cycle, which is the same idiom as `patients.relinkHooks`. Step 5 now reads "Authorised: <service> — PA-…, valid until …" in place of "Pre-auth required" where an authorisation covers the charge, and the coverage table's Pre-auth cell says Authorised with the request and its validity under it; the result is untouched, because a requirement that has been met is still a requirement. `data/engines/estimate-pricing.js` reads the same hook, so `preAuthFlags` carry `itemId` and `authorization` and the estimate document splits them into what still needs asking for and what is already in hand — frozen with the rest of the document, which is what the patient was told. Both the snapshot's flagged lines and the document's carry **Create auth request**, pointed at that snapshot or that estimate and that one charge.
Clearance: `data/repositories/encounters.js` gained `clearanceFlagHooks`, the array `clearanceFlags(enc)` folds in, and the pre-auth repository pushes one — "Pre-auth pending — PA-…" for a request with the payer, "Pre-auth expired — PA-…" for a lapsed one, and "Pre-auth exceeded/missing — <service>" read off the visit's own eligibility snapshot (`financial.snapshotRef` → the coverage rows whose `preAuth.required` is true and which no request answers). The hook is what keeps the arrow one-way: encounters never learns what a pre-auth is. `clearanceIndicator`'s Not started branch now carries the items in its tooltip as the other three already did, so work waiting on a visit nobody has stamped is still readable.
UI calls: the form is the referral form's `.split` — the summary in the bounded pane, four panels beside it, one Save — and Upload is disabled until the draft has a number, because a document is filed against one; Submit saves first for the same reason and then opens the request page scrolled to the printable form, which is what the payer receives. The decision dialog redraws its own detail half whenever the answer changes shape and carries typed values across, since retyping an authorisation number because a quantity moved would be the dialog's own fault. The Remaining column appears only once there is an approval to count down. A restricted record's request is withheld outright on the page and its cover, services, auth number and money on the worklist row — the line the encounter board and the estimates list already draw.
Seed: 10 hand-written requests — two drafts (one prefilled from the seeded MRI estimate, one the resubmission of the denial below it), three with the payer (one urgent and four days unanswered, which is the red the Pending since column shows), two approved (one five days off lapsing, one part-consumed at 3 of 8 sessions), one partially approved with two lines cut back and the report copy refused, one denied that the second draft answers, and one seeded live so `expireAuths()` is what retires it on load — that last one hangs off the open surgical admission, so the visit's clearance reads "Pre-auth expired" beside the "Pre-auth pending" of the urgent request on the same encounter. Amounts are read off the contract rather than typed, and the seed writes `linked.preAuthIds` on the visits it names so the Linked Records tab and `byEncounter()` agree.

### 20 — Frontis: Financial Clearance
Added the eighth nav entry, badge = visits that are Blocked. `#/frontis/clearance` is the worklist (four-card rail, Needs attention | All, search over patient, MRN and encounter number, Clearance status / Type / Department / Financial class / date-range filters, paging, and Open checklist · Open encounter on each row, ordered worst first then longest waiting); `#/frontis/encounters/<no>/clearance` is the answer for one visit, which is also the encounter page's new **Clearance** tab — a tab id in the path already deep-linked, so the screen and the tab are one file. `modules/frontis/features/clearance/` holds the worklist (`clearance-worklist.html`/`.js` + `clearance-kpis.js`), the tab (`clearance-view.js`), the five rows (`clearance-checklist.js`) and the two dialogs (`acknowledge-dialog.js`, `payment-dialog.js`). Two entities Frontis owns and only this feature writes: `acknowledgments` (the patient's signature against an estimate) and `payments` (money taken at the desk before the service, carrying `receiptNo` and `migratedToAccount` so a patient-accounts feature can take them over). `shared/config.js` gained `CONFIG.clearance` — what each kind of visit has to be signed for, what is collected up front and at what share, and how long an item may sit before the worklist paints it.
`data/engines/clearance-engine.js` is the only place the checklist is decided: `compute(encounter, ctx)` → `{ status, items, blocking, pendingSince }` over Eligibility, Referral, Pre-authorisation, Estimate acknowledged and Payment / deposit, each `{ key, label, state (Passed|Pending|Failed|N/A), short, detail, since, action }`, plus `estimateFor`, `anticipated` and `depositFor`. It is pure — it reads six repositories and writes nothing — and `data/repositories/clearance.js` is the wiring: `refreshClearance(no)`, `refreshAll()`, `indicator()`, `search`/`counts`, a store subscription that re-derives every open encounter after any commit that is not its own, and one sweep on load. Any Failed is Blocked, else any Pending is Conditionally Cleared, else Cleared. Regression needs no plumbing: an authorisation that lapses overnight or a check that ages out flips its item on the next sweep.
Architecture call: the amendment puts `refreshClearance` on `data/repositories/encounters.js`, but the engine reads referrals, estimates and pre-auth requests, and all three reach the encounter register through their own seeds — an import from encounters.js into the engine would close a cycle. So the encounter arrives as an argument, `encounters.js` keeps only `stampClearance(no, stamp)` (a writer that decides nothing, and neither commits nor logs, so a thirty-row sweep is one commit and audits only the moves), and `data/repositories/clearance.js` owns both ends. `clearanceIndicator` moved there as `indicator()` with it, since the statuses are now the engine's vocabulary; `CLEARANCE_STATUSES` is `['Not started', 'Cleared', 'Conditionally Cleared', 'Blocked']` and `needsClearance` reads the middle two. The ad-hoc pushes are gone as the amendment asks: amendment 19's `clearanceFlagHooks` and its push in `preauth-requests.js`, amendment 18's referral flag as a tooltip line, and the seeded `clearance.items` strings — all four pre-auth sentences are checklist items now, produced where every other item is.
Data model calls: item 1 reads the newest *passing* check on the visit's cover and falls back to the one the visit was classified on, which is what makes Re-check able to answer it — a re-check writes a new snapshot and never re-points the classification. Newest-of-any would be wrong in both directions: it would let a pre-registration check run for some other visit fail this one, and it would throw away a supervisor's override, which is a pass recorded against exactly the refusal it answers. Item 2 treats a referral spent on *this* visit as answered whatever it says now, since one visit came off it here. Item 3 takes the union of the contract-flagged anticipated services and the requests hanging off the encounter, because an approval that lapses under an open visit is the thing that gets missed and the contract may not flag what it was raised for — NSSF's Service-Group-Inpatient rule never flags a physiotherapy session, and the lapsed authorisation for one is exactly the seeded regression. Anticipated services are the union of the estimate's lines, the check's services and the requests' — three registers each knowing part of it. Item 4's estimate is the one an acknowledgment names, else one linked to the visit, else a live quotation for this patient that is about *this* visit (same department, same kind of stay), so a valid estimate for another department is never offered in its place. Item 5's arithmetic lives in `depositFor()` alone, so the checklist line, the payments card and the collect dialog quote one set of figures; Upfront Settlement is treated as a deposit at 100% until the accounts feature turns it on.
UI calls: the Clearance tab leads the encounter page whenever the visit still needs attention and yields to Visit info when it does not, since a cleared visit has nothing to chase. It owns its own node inside the panel so the listener for its two dialogs retires when the tab is redrawn — the shell's freshBody rule one level down — and it is built in JS rather than from a `clearance-view.html`, the call `encounter-linked.js` already made for a tab with no page of its own. A blocking chip is the item's `short` and the row's tooltip carries the sentence; time pending is a critical `.badge` past the config's hours rather than red type, because the system tints a badge and not a line of text. The Linked Records Clearance row is the one row with no record to link, so it shows the computed answer and opens the checklist instead of offering Create. The board's Clearance chip is now a control that opens the tab. And the receipt prints alone: the dialog marks `.main` with `data-print="hide"` while it is open and unmarks it on close, which is `app/app.css`'s existing rule used as written rather than new CSS.
Seed: `data/seed/acknowledgments.js` and `data/seed/payments.js` are tables of intents — this visit, signed this way, this share of the deposit taken — with the estimate found by the same rule the engine reads by and the amount computed from the contract, so a receipt is never a record of money nobody could have been asked for. Today's board gained two visits (a second-class-plan clinic visit whose CT the fund refused, and a self-funded admission nobody has quoted) and the estimates seed three quotations, one for each open admission clearance is worked against. The ten open visits answer Cleared ×5, Conditionally Cleared ×1 and Blocked ×4: an insured admission with every item settled and nothing owed at the desk (Bupa carries the whole allowed amount, so the deposit computes to zero); a half-collected deposit beside an authorisation still with the payer; a self-funded admission blocked on the estimate it never had; a refused authorisation; two visits the fund wants a referral for; and the lapsed authorisation on the surgical admission. `data/seed/encounters.js` writes exactly one clearance stamp, as of yesterday, so the first sweep has something to move from — which is how the trail carries "Clearance: Blocked → Conditionally Cleared (Pre-auth pending, $7.50 remaining)" rather than the claim that it would.

### Frontis: the encounter seed's generated visits were all self-pay (no amendment — found in review)
`data/seed/encounters.js` computed `policyId` for each of its seventeen generated visits and never passed it to the spec, so `classify()` was called with null every time and all of them were classified Self-Pay — which made the `insured` roll and the `INSURED` list beside it dead code, and left two of eighteen posted encounters resolving to a contract. The fix is one word, `policyId,` in the spec object. Found by the session applying amendment 21 while seeding the ledger off the board.
It cannot move anything amendment 20 seeded: `data/repositories/clearance.js` sweeps open visits only, and every generated row is Completed, Discharged or Cancelled. The one ordering hazard was checked — `buildEncounters()` resolves the `LINKED_SNAPSHOT` admission while the generated entries are still plain spec objects, before `row()` has claimed any snapshot, so EST-2026-000027's admission is unaffected. What does move is which other snapshots generated visits claim, which fills in the eligibility worklist's Linked encounter column.

### 23 — Frontis: Home Screen
Added the module's landing dashboard under `modules/frontis/features/home/`: `#/frontis` (and `#/frontis/home`) is now the first nav entry, with six KPI cards (`home-kpis.js`), four attention panels (`home-attention.js`) and five quick actions in the panel header — three links, plus New encounter and Check eligibility, which ask which patient in a dialog drawing the encounter flow's own step 1 (`home-quick-actions.js`) and then hand the MRN to `encounters/new` or `eligibility/new`. Recent activity is the Pactum panel's, and "View all" opens `#/pactum/activity?module=frontis`. Everything is computed live and redraws on `ctx.onData` and on a change of demo role. New data-layer code is five read helpers: `patients.createdToday()` and a `created` filter on its search, `encounters.activeNow()`/`typeSplit()`, `duplicates.open()`, and `clearance.byStatus()`/`outstandingItem()` over a new `item` filter (plus `itemOf`/`itemOutstanding`).
Architecture: `modules/pactum/features/home/home-activity.js` is now `shared/activity-trail.js` — a module never imports another module's files, and the resolver that turns an audit entry into a record page is exactly what both dashboards and the full trail need one copy of. It reads repositories, which is what it shares with `shared/billing-breakdown.js`: the arrow runs shared/ → data/ and never back. It gained the `preauth` and `account` branches, a `module` on each entity type and `entitiesOf()`, and `#/pactum/activity` gained the module select those feed — a payment or an acknowledgment is still named without a link, until the feature that owns it says where it is read.
Data model calls: the two clearance figures are `clearance.byStatus('Blocked')` and `clearance.outstandingItem('eligibility')` rather than the amendment's `encounters.*` — the stamp's vocabulary belongs to the file that computes it, and both helpers are literally `search()` with the link's own filters, so the card's number is the row count under it. Expected arrivals counts today's arrivals of every status (the worklist's own "Expected today" slice, which `?date=today` presses) rather than the nav badge's open-only set, because a card must equal the list it opens; "n ready to convert" is the sub-line, and the panel beside it is today's open arrivals then the days after, so its View all is the whole worklist rather than today's slice. Three filters were added to carry the deep links — Registered today on Patient Master, an outstanding-item select on Clearance, and `?date=today` on Expected arrivals, which selects that screen's own card — and `?view=needs-action` is accepted beside the Pre-Auths worklist's own `view=action`.
UI calls: the amendment's `.kpi__sub` is `.metric-rail-card__sub`, the system's own footer line, and the rail is six fluid cards in one row with no track count; the quick actions ride in the panel header rather than the floating bar, with Register patient primary and last, which is where the system puts a primary; the 2 + 2 grid is two `.worklists` rows, since that grid fits as many 280px columns as it is given and four panels would make four; the click handler ignores anything inside an `<a>`, so the MRN, Convert and Review links inside a row win over the row's own target; and Account flags is drawn only when `accounts.flagged()` exists — amendment 22's helper — so the dashboard is four panels until it lands rather than naming a number nobody can produce.

### 24 — Claima: Module Bootstrap + Parallel Protocol
Added module `claima` (Claims Management), registered between Frontis and Template, with one placeholder screen — `#/claima` (and `/home`) renders the "Claima — features arriving" panel with the claim count read live — and `modules/claima/COORDINATION.md`, the contract the three parallel feature sessions (amendments 25, 26, 27) write under: each adds its nav entry and routes inside the block marked with its TAG in `modules/claima/module.js`. `claims` moved from Pactum's holding to Claima's ownership (the note is on both manifests; the repository and the seed stay in `data/`), the repository pushes the merge re-link hook that moves a merged record's claims, and `shared/config.js` gained `CONFIG.claima` (`submissionDeadlineDays: 30`, `scrubberRules: []`, `ackSlaDays: 3`).
Data model: a claim is now its lines — `lines[]`, `totals`, `patientMrn`, `encounterNo`, `policyId`, `createdAt`, `batchId`, `remittanceId`, `scrubberFindings`, `attachments` and the eleven-status vocabulary — and the amendment-11 flat fields are a view of `lines[0]` and `totals`, re-derived by the new leaf engine `data/engines/claim-totals.js` (`lineOf`, `totalsOf`, `syncClaim`) on every write, so Performance reads the same figures it did. `data/seed/claims.js` keeps its generator and shapes each draw: the plan's coverage split gives the line its payer and patient share, a Pending draw is Submitted or Acknowledged by `ackSlaDays`, and the holder is a patient with a policy on the plan 40% of the time, any registered patient otherwise. The repository gained `search`, `byStatus`, `byPatient`, `byEncounter`, `history`, `nextClaimNo`, `create`/`update` (Draft/Ready only), `setStatus` (audited "from → to") and `recomputeTotals`, plus `isDenied`, `isPending`, `isEditable`, `IN_FLIGHT`, `UNSUBMITTED` and `LINE_STATUSES`; every helper amendment 11 wrote is unchanged. There is no `history` array on the claim: the trail is the audit entity keyed on the id, read through `history(id)`.
Two calls: legacy `allowedPaid` is what the payer allowed on its remittance — `totals.paid + totals.patientShare` once `paidAt` is set, zero before — so a fully paid claim still reads as allowing the whole expected amount and the variance definitions hold; and `data/engines/performance-engine.js` now asks the repository what a status means (`claims.isDenied`, `claims.isPending`) in the four places it compared status literals, since 'Pending' left the vocabulary and an Appealed claim with nothing paid is still a denial — four one-token edits, the one thing touched in Pactum, and `selfCheck()` still passes. `data/seed/claims.js` reads the patients and policies repositories now as well as contracts and the charge master; nothing in that chain reads claims, so the graph stays acyclic.


### 25 — Claima: Charge Capture
Added the capture register under `modules/claima/features/charge-capture/` and the second Claima nav entry, badge = visits still holding unreleased lines. `#/claima/charges` is the unbilled worklist (four-card rail, search over visit, patient, MRN, code and reference, Department / Line status / Flag / Source / date-of-service filters, one row per visit that expands to its lines, Edit · Replace · Cancel · Hold/Unhold · Approve late · Release line · History on each line, Release on each visit, and Manual charge · Run feeds now · Capture health in the panel header); `#/claima/charges/health` is the health screen (feed status per source with Run now and the failed events, bed-night gaps, zero-price lines), which the worklist hands the mount over to on the deeper path. `unbilled-worklist.html`/`.js` + `unbilled-rows.js` + `unbilled-kpis.js`, `manual-charge.js` (the modal, previewing through the engine), `charge-line-actions.js` (the dialogs), `feed-simulator.js` (the run and its result dialog, shared by both screens), `capture-health.html`/`.js` and `charge-history.js`. Two entities Claima owns: `charges` (`data/repositories/charges.js` + `data/seed/charges.js`) and `feeds` (`data/repositories/feeds.js` + `data/seed/feeds.js`, which also keeps the `feedRuns` table). `CONFIG.claima.capture` holds the 7-day late window and the per-category triggers; `shared/roles.js` gained `canLateCharge` (coder and CMO). The published surface — `byEncounter`, `releasedByEncounter`, `release`, `afterReleaseHooks`, `afterCorrectionHooks`, `lateCharges` and the rest — is listed in `modules/claima/COORDINATION.md`.
Architecture calls: a capture is priced by `account-engine.postCharge` and written through `accounts.postCharges`, the same act the accounts desk posts with, so a captured charge lands in the ledger, stamps `chargesPosted`, links the account and draws on an upfront settlement exactly as a posted one does; the repository reads `accounts.js`, which never reads it back. Every correction is the ledger's own shape — `edit` and `replace` reverse the rows and capture afresh with `reversesId` set (unreleased lines only), `cancel` reverses, `hold` moves nothing — and a released line cancelled or held fires `afterCorrectionHooks` for the claim. The two features a late charge has to tell — A27's `claims.onLateCharge` and A26's `coding.requestRecode` (only on a visit that already has a chart, since a release is what puts one in the pool) — are reached through dynamic imports rather than static ones, since both files import this one statically and a static import back would close a cycle at module evaluation. Both tables build on first read; `data/store.js` is untouched. The seed's unreleased lines are pushed straight into the ledger with the next ids and sequence numbers, the way `data/seed/ledger.js` writes its own — a seed never commits.
Data model calls: a line is late when the visit has closed or already released something, inside the window while that was ≤ 7 days ago; a feed's late line beyond the window is captured Held for a role with late-charge rights to approve, while a person with those rights approves as they capture, by giving the reason. `ZeroPrice` reads `allowed === 0` rather than the amendment's gross-and-allowed-both-zero: no charge-master line is priced at zero and a seed does not edit another module's catalogue, so the flag catches the contract's Not Billable rule as well and `zeroPriceReason` names which of the two did it — the health panel offers Open in CDM for one and the contract's Rules for the other. A gap is an admission whose midnights on the ward (its length of stay once discharged, the calendar days since it opened while it is still in the building) outnumber the Room & Board captured on its lines and inside its packages' consumption, so the seeded gap is the intensive-care admission with one night and no room line — the amendment's "3 nights, 2 room lines" needs a ledger this seed does not write — and the pending census event closes it on the first run. A package captured live is one line carrying every ledger row it produced (`overageRows` counts the extras), and the seed reads the ledger the same way, folding an overage row onto the package it hangs off. Releasing a visit takes every Unreleased line that is clean and reports the rest — held, disputed, zero-priced, or late and unapproved — as excluded with the reason. The encounter page's Linked Records list has no registration hook, so the worklist links to the encounter and not the other way.
UI calls: the group row's Release is disabled with the reason when nothing on the visit is clean; a line's Edit and Replace are disabled once released, since a released line is corrected by cancelling it; the manual charge's Capture carries the repository's own refusal in its tooltip, so the late window and the role gate read the same sentence the repository would return; a failed feed event keeps its refusal on the row with Retry beside it; and the health screen's cards jump to the panel that breaks the number down, the Contract Performance pattern. Seed: 47 released lines read off the ledger (one reversed, two Manual), 12 unreleased over four visits — the ICU admission (one held on the radiologist, one consult entered by hand), the emergency arrival, the surgical admission whose released package makes the nursing hours late and whose fund absorbs the antibiotic, and an orthopaedic discharge from before the window with a physiotherapy line awaiting approval beside a report copy already approved — and six Pending feed events, one of which fails on run against a retired charge. The ledger rows a feed posts are stamped `by` whoever is signed in when it runs, because `accounts.postCharges` takes no `by`; the capture line itself names the feed.

### 26 — Claima: Encounter Coding & CDI
Added the coding feature under `modules/claima/features/coding/` and the second Claima nav group: `#/claima/coding` is the worklist (four-card rail — Awaiting, Beyond SLA, In progress, Query pending — All charts | My work, search over encounter, patient and MRN, Status / Coder / Department / Type / Age band / Completed-date filters, paging, and Assign · Take · Open · View history on each row, worst first), `#/claima/coding/<encounterNo>` the workspace (`.split--40-60`: the evidence — patient, visit, released charge lines, documents with an inline text viewer — in the bounded pane, the coding beside it: ICD and procedure search with Add, Principal radio, POA select on an admission, a card per procedure with date, doctor and the charge lines it accounts for, the Queries strip, the validation panel with Jump on each blocking entry and Acknowledge + reason on each warning, and Release to pool · Save draft · Mark coded), `#/claima/coding/queries` the physician queue (oldest first, every physician's with a filter, the thread in the shared drawer where a physician role answers and a coder resolves, reopens or withdraws) and `#/claima/coding/metrics` (backlog by age band × department as stacked bars, charts coded per day per coder over 14 days, open queries by physician, turnaround and the share inside SLA by visit type — inline SVG copied from Pactum's perf-charts, not imported). Nav badges: Coding counts charts beyond SLA, Queries the questions no physician has answered. `shared/roles.js` gained `canCode` (coder, CMO), `canAssignCoding` (CMO), `canRecode` (coder, CMO) and `isPhysician` (the attending role); `shared/config.js` gained `CONFIG.claima.coding` — the SLA per visit type, the charge categories a procedure code has to account for, and the age bands.
Three entities, Claima's (the note is on the manifest). `coding` and `cdiQueries` share `data/repositories/coding.js`, over the cap on purpose: a query moves the chart's status and the chart's validation reads the queries, so splitting them would put one rule in two files. The record keeps every version ever marked coded and the draft is simply the newest version without a `codedAt`; `byEncounter(no)` hands a claim the newest *coded* one. `validate(no, draft?)` is the one list of what blocks Mark coded — one principal, POA on every inpatient diagnosis, every released Procedure / Surgery / Radiology line linked to a procedure, every procedure linked to a line and dated inside the visit, no open query — and of what only warns (an unspecified code, a sex or age the code does not fit, a procedure with no doctor), which an acknowledgment with a reason answers; the workspace runs it over the draft on screen, so the panel is right before the save. `recode()` appends a copy of the standing version carrying the reason, fires `afterRecodeHooks` and calls `claims.markStale?.()`; `markCoded()` stamps it, closes the requests and fires `afterCodedHooks` — the two arrays amendment 27 subscribes to. `requestRecode()` is what a denial or a late charge calls; a request on a coded chart paints it Recode Requested and starts a fresh SLA clock at the request, since the first coding was on time. `clinical-docs.js` is the evidence a chart is coded from (`byEncounter` is what a claim attaches); `code-sets.js` is static like `reference.js` — ~170 ICD-10 codes and ~65 procedure codes, 28 and 27 written out and the rest families × modifiers, each carrying the sex and age band the sanity check reads.
Data model calls: the released lines are the capture register's (amendment 25 — `charges.releasedByEncounter`, priced at what the payer allows, and a procedure links to its `CHG-` ids), with the ledger's posted charges standing in only where that register cannot answer; the seed reads the lines the same way, so a chart seeded on the ledger and one seeded on the register never disagree. The amendment says "procedure-category" lines and the charge master's Procedure category holds a dressing and a nebulisation; imaging is a coded procedure too, so the rule reads Procedure, Surgery and Radiology from config — without Radiology no closed visit on the board would have had a line to link. `assignedTo` and `codedBy` are coder ids: the demo's RCM role and two colleagues who never sign in (`CODERS` in the seed), so assignment, the Coder filter and the throughput chart have somebody to compare against. A chart saved by a coder who had not taken it becomes theirs on that save. Query status drives chart status — Query Pending while one is Open, back to In Progress when the physician answers — and an Answered query still blocks Mark coded until the coder resolves it, because an answer nobody has read is not an answer. The physician queue shows every physician's queries with a filter rather than one doctor's: the demo's physician role is not on the attending list, so `isPhysician` gates the Answer button and the thread records who answered.
UI calls: the code pickers are a search field over a hits table with Add on each row, since the design system ships no combobox and a table of twelve hits is the honest picture of a catalogue search; Enter adds the first hit. A procedure arrives dated on the day the visit closed, under the attending and linked to the first unlinked line in its own category — one checkbox away from right when the guess is wrong. The workspace redraws whole from state on every change and the two search fields redraw only their hits, so a half-typed code survives the redraw that adding the last one caused; Mark coded is disabled while the draft is unsaved, because what is marked is what is stored. Jump focuses the section before it scrolls (the call Contract Performance already made). The metrics' stacked bars and day columns are the only shapes the screen needed and they are SVG over `--viz-*` tokens like every chart before them.
Seed: six charts found on the board by patient and visit rather than by number — the demo coder's heart-failure admission In Progress inside its SLA with an answered Missing Documentation query (I50.9 waiting to become I50.21), a VIP pneumonia admission Query Pending seven days on a Specificity question the note cannot answer, a cardiology CT visit and an oncology emergency visit Coded (one procedure each, linked to the imaging line), a collapse admission Coded and sent back by an internal audit (the seeded Recode Request, I95.1 to replace R55 as principal), and an orthopaedic admission In Progress with the other coder and past its SLA — plus three queries in three states and eight documents whose text says what the coder needs to find. Everything is dated as offsets from today, so the SLA colours, the 14-day chart and the query turnaround hold whatever day the demo runs; the eleven other closed visits sit Unassigned, which is the backlog the Beyond SLA card counts.
Found in verification, not Claima's: `store.resetToSeed()` overflows the stack at committed HEAD (the derived seeds re-enter each other while every table is empty, because the reset commit runs subscribers before anything is rebuilt), and `modules/frontis/features/accounts/account-receipts.js` both imports and declares `emptyHtml`, so the accounts screens fail to load. Both are in COORDINATION's Conflicts and left alone here.

### 27 — Claima: Claim Assembly & Portfolio
Added the claim portfolio and the claim page under `modules/claima/features/claims-assembly/`, plus two leaf engines. `#/claima/claims` (nav badge = Ready for submission) is the portfolio: a four-card rail (Value in assembly, Ready, Stale, Avg age — the fourth sorts by age rather than filtering), an In assembly | All scope, a By payer | By status | Flat view (grouped views are `<details>` per group with a count and a total), search and Payer / Status / Scrub result / Value band / Age / date filters, Open · Run scrub · Finalize · Refresh · History on each row, a checkbox per row with **Scrub selection** and **Finalize all passing** in the panel header, and downstream rows (Submitted+) read-only with a "Managed in Submission / Remittance" note. `#/claima/claims/<no>` (and a tab id after it) is the claim page: banner with kind chip, status, scrub result, stale chip, cover, contract-version chip, value, age and the parent/child chain links; a stale banner with Refresh now; Refresh · Run scrub · Finalize · Reopen; and Lines (overage rows, stamped authorisations, Late chips, the lines the assembler left out and why), Coding (the snapshot, with Open coding), Attachments (the contract's requirements with a missing indicator, what is attached by origin, the visit's other clinical documents with Attach, upload with link-to-lines), Scrub (the latest run grouped by category, jump-to, Acknowledge on a warning, earlier runs) and History (the trail, with each refresh's diff inline). `data/engines/claim-assembler.js` (`assemble`, `snapshotCoding`, `diff`, `sameLine`) and `data/engines/claim-scrubber.js` (`scrub` over six categories, `resultOf`, `grouped`) are leaves; the A27 section of `data/repositories/claims.js` resolves the world for them and publishes `markStale`, `onLateCharge`, `assembleFor`, `readyForSubmission` and the rest (all listed in `modules/claima/COORDINATION.md`). `data/repositories/claim-attachments.js` is the upload register; `shared/roles.js` gained `canReopenClaim` / `canAcknowledgeScrubWarning`; `CONFIG.claima.assembly` the value bands.
Pactum: the Pre-Auth tab gained a Documentation Required section below Referral Required (`documentation-required-section.js` + `documentation-required-form.js`, the referral pair copied), over `contract.documentationRequired` in `data/repositories/contracts.js` (`documentationFor` and the save/remove pair). Rows add up rather than override — a payer that wants a summary on every claim and a report with the imaging wants both — and a threshold narrows a row to lines (or, on a Contract row, claims) worth more than it. Seed: NSSF wants a Discharge Summary on the whole contract and an Operative Note on Surgery above $2,000; AXA an Imaging Report on Imaging.
Architecture calls: the three peer repositories are loaded by dynamic import and feature-detected — `coding.js` imports `claims.js` statically, so a static import back would close a cycle, and the screens had to render against seeded claims before any peer existed. The seed and the load-time assembly wait for all three to settle (`claims.peersReady`), so a seeded line carries the capture register's `CHG-` id; a claim assembled off the ledger before the register loaded still matches the register's line for the same posting because `sameLine` falls back to the ledger rows they share. `data/engines/claim-totals.js`'s `lineOf` now spreads unknown fields through, so the assembler's `chargeLineId`, `authNumber` and `late` survive `syncClaim` — no existing field or behaviour changed. The assembly claims are seeded lazily on the first read that needs them rather than when `data/store.js` loads, and a reset re-seeds on the next read.
Data model calls: a claim is assembled from the released lines with a payer share, each overage line its own row; with no contract every line is kept and the scrub says why the claim cannot go, since an absence is not a zero — which is what the ISF visit's claim demonstrates. The coding snapshot is what the claim was assembled with: a refresh replaces it only when the coding register holds a coded version, and a chart the register never coded leaves it standing. The Secondary is defined at assembly from the policy behind the one the visit was classified on, Draft and not activated, with no lines until the primary's remittance — Finalize and Refresh refuse it and say so. Stale voids the scrub and takes a Ready claim back to Draft; a Ready claim is locked (no refresh, no upload, no scrub) until Reopen, which is role-gated and needs a reason. A late charge lands on a Draft primary flagged Late, and on a Ready+ primary spawns (or extends) a Supplementary chained to it. The Marked coded hook assembles a visit with no claim, refreshes a draft, and stales a finalized claim. The scrub's Financial check re-prices every non-overage line through the billing engine at its date of service on the stamped version, so a contract corrected after assembly shows up as a difference rather than silently.
UI calls: a warning is acknowledged per run — a new run is new evidence — and an Error blocks finalizing outright; a scrub finding's jump opens the tab it is read on and scrolls to the line, the totals row or the requirement. The rail's Avg age card sorts rather than filters (a second click reverses), the one card whose number is not a row count. The refresh diff is one renderer (`claim-refresh.js`) used by the dialog a refresh ends in and by the History tab. The claim page's tab strip is the design system's `.sections` directly under the panel header, the encounter page's shape. Portfolio numbers: the amendment's twelve seeded claims are eleven — nine closed, insured, charged encounters exist and the assembler keeps one primary per encounter, so three plain drafts rather than four; and the two Ready claims are exactly the two the engine passes today, since a seeded Ready claim whose scrub no longer passes stays a draft rather than lying about its lock. Claim numbers are read off the table after the 600 generated rows (the first is CLM-2026-000602, because a generated row already holds 000601). A restricted record's claim is withheld on the page and its cover and money on the row, the encounter board's line.

### Fix: reset overflow and the accounts page (no amendment — asked for directly)
`store.resetToSeed()` overflowed the stack because the derived seeds re-enter each other while every table is empty: the eligibility seed's `verify()` and the estimates seed's `priceEstimate()` both consult the pre-authorisation hook, whose register seeds off the estimates, which seed off the encounters, which seed off the eligibility checks. A fresh load never met the cycle only because the hook is registered after those seeds have built; a reset runs it with every hook in place. Both engines now take `{ authorizations: false }` — `verify(input, { authorizations })` in `data/engines/eligibility-engine.js` and `priceEstimate(estimate, { authorizations })` in `data/engines/estimate-pricing.js` — and the three seeds that verify or price while building (`data/seed/eligibility.js`, `data/seed/prereg.js`, `data/seed/estimates.js`) pass it, which is the answer a fresh load already gave. `data/store.js` gained a re-entry guard on `resetToSeed()`: a reset runs once and notifies every subscriber exactly once with `'reset'`. Verified: Reset demo data completes, and `#/pactum/home`, `#/frontis/patients`, `#/claima/claims` render after it.
`modules/frontis/features/accounts/account-receipts.js` both imported `emptyHtml` from `account-tabs.js` and declared its own, so the module failed to parse and `account-view.js` with it; the local copy is gone and the import stands. `node --check` passes such a file, which is why the Speed rule now says the browser load is the verification. `#/frontis/accounts/MRN-000105` loads.

### Claima: every Claima entity links from Recent activity (no amendment — asked for directly)
`shared/activity-trail.js` resolves the Claima entities now, through a module half of its own: `shared/activity-claima.js` carries `CLAIMA_ENTITY_TYPES` (spread into the trail's list, and Claima joins the module filter on `#/pactum/activity`) and `describeClaima(entry, isMasked)`, which the trail asks before its no-link fallback — split by module so the trail file stays near the cap as the platform grows. Five branches read their repository: `charges` (a line, opening the capture worklist grouped under its visit through the `?encounter=` deep link it already accepts), `feeds` (the health screen), `coding` and `clinicalDocs` (both keyed on the encounter number, both opening the workspace) and `claims` (keyed on the claim id, opening the page by claim number; withheld on a restricted record the way an encounter's row is, since a claim names the payer and the money). Five are wired ahead of amendments 28–30 by id alone — `batches` → `#/claima/submission/<no>`, `remittances` → `#/claima/remittances/<no>`, `denials` → `#/claima/denials/<id>`, `writeoffs` → `#/claima/writeoffs/<id>`, `nullifications` → `#/claima/nullifications/<no>` — so their rows link from the day each feature lands; the owning session upgrades its branch to a lookup when the repository exists (A28 and A30 have confirmed the keys and routes; A30's follow-ups log on the claim itself).
`data/repositories/coding.js` now reads its table once on load, the way the other self-seeding registers sweep on load: its seeded trail was written on first read, so a dashboard that never opened a chart showed none of it.

### 28 — Claima: Claim Submission
Added the submission feature under `modules/claima/features/submission/` and two Claima nav entries: Submission (badge = batches not yet with the payer) and Rejections (badge = claims sent back, still to fix). `#/claima/submission` is the workbench — a four-card rail (Ready value jumps to the queue, Due today and Open batches select their rows, Rejected opens the worklist), the ready queue by payer (count, value, mode, cycle with "due today", oldest age, and Create batch or Add to the payer's open batch) and the batches table (search across batches and the claims inside them, Payer / Status / created-date filters, Open · Generate · Mark submitted · Acknowledge · Rejections · History on each row); `#/claima/submission/<batchNo>` is the batch page — banner, the Open → Generated → Submitted → Answered stepper, and Claims, Files, Submission, Acknowledgment, Rejections and History tabs, a tab id in the path deep-linking to each; `#/claima/submission/rejections` is the worklist, whose rail's cards are the fix routes. `submission-workbench.html`/`.js` + `submission-kpis.js`, `queue-panel.js`, `batches-table.js`, `batch-chips.js` (status, mode, contents, the timeline and the cycle strip), `batch-actions.js` (create, add, generate, mark submitted, exclude, reject, close), `acknowledge-dialog.js` (the reconciliation form), `fix-dialog.js` (Fix & Resubmit), `batch-view.html`/`.js` + `batch-tab-claims.js` + `batch-tab-files.js`, `batch-history.js`, `rejections-worklist.html`/`.js`. One entity Claima owns, `batches` — the batch with its files, its submission, its acknowledgment and its rejections, one shape in `data/repositories/batches.js` (over the cap on purpose: one entity, one file), seeded on first read from `data/seed/batches.js`; `data/seed/rejection-reasons.js` is static like `reference.js`; `data/engines/submission-files.js` writes the files (JSON lines + CSV manifest for an electronic payer; printable claim forms, cover sheets and a batch cover for a manual one); `CONFIG.claima.submission` holds the payer profiles (mode, cycle, methods). The published surface — `byClaim`, the three hook arrays, `claims.resubmit`, `claims.rejectionsWorklist` and the rest — is in `modules/claima/COORDINATION.md`.
Data model calls: a resubmission keeps the claim number — the record is the same, `cycle` counts the trips to the payer, the closed cycles sit in `cycles[]` with the batch and the rejection each ended in, and `previousCycleNo` is a cycle number. Two records would have broken A27's assembler: a rejected cycle 1 still "live" would take the lines off its cycle-2 successor on refresh (`takenLines`), and the Refresh route is the one the amendment's own words assume (`assembleFor` + reopen). A batch member is therefore (claim, cycle) — `claimCycles` beside `claimNos` — which is what `byClaim` reads back per cycle. Acknowledgment is reconciled, not trusted: included = accepted + rejected + not received, the typed counts have to agree with the per-claim answers, and a claim the payer never received is ejected back to Ready for the next batch rather than marked anything. A rejection may be recorded on any batch that went out, before or after the acknowledgment (a Submitted batch with one becomes Partially Rejected and the ack form pre-marks it). Fix & Resubmit routes by the code: a claim assembled from a visit goes back to draft (Refresh re-assembles it, Recode raises the request on the chart through a dynamic import of `coding.js`) and is finalized again on the portfolio, where the store subscription in `batches.js` hears the `claims.status` commit, closes the rejected cycle and puts the claim in the payer's open batch; a legacy claim with no visit behind it cannot pass A27's scrub (no coding, no encounter), so it is resubmitted as filed on the next cycle once the desk has made the fix the route points at — which is the only way the seeded rejections can complete live. Generation validates first and ejects what is no longer Ready with the cause; adding to or excluding from a Generated batch takes it back to Open, since the files no longer describe it.
UI calls: the claim page has no tab registry and belongs to A27, so the cycle strip and the batches a claim went out in live on the batch page and the worklist, and the request for a Submission tab is in COORDINATION — link only until then. The printed kinds preview in the shared drawer with a Print button that marks the page and the drawer's own chrome `data-print="hide"`, the receipt's pattern; a text kind previews in a `<pre>`. The stepper's last step reads what the payer actually said (Acknowledged / Partially Rejected / Rejected) and is painted blocked only on Rejected. The claim form drops the Date and Auth columns when nothing in the claim fills them, so a legacy claim's form fits the drawer. The queue's button reads "Add to BAT-…" when the payer already has an open batch, so a payer never has two.
Seed: six batches over NSSF, Bupa and MOPH — the amendment's AXA and Bankers hold too few in-flight claims to fill one, so the manual payer is MOPH (Monthly · 5th) and the electronic one Bupa (Weekly · Wednesday); the profiles keep AXA Manual Weekly Tuesday as written. The claims are the generated in-flight rows, newest `submittedAt` first per payer and only those that existed by the day they were finalized, each stamped `batchId`, its status and a trail dated after its `createdAt`; the remittance seed (A29) skips anything carrying a `batchId`, so the two never answer the same claim. `store.resetToSeed()` now re-seeds through the reset fix above.

### 30 — Claima: Claim Lifecycle Tracking
Added the lifecycle feature under `modules/claima/features/lifecycle/` and four nav entries: `#/claima/pipeline` is the board (one column per `CONFIG.claima.lifecycle.boardColumns`, count and value in each header, cards with the claim, patient, cover, value, days-in-status chip, kind chip, family chip and ⚑, search and Payer / Department / Type / Value band / Age / date filters, Collapse families, a Columns menu kept in sessionStorage, and `?status=&bucket=` from an aging cell); `#/claima/aging` is status × 0–15 / 16–30 / 31–60 / 60+ over days in status, every cell a control that opens the board narrowed to it, with a footer that asserts the grand total and every column equal the board's; `#/claima/timeline/<claimNo>` is the family strip and the events; `#/claima/followups` is the queue (Silent / Escalated / Due today rail, Payer filter, Log follow-up on a row and on a same-payer selection) with badge = the silent count; `#/claima/payer-stats` is the table per payer (submitted, submit → ack, submit → payment, rejection and resubmission rates, denial rate, trend sparkline, silent count and value, escalated), a period selector, a drill to the monthly trend and Export to Excel. `pipeline-cards.js`, `lifecycle-chips.js`, `lifecycle-charts.js` (perf-charts copied, not imported), `followup-kpis.js`, `followup-dialog.js` and `timeline-tab.js` sit beside the five screens. New entity `followups` (append-only: `data/repositories/followups.js` + `data/seed/followups.js`); `CONFIG.claima.lifecycle` holds the silence and escalation thresholds (30 / 60 days, overridable per payer), the board columns and the hidden ones.
Architecture calls: `data/engines/claim-events.js` is the engine and the `lifecycle` interface in one — it reads claims, audit, handoffs, followups and payers, and is read by no repository, the billing-engine's position in the layering — so `lifecycle.escalated()` and `lifecycle.silent(payerId?)` are imported from there rather than from an A30 section of `claims.js`, which this amendment leaves untouched (a repository may import only a leaf engine, and this one is not a leaf). The three Claima peers — batches, remittances, denials — are dynamic imports feature-detected on every read, and their hooks are not subscribed to: every figure here is computed from the registers, so a posting or an acknowledgment moves a card, empties a queue row or clears an escalation by existing, and there is nothing a hook would have to reset. `payer-stats.js` calls `performance-engine.rollup()` for the denial rate and days to pay rather than restating them, and `selfCheck()` asserts on load that every payer's two figures equal `payerRollup`'s for the same period — the footnote on the screen reads the answer. The lifecycle vocabulary of a claim (status, patient, cover, value cells) is imported from `claims-assembly/claim-chips.js` — one module, one way of naming a claim — while the cross-module chart helpers are copied.
Data model calls: an event is derived and never stored. The timeline is the claim's audit trail first (every `Status: from → to` line, `Created`, `Refreshed`, `Scrubbed`, `Batched`, `Resubmitted`, `Activated`), then the claim's own stamps where the trail is silent — the 600 generated claims carry none — then the registers: `batches.byClaim` + `latestGeneration` for Batched / FileGenerated / Submitted / Acknowledged, `remittances.byClaim` for the posting and the patient shift, `denials.byClaim`, `handoffs.forClaim`, `followups.byClaim`, plus a computed Escalated. Two derivations say so on the row: a seeded Acknowledged claim with no stamp reads its acknowledgment off `ackSlaDays`, and a seeded denial with no answer date is shown at submission. A resubmission keeps its claim number (A28 counts cycles on one record), so the family strip draws `claims.cycleChain` — a closed cycle names its batch and the rejection it ended in — and `family()` walks `parentClaimNo` up and `childClaimNos` down for the supplementary and secondary claims; the collapsed board's card is the family's newest cycle with "+n" over what is folded. Days in status are read from the newest event that put the claim there and are tinted only on a status somebody is waiting on — a claim paid in March has sat in Paid for months, and that is age, not delay — so the 60+ aging cell is painted on open statuses alone. Silence is days since the last payer event (Acknowledged, Rejected, RemittancePosted, Denied), since submission when there was none; a card's number and the row count under it are one figure because the board and the aging table read one `visible()`.
Seed: five claims with the payer past the silence threshold, the oldest with each of five payers, carry the calls the desk already made — two on the escalated NSSF claim with the next one due today, an overdue email to the Army fund, a visit to the civil servants' cooperative, a call to Bupa booked for tomorrow and a message left at a switchboard with nothing booked — written into the register's own trail at the times the calls were made. The seed is deliberately not built on first read: the submission and remittance seeds move the claims they touch, so the events engine seeds the follow-ups once those registers have settled (and again after a reset), on claims no other seed has written a trail for, which is what keeps a seeded call from landing on a claim the payer has since paid.
UI calls: the design system ships no kanban, so a column is a `.panel` and a card a `.row-list` row — the shell's worklist vocabulary — and the columns sit in the `.worklists` grid with one inline `grid-template-columns` naming their count and `overflow-x: auto`, because a wrapping grid would put Denied under Draft and lose the left-to-right read; a column draws forty cards and a "n more" button, the header count being the whole column. Cards are not dragged, and the info glyph in the header says why. The ⚑ is the character the amendment writes, in a `.badge`, since a glyph inside the 20px badge spills out of it; the timeline's event icon sits in the detail column beside the tinted badge for the same reason. The Columns menu is the shell's `.menu-anchor` + `.menu--pop` popover. The follow-up rail's cards select within the payer on screen rather than clearing it, so the numbers stay the numbers for that payer; a queue row opens the timeline, the claim number inside it the claim. Payer statistics' drill panel is focused before it is scrolled to, the call Contract Performance made. Request open to A27: a Timeline tab on the claim page at `#/claima/claims/<no>/timeline`, delegating to `timeline-tab.js` (`render(host, { claimNo })`) — written under Requests in COORDINATION.md; until it lands the page is `#/claima/timeline/<no>`.

### 29 — Claima: ERA Processing & Payment Posting
Added the remittance feature under `modules/claima/features/remittance/` and the Remittances nav entry, badge = captured and not yet posted. `#/claima/remittances` is the workbench (four-card rail, Unposted first then oldest, search over number, payment reference and claim, Payer / Status / date / has-unapplied filters, paging, Open · View history on each row, and Upload ERA · Manual entry · Unapplied cash in the panel header); `/remittances/upload` the four-step ERA stepper (file → parse report with a downloadable failures file → review with each row matched → saved as Unposted, the file kept verbatim); `/remittances/new` manual entry (payer, reference, date, method and the total as the control figure, then a claim lookup that loads the claim's lines for paid / adjustment + code / denied + code, with the strip under the grids saying what is still to account for — `?no=` continues an unposted remittance, which is where a file's failed rows are typed in); `/remittances/<no>` the remittance page (banner, Post · Close · Enter lines, and the tabs Match & reconcile, Exceptions, Postings, File and History); `/remittances/unapplied` the cash no claim accounts for, by payer, red past 30 days, with Apply to claim · Refund to payer · Adjust. `workbench.js` hands the mount over on every deeper path. Three entities Claima owns: `remittances` (`data/repositories/remittances.js` + `data/seed/remittances.js`), `unapplied` (`unapplied.js` + seed) and `denials` (a stub with `create`, `reverse`, `byClaim`, `worklist` and the code catalogue in `data/seed/denials.js`, which amendment 31 takes over). `data/engines/posting-engine.js` is the pure half — the ERA parser, the line evaluation, the matcher, the control and `plan()` — and `CONFIG.claima.posting` carries the tolerance per payer, the 30-day warning and the adjustment codes.
Posting is one act with a fan-out the summary dialog lists record by record: each matched row's claim moves through `claims.applyRemittance` → `setStatus` (Paid / Partially Paid / Denied, `paidAt` the payment date, `remittanceId` the number, each line stamped with the payer's answer); a denied line becomes exactly one `denials` record; a PR-* adjustment becomes a `PortionShift` on the patient's account (the one additive Frontis touch the amendment allows — `TYPES` and `describe()` in `data/repositories/ledger.js`) and, when amendment 22's `data/engines/settlement-engine.js` exists, `reconcile(encounterNo)` is called after it — the engine is loaded by dynamic import and feature-detected, since that amendment is not applied yet; a coded shortfall past the payer's tolerance becomes a Defensio hand-off (`handoffs.create`, one per claim, the note naming the lines and the tolerance); an overpayment an exception the desk resolves by refunding, holding as credit or adjusting; the residue an unapplied cash row; and a partial payment on a primary with a cover behind it activates the secondary (`claims.activateSecondary` — the one A27 defined at assembly, or one defined now for a generated claim — its lines what the primary's payer left, chained both ways). Ambiguous and unmatched rows post nothing and wait as exceptions, so a clean claim posts while its neighbour is still being matched; Post is blocked outright only when the rows claim more cash than the payment carries. A posting is append-only: Reverse writes the pair — ledger reversals, the claims back to their previous status through `claims.revertRemittance`, denials marked, held cash adjusted, the secondary emptied — reopens the rows for correction and Post runs again; a hand-off already with Defensio is named for Defensio to close, not withdrawn from here. Close needs a standing posting, no open exception and no unposted row.
Data model calls: the amendment's control reads `Σpaid + Σadj + Σdenied + unapplied === total`, but a remittance's total is a cheque and adjustments and denials are reasons rather than money, so the block is on cash — `Σpaid + unapplied === total`, with adjusted and denied shown beside it — and the residue is what no row's paid amount accounts for; held once per remittance across its postings, so a second posting does not hold it again. Expected is the claim line's stamped `payerShare` and never recomputed. A PR-* shift is not a shortfall — the amount now belongs to the patient, so it comes off the variance and moves `payerShare` to `patientShare` on the claim line (which is what keeps Performance's `allowedPaid` right) — while a CO/OA adjustment stays in the variance: inside the payer's tolerance (the floor amount or the share of the line, whichever is kinder) it reads Adjusted and the claim is Paid, past it Underpaid, Partially Paid, and handed off. The account engine gained the one `effect()` branch a `PortionShift` needs — a third Frontis touch beyond the two the amendment names, made because a shift row the balance ignored would have been a lie on the account. `claims.nextPolicyOf` already existed in A27 for an encounter, so the A29 helper is `secondaryPolicyOf(claim)`.
UI calls: the matcher is the design system's `modal--xxl` with the candidates side by side and a chip on whatever the row echoes (same date, same billed amount, or a payer share equal to the cash) rather than a score; a row's own buttons sit in its `<details>` summary and the page's handler calls `preventDefault` there, or the click would also fold the row; the entry grid stacks each code select under its amount to fit seven columns; a hidden refund-reference field is wrapped in a plain `div`, since `.field` sets its own display and outranks `hidden`; the stored file is shown in a read-only `.field--area` textarea, the system's multi-line control, because it ships no `pre`; and manual entry reads every grid before it writes any, because the first write's commit redraws the screen and would have taken the unsaved grids with it.
Seed: six remittances written as intents and driven through the repository's own `post()` with the dates the events happened on — largest payer share first per answer, never a claim in one of amendment 28's batches, always submitted before the payment date, the payment moving after the claims when too few were (a peer caught a denial dated a month before its claim existed). NSSF Closed with $40 held 43 days (the red row), Bankers Posted with Exceptions from a file whose one row names no claim number and nothing to match it by, NSSF Posted with the PR-2 shift that activates Nour Baalbaki's AXA secondary and a $75 residue applied three days on, Bupa Posted with a CO-197 denial, a PR-1 shift and a $224 hand-off, the Army fund's file Unposted with two failed rows, Allianz manual Unposted half entered; the upload screen's sample is Bupa's answer to four more claims, built on demand off the claims table so it posts whenever it is loaded. The in-flight pool is thinner than the amendment assumed (a payer's unanswered claims are the recent ones, and amendment 28 batches the newest of them), which is why the money on the screens is what the pool had rather than the amendment's round figures.
Open: a secondary activated on a generated claim has no encounter, so A27's Refresh on its page fails with "Encounter null is gone" — the page could hide Refresh on a claim with no encounter; `#/claima/denials/<id>` in `shared/activity-claima.js` waits for amendment 31's page, and `unapplied` entries have no branch there yet (`#/claima/remittances/unapplied` holds them all).

### 33 — Claima: Write-Off Management
Added the write-off feature under `modules/claima/features/writeoffs/` and the Claima nav entry **Write-offs**, badge = requests waiting on the signed-in role's signature. `#/claima/writeoffs` is the worklist (four-card rail — Pending approval, Approved unposted, Posted MTD, Discretionary share — an All | Pending my approval scope, search over request, claim, MRN, patient and requester, Status / Source / Reason / Classification / Amount band / Side / date filters, paging, and a row that opens the decision page); `#/claima/writeoffs/new?denial=&claim=&mrn=&rerequest=` is the request screen (`.split--40-60`: source kind and record, amount with live validation against what is outstanding, reason with its classification badge, approval mode, justification and evidence in the rail; the context card beside it); `#/claima/writeoffs/<id>` (+ `/context|chain|history`) is the decision page (banner, Approve · Reject · Post · Reverse · Re-request, the tier ladder in the design system's stepper with the signatures under it, and Context / Chain / History tabs); `#/claima/writeoffs/analytics` is the breakdown (MTD / QTD / YTD, the contractual-versus-discretionary split first, by reason / payer / department / service line / requester, the six-month trend, approval statistics, repeat accounts and repeat payer-reasons, Export CSV, and the reconciliation footnote). `writeoff-chips.js`, `writeoff-kpis.js`, `writeoff-context.js` (one card for the request screen and the decision page, so the signer reads what the requester read), `writeoff-actions.js` (the four dialogs) and `writeoff-charts.js` (lifecycle-charts copied, not imported) sit beside the four screens. New entity `writeoffs` (`data/repositories/writeoffs.js`, over the cap on purpose — one entity, one file — and `data/seed/writeoffs.js`), the static reason list `data/seed/writeoff-reasons.js` (W01–W07, W99, each carrying its classification), the leaf engine `data/engines/writeoff-tiers.js` (`tierFor`, `stepsFor`, `tierBlock`, `canDecide`, `canReverse`, `decide`), `CONFIG.claima.writeoffs` (the three tiers, the default mode, `autoPostOnApproval`) and `canApproveWO1/2/3` on `shared/roles.js` (the coder signs tier 1, the CMO all three). Published helpers are under A33 in `modules/claima/COORDINATION.md`.
Data model calls: the chain request → decision → posting → reversal is immutable — a signed step is never edited, a posting is undone by a linked counter (the ledger's own `reverse()` on the patient side, `claims.reverseAdjustment` on the payer side) and the request stays on the register as Reversed. The requester never signs, at any step, whatever tier they hold, and a role signs a step only at a tier it holds; Sequential mode is one step per tier up to the required one, Single one step at it, and the request chooses with the config as the default. Posting re-validates against what is outstanding *now*: a balance that shrank caps the posting and the note says by how much, one that grew is never chased, and nothing left refuses. The payer side is `claims.adjust` — an entry under `claim.adjustments[]` and `totals.adjusted`, so the balance falls and Performance's `allowedPaid` does not move — and the status is not touched: a denial resolved by a write-off is the denial feature's call (`denials.resolveWrittenOff` is called when the source is a denial the register holds, `reopenFromWriteoff` on reversal, both feature-detected). The patient side is the ledger's existing `Adjustment` type with `detail.origin { writeoffId }` and `detail.allocations` oldest charge first, so the account's open charges shrink with the balance and the Frontis account page shows the row and the balance without a line of Frontis code changing.
Architecture calls: amendment 22's `settlement-engine.js` and `adjustment-form.js`, which this amendment names, were never built — nothing in the tree handled an `Adjustment` row — so `data/engines/account-engine.js` gained an `adjustments` bucket (the A29 PortionShift shape: one branch in `effect()`, subtracted in `outstanding`, `unpaid` and `patientDelta`) rather than cutting `patientShare`, which keeps `charges = payer + patient + undecided` true and lets a statement still say what was owed before it was let go; `settlementEngine.reconcile` has nothing to be called on and is not. The denials repository is reached by dynamic import and feature-detected in both directions (A31 reaches this one the same way), and the seed waits for it (`peersReady`) so a seeded request can name the denial it answers; a commit that resolves a seeded denial redraws the worklist while the table is still empty, which is why `ensureSeeded` carries a re-entry guard. The pursuit history on the context card reads `data/engines/claim-events.js` and `followups.js` from the screen, not the repository — a repository imports only a leaf engine.
Reconciliation: `reconciliation(range)` compares posted denial-sourced write-offs against `denials.amounts.writtenOff` over the denials they name and `selfCheck()` logs one `[writeoffs] self-check` line on load; a write-off naming a denial the register does not hold (a seeded stand-in on a Denied claim) is counted beside the check rather than inside it, and the footnote says so. Passes on this tree.
UI calls: the worklist's Discretionary card is a rate over the year and not a slice, so it is the one card that navigates (to the analytics) while the other three select; the request form's Submit is disabled with the repository's own validation in its tooltip and the same sentences in the error box once a field is touched; the tier ladder is the design system's stepper (done / current / blocked) with the signatures in a table under it; the chain is a `.journey` naming each linked record — the ledger row on the account page, the claim adjustment on the claim page, the denial on its page; the analytics' six breakdown panels are two `.worklists` rows of three, since the grid fits as many 280px columns as it is given; and a restricted record's request is withheld on the page and its patient, source and money on the row, the encounter board's line. The request route's `?item=` has no line-level write-off to open and is folded into the justification instead.
Seed: eleven requests found on the registers by rule rather than by id (the largest residual NSSF still owes, the account with the smallest balance, the oldest claim a payer has gone quiet on, the denials the triage seed classes as Write-Off Candidates), numbered WO-2026-000047…057 so the first live request is 000058 — a posted timely-filing write-off on a denial, two denial requests waiting at tier 1 and tier 2, an approved residual to post, a posted residual, a small balance posted on the ledger, a sequential hardship write-off with tier 1 signed and tier 2 waiting on management, a refused bad debt and its re-request raised today, a posted-then-reversed administrative error, and an aged item; the requesters are two clerks who never sign in, so both demo signers have work. Verified live: request → self-approval refused → approve → post → the Frontis account page reads the Adjustment and $0.00 outstanding → reverse puts it back; the payer side the same way on a residual; every module in the tree imported in the browser (374 files, 0 failures).
Found in verification, not A33's: `[performance] self-check FAIL` on a clean load of the current tree, flipping the moment `data/repositories/remittances.js` is imported — under Conflicts in COORDINATION.md for the consolidation pass.
A32 landed while this was being verified: a claim whose chain a nullification ended (`nullification.path === 'EndChain'`, Void, never resubmitted) is offered in the Aged item picker beside the silent claims, labelled "chain ended NUL-…" and defaulting to W02 — its balance is exactly what a write-off review picks up.

### 32 — Claima: Claim Nullification
Added the nullification feature under `modules/claima/features/nullification/` and one nav entry with no badge (a log is not work): `#/claima/nullifications` is the immutable log (a four-card rail — nullified this month, its value, the replacement rate, the top reason — search, Reason / Path / Disposition / Payer / Replacement / date filters, paging, Export CSV) and `#/claima/nullifications/<no>` the record (the whole record, the family strip with the nullified claim beside its replacement, the charge round trip — each line the claim handed back and where it is now, linking to the unbilled pool — and a timeline excerpt of both claims around the withdrawal). **Nullify** sits in the claim page's header on every status but Void — the dialog for an allowed path, the routing card for a blocked one — and a Void claim wears the record as its banner and its action chip; a replacement wears "Replaces CLM-…". `nullification-log.html`/`.js` + `nullification-rows.js` (table, empty state, CSV), `nullification-view.html`/`.js`, `nullification-chips.js`, `nullify-dialog.js` + `nullify-dialog-form.js`. One entity Claima owns, `nullifications` (`data/repositories/nullifications.js`, seeded once the claim repository's peers settle from `data/seed/nullifications.js`, again after a reset), `data/seed/nullification-reasons.js` static like `reference.js`, `CONFIG.claima.nullification`, `canNullifyClaim` / `canApproveNullification` on both financial roles, and the `nullifications` lookup in `shared/activity-claima.js`. The published surface — `pathFor`, `validate`, `nullify`, `byClaim`, `log`, `stats`, and the A32 sections of `claims.js` (`nullify`, `assembleReplacement`), `batches.js` (`removeClaim`) and `charges.js` (`returnFromClaim`, `releaseForReplacement`, `linesForClaim`) — is in `modules/claima/COORDINATION.md`.
Data model calls: the path is read off the status and nothing else — Draft and Ready (stale or scrub-failed included) go Direct and leave any open batch; Submitted and Acknowledged need the payer told, and the notification (method, reference, date) is a gate, not a note; Rejected ends the chain; Paid, Partially Paid, Denied and Appealed are blocked with the screen that undoes them first, because a posting is reversed in Remittances and a denial is worked from its denial — the claim is never nullified around either. The record is immutable and `nullify` is the one write; it runs the owning repositories' own writes in order (batch removal, the claim to Void with its lines Void and an unactivated secondary defined beside it voided too, the register lines back to the pool, the re-classification, the replacement), so each trail reads as that repository writes it and the record is the summary. A replacement is always a fresh assembly of the visit as it stands — the returned lines released again without the hooks, so the coding clock does not restart — never a copy, and a wrong payer re-classifies the visit through Frontis's `encounters.reclassify` before anything is assembled; a visit that lands on Self-Pay assembles nothing and the record says so. The second signature is gated on the claim's payer share and the threshold is $1,000, not the amendment's $2,000: the largest claim in the dataset bills $1,161, so at $2,000 the two-approver path could never be reached. Both financial roles carry both flags, so whoever raises it the other signs.
UI calls: the dialog's conditional blocks are plain wrappers carrying `hidden` (a `.field` or a `.toolbar` sets its own display); the replacement toggle is `.segmented`, the system's primitive for one of two, and a reason that usually replaces (N01, N04) turns it on; the second approver is a select of the people who hold the flag minus the requester, since the demo has no second sign-in; the rail's month-to-date count and value select the same slice and press together, the replacement rate selects the replaced records and the top reason selects its own; a restricted record's row withholds its cover, its money and its replacement, and its record page withholds outright, the claim page's line. The timeline excerpt is `lifecycle.byClaim` cut at the Void event — the batch removal names the nullification too, so the status is what is looked for.
Seed: six records numbered NUL-2026-000013…018, so the first live one is the amendment's NUL-2026-000019 — an end-of-chain on a generated AXA claim rejected twice (its two cycles written onto the claim with no batch, that dataset's own convention); the two-approver withdrawal of the largest acknowledged claim still with its payer (Bankers, $1,120.50, notified by email); a duplicate in the acknowledged Bupa batch, withdrawn on the portal; the ISF clinic claim the scrub could never pass, re-classified to Self-Pay with no claim to assemble — the fund confirms the cover and holds no direct-billing agreement, so the patient settles and claims back; the finalized MOPH clinic claim added to BAT-2026-000141 and pulled two hours later, assembled fresh — its replacement fails the scrub on "no coding" because the chart is in the coding backlog, which is the demo path (mark it coded and A27's hook refreshes the draft); and the Allianz clinic claim whose blood count the lab feed captured on the wrong visit, returned on hold. Claims are picked by state, never by number, and never one with a trail another seed wrote. Two things the seed costs the demo: the MOPH row leaves the submission ready queue (its only Ready claim is void and the replacement is a draft), and the portfolio holds no Fail scrub until one is made live.
Verified live: a Ready claim nullified with the two-approver signature and a fresh replacement (NUL-2026-000019 → CLM-2026-000615, the supplementary's line left off it), the routing cards on a Paid and a Denied claim, the held line in the unbilled pool with its "Returned from claim" trail, Reset demo data re-seeding the six, and every module in the tree imported in the browser (374 files, 0 failures). Open: Pactum's Performance still counts a Void claim's gross in "Gross billed" (`claims.list()` has no status filter) — a Void filter in `performance-engine.js` is Pactum's call; and a wrong-payer replacement onto a *contracted* cover has no seed, because no closed insured visit's patient holds a second contracted policy — add one on the Insurance tab and the dialog offers it.

### 31 — Claima: Denial Triage & Routing
Added the denials feature under `modules/claima/features/denials/` and two Claima nav entries: Denials (badge = untriaged) and Denial analytics. `#/claima/denials` is the worklist (five-card rail — Open value, Untriaged, In progress, Recovered MTD, Written off MTD — search over the denial, the claim, the patient and the payer's reason, filters by Payer / Status / Class / Payer reason / Root cause / Route / Amount band / landed dates / Appeal deadline / Assignee, Untriaged first then largest open amount, a checkbox per row with **Triage selection** over a same-reason selection and **Assign to me**, and Assign · Triage/Open on each row); `#/claima/denials/<id>` is the denial page (banner, the five-figure amounts strip, the triage panel — class, root cause, the suggested route with its reason, the override select with every route it cannot take disabled and saying why, Route, Re-route with a reason — the resolution panel, and Evidence / History: the claim's lines with the denied one marked, billed against the expected payer share stamped at assembly, the contract version, the linked encounter, authorisations, referral, eligibility check and documents, the remittance, and the last eight events of the claim's timeline); `#/claima/denials/analytics` is the period screen (denied and recovered by month, payer reasons beside root causes with count, amount, recovery rate, days to resolve and trend, a breakdown by payer / department / service line / doctor, repeat patterns, the prevention feed ranked by denied value naming the feature that prevents each group, Export CSV, and the reconciliation footnote). `worklist-rows.js`, `denial-kpis.js`, `denial-chips.js`, `denial-actions.js` (the five dialogs), `denial-evidence.js`, `triage-panel.js`, `resolution-panel.js`, `denial-history.js`, `denial-charts.js` (perf-charts copied, not imported) and `analytics-tables.js` sit beside the three screens. `data/repositories/denials.js` is taken over from A29's stub with every helper it published kept (`create`, `reverse`, `seedHooks`, `byRemittance`, the catalogue); `data/seed/root-causes.js` is the static list of 17 causes over eight groups, each with its suggested class and route and the owning feature; `data/engines/denial-router.js` is the leaf that decides class, route and deadline; `CONFIG.claima.denials` holds the appeal windows (30 days, NSSF 45, MOPH 60, Bupa 21) and the 7-day warning; `role.canResolveDenial` is the coder's and the CMO's.
Data model calls: the money is five figures that always add up — denied = recovered + lost + writtenOff + transferred + open — where `transferred` is the remainder a partial recovery carried onto a successor denial (`previousDenialId`, `repeatCount + 1`), so the amendment's four-bucket identity holds over a chain and `selfCheck()` asserts both on load (`[denials] self-check pass`). One denial per claim line while it is open: a line refused again on a later remittance is the same record reopened as a repeat (its route ended "Denied again", status back to Triaged), because A29 creates one denial per denied line per posting and a second row would have counted the money twice; a line refused again after its denial was resolved is a new record chained to the old one. Routing needs a triage first and creates the work item where the fix is made — `coding.requestRecode` (and the claim reopened to Draft), `charges.requestCorrection` (an A31 helper: the line held as Disputed with the request on it), `preauth.resubmit` / `renew` / a new draft request, `claims.openForDenial` (an A31 helper — the denial's `rejectedCycle`, so A28's chain resubmits the claim on its next cycle and puts it in the payer's open batch; a claim with no visit is resubmitted as filed), `handoffs.create`, `claims.setStatus(Appealed)` + `followups.log`, `writeoffs.requestFromDenial` — and stores the reference; one active route, a second needs a reason and keeps the first in `routeHistory`. Resolutions are derived: `remittances.afterPostHooks` pays the line back (in full → Recovered, in part → Partially Recovered plus the remainder as a new denial), a hand-off's outcome (Won / Lost / Settled, recorded on the page since Defensio does not exist, and read off a hand-off Pactum marks Recovered), the write-off feature's `resolveWrittenOff` (and `reopenFromWriteoff` when it reverses), and a manual close with a reason, flagged `manual`. The deadline is computed on every read (`deadline.appealBy` stored, `passed` derived): an active route has met it, and one that passes on a denial nobody routed sets Deadline Passed with the money still open — the desk can still route it. A payer's answer to a reconsideration is its own act (`recordPayerAnswer`): overturned puts the claim back with the payer as Acknowledged, because A29 posts a remittance only against an in-flight claim, and the remittance that follows is what recovers the denial; upheld ends the route and hands the denial back triaged.
Architecture calls: `denials.js` reaches the remittance and coding registers by dynamic import (both import files that import it) and feature-detects the write-off register (A33); the router engine reads only the two catalogues and config, which is what lets the repository wrap it. A seeded assembled claim carries ledger ids on its lines when it was assembled before the capture register loaded, so `chargeLineOf` resolves the register line through the ledger row the two share — the assembler's own `sameLine` rule. `worklist()` keeps A29's meaning — the open denials — since the write-off seed reads it that way; `search()` is the whole register and the screen reads that. `denials.js` is over the file cap on purpose: one entity, one file.
Seed: 16 intents over the generated Denied claims (`data/seed/denials.js`, driven through the repository's own writes with the dates the events happened on, beside A29's Bupa CO-197) — a Defensio win, a loss and a settlement whose remainder is the ×2 successor, a manual close by the CMO, a Defensio appeal in progress, a Refresh that went back out on cycle 2 into the open NSSF batch, an auth rework drafted after the fact, a payer reconsideration with its follow-up, one triaged and assigned, two write-off candidates left unrouted for A33's seed (which posts the LATE_FILING one and calls back), a Bankers duplicate six weeks old that `sweep()` moves to Deadline Passed on the first read, a MOPH refusal five days from its sixty-day window (the amber countdown) and three untriaged from this week. A payer with too few Denied claims on a reason lends its largest Denied claim and the claim's `denialReasonCode` is restamped from the payer's code — the register is what says why a claim was denied. No seeded denial sits on a claim assembled from a visit (the two Ready assembled claims are A28's demo), so Recode and Charge correction are reachable live — finalize, submit, post a refusing remittance — and were verified that way (a held Disputed line with its correction request, a recode request on the chart), not on a seeded row.
UI calls: the worklist's Route column is the chip plus the work item's link, and on a denial resolved without a route of its own it names the resolution's record; the appeal countdown reads "met" once a route is active, since the appeal is under way; the amounts strip is five plain cards (nothing on the page to select); the two panels own their nodes and listeners, the shell's freshBody rule one level down; and the reconciliation footnote says how the register and Performance overlap — Performance counts denied claims by date of service, the register denial records by the day they landed, and a routed claim resubmitted on a new cycle leaves the first count and not the second — rather than pretending they are one figure. Open: A29 posts a remittance only against a Submitted or Acknowledged claim, so a line-level denial on a Partially Paid claim is recovered by hand or through Defensio until a reconsideration puts the claim back with the payer; the Claima home is still A24's placeholder, so `nearDeadline()` is published and read by nothing yet.

### 35 — Claima: Home Screen
Replaced the A24 placeholder with the module's landing dashboard under `modules/claima/features/home/`: `#/claima` (and `/home`) is the first nav entry, badge = escalated claims + untriaged denials, with six KPI cards (`home-kpis.js`), four attention panels in a 2 + 2 grid (`home-attention.js`), the trail's Claima half full-width under them, and five quick actions in the panel header (`home-quick-actions.js`) — Manual charge, Create batch, New remittance, Denials queue, and the day-close action, which reads "Close day" for a role carrying `canCloseDay` (amendment 34's flag, feature-detected) and "Today's DTR" for everyone else, both at `#/claima/dtr`. Everything is read from the helpers published in `modules/claima/COORDINATION.md` — `charges.unbilledGrouped`, `coding.worklist`/`counts`, `claims.readyForSubmission`, `batches.readyQueueByPayer`, `lifecycle.silent`/`queueStats`, `denials.worklist`/`counts`/`nearDeadline`/`deadline`, `remittances.search` — and redraws on `ctx.onData` and on a change of demo role. The one addition outside the feature is an `entities` scope on `shared/activity-trail.js`'s `recent`/`activityHtml`, so the panel lists what `#/pactum/activity?module=claima` lists.
Cash posted today is amendment 34's: the card imports `data/repositories/business-days.js` dynamically, reads `dtr.today()` when it is on disk, and until then shows "DTR arriving" with the same three figures read off the ledger and the remittances (a posting made today, a receipt taken today, a remittance captured and not posted) in the sub-line — the only place the screen sums anything itself, and it stops the moment the report exists.
Data model calls: a card's headline is the row count of the screen it opens, the rule Frontis Home already follows, so the four amendment targets that named a filtered slice open the whole list and the sub-line names the slice — Coding backlog counts the charts not yet Coded and opens `#/claima/coding` (the worklist lists the coded ones last; `?sla=breached` is not a parameter the screen reads, `?beyondSla=1` is), Awaiting payer opens `#/claima/followups` (the queue is the silent set, longest silence first; `?slice=escalated` is the screen's own word for the amendment's `?escalated=1`), Open denials opens `#/claima/denials?slice=open` (untriaged first is that list's own order) and Unbilled charges counts the lines under the worklist's visits. Ready to submit is `claims.readyForSubmission()` — the Claims badge's own figure — and its tooltip splits it into the claims waiting in the ready queue and those already in an open batch, both of which the workbench shows; the nearest appeal deadline is the first row `denials.nearDeadline()` hands back when asked for every open denial. Manual charge links to `#/claima/charges`: the worklist reads no `?action=` parameter, and the button is in its header.
UI calls: the chips are imported from the sibling features' chip files (`claim-chips.js`, `lifecycle-chips.js`, `denial-chips.js`, `remittance-chips.js`) rather than copied — one module, one way of naming a claim, the call the lifecycle and denial features already made; the header row is drawn whole so its buttons are flex children; a restricted record's cover and money are withheld on a row the way the portfolio withholds them, and the whole dashboard redraws on a role change; a row's `data-go` yields to any link inside it. `#/claima/dtr` falls back to the module home through the shell until amendment 34 lands.


### 34 — Claima: Daily Transaction Report
Added the DTR under `modules/claima/features/dtr/` and two Claima nav entries: Daily report (badge = today's RED checks not yet fixed or documented) and DTR archive. `#/claima/dtr` is today's report and `/dtr/<date>` another day's — the same screen, live while the day is Open or Reopened and read from the stored version once it is Closed: banner (date, status, DTR number and version count, Close day / Reopen behind their roles with the repository's own refusal in the tooltip, previous/next day), the checks strip (five cards, OK or RED, each opening its detail: figures, the offending transactions with links and the action that answers each — Open a session and attach, Close session — and Document exception behind its role), the five sections as collapsible tables where every row and every "of which prior-day" sub-row drills to the transactions behind it in the shared drawer, the carried balances (opening + movement = closing per category) under each, and the sessions panel with Open session, Receipts and Close session. `#/claima/dtr/archive` is the archive (date, DTR no., status, versions, closed by/at, exceptions, reopen history, net patient cash) over the month-to-date roll-up, labelled informational; `/archive/<date>` the frozen snapshot rendered from the stored JSON alone with a v1/v2 switcher, Export PDF (print) and Export Excel (a CSV built from the snapshot). Files: `dtr-today.html`/`.js`, `dtr-checks.js`, `dtr-sections.js`, `dtr-drill.js`, `dtr-sessions.js`, `session-close-dialog.js`, `day-actions.js`, `dtr-archive.html`/`.js`, `dtr-snapshot.js`, `dtr-chips.js`. Two entities Claima owns: `businessDays` (`data/repositories/business-days.js` + `data/seed/business-days.js`) and `cashSessions` (`cash-sessions.js` + seed); `CONFIG.claima.dtr` holds the $50 countersign threshold and the role flag names; `shared/roles.js` gained `canCloseDay` (coder, CMO), `canReopenDay` (CMO), `canDocumentException` (coder, CMO) and `canCountersign` (coder, CMO). Published: `dtr.today()` (the compact shape the module home reads), `compute(date)`, `report(date, version?)`, `drill(drill)`, `current()`, `closeDay`, `reopenDay`, `documentException`, `archive()`, `mtd()`, and on sessions `open(cashier, date)`, `attach(ref, cashier, date)`, `attachOrOpen(txId)`, `close(id, { declared, reason, countersignBy })`, `systemTotals`, `varianceOf`, `unattached(date)` — all listed in `modules/claima/COORDINATION.md`.
Architecture calls: `data/engines/dtr-engine.js` is a leaf — it imports nothing from `data/` — and the repository builds the one world it reads (every register's rows, the sessions, the prior closed snapshot, the exceptions, and name lookups that go through `patients.view()` so a restricted record reads masked), which is what lets `business-days.js` import it without a cycle; the engine's section builders and its `select(world, drill)` share one table of selectors, so a row's count is the length of the list its drill opens by construction. The two repositories cannot import each other, so `cash-sessions.js` exposes `openGuards[]` (the day repository pushes "a closed day takes no session") and the ledger hook lives in `business-days.js` and calls `cashSessions.attach`. `ledger.afterAppendHooks` is harp-31's (A22) — the one Frontis touch the amendment allowed was not needed, since that session was adding the array the same hour and agreed the name. The register seeds behind the claim repository's peers (nullifications, denials and write-offs seed there too) and again after a reset; the hook tags a prior-day row only once the register holds a closed day, because the peers' seeds append to the ledger while mine is still empty and those rows are history being written, not late postings.
Data model calls: a row lands on the day it was written on — `priorDay.postedOn` when the ledger hook stamped one (the row's own date was already closed, or was before the register on a day it never opened), else its own timestamp — and a reversal counts negative on the day it was written and never reopens the day the original was counted on, which is what keeps a frozen snapshot true after a correction; a posting after today's close opens tomorrow and lands there. Charges are keyed on capture time with prior-day read off a date of service before the business day, since the register carries no tag. Every section carries `rows` (with `inTotal` saying whether the row counts into the section's headline — the by-source partition and the Late row are shown for reading) and `categories`, where opening is the prior closed snapshot's closing and the first report starts at zero. The payer posting equation is the A29 cash identity, per remittance posted on the day — paid + unapplied held (+ paid on rows still to post) = the payment total — with adjustments and denials shown beside it, because a remittance total is a cheque and a CO code is a reason; the amendment's `paid + adj + denied + unapplied` is not an identity the posting engine holds. Write-off reconciliation reads three registers: the section's payer-side rows, `writeoffs.postedTotals({ from, to })`, and what each denial acknowledges of the write-off posted against it. The opening/movement/closing check is structural (the arithmetic holds by construction) and its one live failure is continuity — the previous business day still open, which is what stops today closing before yesterday. A session's system totals are computed from its attached rows until close and frozen onto the row then; `systemTotals` in the engine is the one definition both the dialog and the report read. Closing a day needs every session closed *and* zero undocumented REDs — an All sessions closed exception cannot stand in for closing them. The world reads `writeoffs.postedTotals` with a `{ from, to }` range, which its `posted()` already accepted.
UI calls: the checks strip is the system's `.metric-rail` with the value as a word (`text`), critical for RED, warning once documented ("RED · noted"), and the selected card is `aria-pressed` with its detail drawn under the strip — the KPI-card rule, with the detail in place of a filter. The sections are `<details>` with the panel header as `<summary>`, the billing breakdown's collapsible, and which are open survives a redraw. The countersign block in the session-close dialog is a select over the roles that hold the flag, minus the cashier and the closer — the amendment's "second user via role switcher for the demo" — wrapped in a plain `div` because `.toolbar` sets its own display and outranks `hidden`. The session-close and day-close dialogs ask the repository's blocker before writing and show its sentence, so a disabled button and a refused dialog say the same thing. The CSV download helper is copied from the nullification log rather than imported across the module boundary. A money cell carries its sign and a dash for nothing; the system tints a badge, not a number.
Seed: five business days ending today, computed by the engine over the registers as they stand rather than hand-written — D−4 closed v1, D−3 closed v1, reopened the next morning by the CMO ("a remittance file received after close was posted onto it") and re-closed v2, D−2 closed v1, yesterday closed v1 over one documented cashIntegrity exception, today open — and three sessions today: Rima Nassar's handover session closed $78.75 short with the CMO's countersignature (the cash is in the night safe), Maya Zgheib's open with the morning's two receipts, Nadine Rizk's pharmacy counter open with nothing yet. The two receipts behind the exception and the prior-day row are pushed into the ledger by the sessions seed the way `data/seed/ledger.js` writes its own — balance payments against the two untouched accounts owing the least, allocated by the account engine, receipts numbered off the register, the trail written at the times they happened — one taken yesterday afternoon before Rima had a session (yesterday's RED, documented) and one taken by the night desk after yesterday's close and keyed in this morning (today's prior-day row, on Rima's closed session). Tarek Solh's $264 card settlement, which the payments seed takes with no session, is today's deliberate RED; the demo fixes it from the check's own row.
Verified live: the RED fixed by Open a session and attach, every session closed through the dialog (one $77.50 short with a reason and a countersignature), the day closed as DTR-2026-0253-v1 by the CMO, a payment recorded afterwards tagged `priorDay { postedOn: tomorrow }` and reported on the 11th unattached, the day reopened with a reason and re-closed as v2, the archive and both versions, the CSV, and Reset demo data re-seeding the five days and three sessions. Every module in the tree imported in the browser (403 files, 0 failures). The `[performance] self-check FAIL` on load is the A29 conflict already under COORDINATION's Conflicts, not this amendment's.

### 22 — Frontis: Patient Accounts (part B — settlement, adjustments & refunds, SOA, flags)
Completed the account on top of amendment 21's ledger, applied to the tree as it stands after Claima A29–A35. `data/engines/settlement-engine.js` is the reconciliation — `reconcile(encounterNo)` → `{ estimatedShare, actualShare, paid, difference, outcome (Settled | Unsettled | Excess | Pending), drivers[], mode, runsContinuously, consent, resolved, stamp }` — pure over the ledger, the register, the estimates and the acknowledgments; the arithmetic itself is `data/engines/account-engine.js`'s new `reconcileRows(rows, encounterNo, quoted)`, over rows handed in, so the ledger seed can settle a visit while it is still building. `data/repositories/accounts.js` writes the answer: `settle` (the stamp `encounter.settlement = { status, at, by, manual, txId, consent, amount }` through the new `encounters.setSettlement`, plus a `Settlement` ledger row when anything was owed), `syncSettlement` (a settled visit that no longer reconciles reverts, audited; an up-front visit that reconciles is marked on its own), `onClosed` (deposits held on the visit applied oldest first, then reconciled — Settled at zero, else Unsettled with the residual), `holdAsCredit` (the patient's consent recorded on the stamp), `adjust` and `refund` (the two corrections, with `ADJUSTMENT_REASONS` / `REFUND_REASONS` from config), `reclassifyCharges`, and the flags — `FLAGS`, `computeFlags`, `refreshFlags`, `refreshAllFlags`, `flagged(kind)`. Statements: `data/engines/statement-engine.js` (`buildStatement`, `rowsInScope`, `reconcileBlocks`, `scopeLabel`) and `data/repositories/soa.js` (`create`, `preview`, `get`, `byPatient`, `nextNo`, `markPrinted`) over the `soa` table, seeded on first read from `data/seed/soa.js`. Routes: `#/frontis/accounts/<mrn>/soa/new` (the generator: scope, detail, language, live preview, the reconciliation check) and `#/frontis/accounts/soa/<no>` (the frozen statement, Reprint, Generate new); no new nav entry. `modules/frontis/features/accounts/` gained `settlement-panel.js` (one renderer and one action handler for the account page's By encounter expander and the encounter page's Financial tab), `settlement-result.js` (the card the discharge dialog shows), `adjustment-form.js` (adjust, refund with the voucher, hold as credit), `account-flags.js` (labels and chips), `soa-doc.js` (the document, EN and AR), `soa-generate.html/.js` and `soa-view.html/.js`. The account page's Generate SOA is live, By encounter carries the Settlement column, Receipts & documents lists the statements, the flags wear labels on the list and the header, the board shows ✓ Settled beside the status with an "Unsettled completed" entry in the Status filter, the encounter's Financial tab carries the panel, the discharge dialog shows the reconciliation before it closes, and Re-classify is refused on a settled visit with the reason.
Wiring, and the one-way arrows it keeps: `data/repositories/ledger.js` gained `afterAppendHooks[]`, called with every row the moment it is appended (batch or not, a throwing hook logged and skipped) — the array Claima A34 asked for by name; `data/repositories/encounters.js` gained `afterCloseHooks[]` (fired by `discharge` and by the outpatient auto-complete) and `afterReclassifyHooks[]` (fired with the encounter and the classification it replaced), plus `setSettlement`, `isSettled`, `reclassifyBlocked` and `SETTLEMENT_STATUSES`. `accounts.js` subscribes to all three, so a row from anywhere — the desk, a Claima remittance's PortionShift, a Claima write-off's Adjustment — reconciles the visit it landed on and recomputes the account's flags without either Claima file calling anything; A29's dynamic `settlementEngine.reconcile(no)` fires and is harmless, being pure. Verified live: a manual remittance with a PR-2 shift posted on ENC-2026-000401 moved its actual share 8.48 → 13.48 and re-derived the outcome; WO-2026-000053 posted on ENC-2026-000411 settled the visit and cleared its flags; a write-off, a prefilled residual payment and Settle now on the account page; the discharge of ENC-2026-000420 showing "Residual $60.80" inline; SOA generated in Arabic and reprinted from the snapshot; Reset demo data re-seeds every state. Every module in the tree imported in the browser (403 files, 0 failures).
Data model calls: the actual share is Σ patient share of the live Charge rows + PortionShift rows − Adjustment rows (a write-off's or the desk's), and paid is payments less refunds on the visit, deposits applied to it, and money from elsewhere on the account allocated to its charges — a payment on the visit counts once. Estimates never settle: the acknowledged estimate is the strip's first column and the source of the drivers (per charge code, overage first, "not in estimate" tagged) and never the figure the difference is taken against. An excess the patient agrees to leave on the account is resolved without money moving — the outcome stays Excess, the consent on the stamp says nobody chases it, and the visit can settle. Only a Settled stamp is written by the triggers; the flags and the board read the reconciliation, so nothing goes stale. A reclassification reverses and reposts only the lines nothing has answered yet — what was paid was paid for that line — as a pair carrying the reason both ways and `detail.reclassification { from, to, reason, replaces }`; a settled visit is not touched. The account engine reads a `Refund` as paid money leaving (`paid` falls, and the payment's credit falls through `allocatedFrom`) and a `Settlement` row as a marker; `paymentCredit(rows, txId)` is what a refund may give back. `OutstandingOverThreshold` uses the list's own `>` rather than the amendment's `≥`, so the flag and the "Over $500" card count the same accounts. `UnsettledCompleted` (ruled after review) needs a residual of at least `CONFIG.accounts.unsettledResidualFloor` ($25) as well as the days — below that it is a small balance to write off, not a collection to chase — and `CONFIG.clearance.paymentMode` is keyed on the encounter types alone (the dead `DayCase` key is gone; IP stays Upfront Settlement).
Architecture calls: amendment 22 names `settlement-engine.js` as pure and the flags as "computed on every ledger write", and the layering rule says a repository imports only a leaf engine — this engine reads four repositories, so it is imported by `accounts.js` the way `clearance.js` imports the clearance engine, and none of what it reads imports `accounts.js` back. The arithmetic lives in `account-engine.js` because the ledger seed has to reconcile before a single ledger row exists and an engine that read the ledger would close a cycle through the seed. A seed that pushes rows straight into the ledger (A25's captured lines, A33's write-off adjustments) fires no append hook, so `refreshFlags` reconciles the account's visits on every read of the account — a change is one commit — which is also what catches a Claima seed that lands after the load sweep. `settlement-result.js` is a file of its own because the discharge dialog lives in the encounters feature and the panel's card is the accounts feature's; the two features import each other's markup the way encounter-view already imports the clearance tab.
UI calls: the strip is the design system's `.metric-rail--3` inside the panel, the outcome a tinted badge, the drivers a table with the overage rows titled; the Refund and Write-off buttons carry the role gate's sentence in their tooltip, the same one the repository refuses with; the statement's Arabic is the same document with `dir="rtl"` on the document alone and the labels from a dictionary in `soa-doc.js`, numbers Latin; a negative balance reads "in credit" on the header and the card rather than as a minus; and the Home panel's flag chips are the accounts feature's labelled ones. The seed's settlement states are kept off restricted records, since the default demo roles read those masked.
Seed: `data/seed/ledger.js` gained the part-B states, each found on the registers by rule — an excess held as credit with the patient's consent (a clinic visit settled up front against a quotation $40 over), an excess refunded with a voucher ($25 back, RCP issued), a duplicate posting adjusted off, a visit opened as Self-Pay and reposted under the cover when the card turned up (the self-pay lines struck through, their reversals, the repost tagged), and every up-front or closed visit that reconciles stamped Settled with its Settlement row — plus the residuals part A already left, which the flags now name. `data/seed/soa.js` freezes two statements: a detailed English one over the account owing most and an Arabic summary of one admission. The first SOA generated live is SOA-2026-000091.
Not applied: `settlementEngine.reconcile` needs no call from Claima's `writeoffs.js` or `posting-engine.js`, and neither file was touched — the note is under Requests in `modules/claima/COORDINATION.md`. Pre-existing and not this amendment's: `[performance] self-check FAIL` on load (A29's, recorded under Conflicts).

### 36 — Defensio: Module Bootstrap + F1 Denial Intake, Triage & Routing
Added module `defensio` (Denial Management), registered between Claima and Template, with a placeholder home at `#/defensio` (four live cards, "features arriving") and the F1 feature under `modules/defensio/features/denials/`: `#/defensio/denials` is the worklist (five-card rail — Open value, Untriaged, Recovered MTD, Written off MTD, Separated out MTD — search, Payer / Status / Separation / Payer reason / Tier / Class / Root cause / Route / Amount band / landed dates / Appeal deadline / Assignee filters, untriaged first then largest open amount, bulk triage over a same-reason selection, Assign to me, and a row that opens the page) and `#/defensio/denials/<id>` (+ `/history`) the denial page: banner with separation, tier and class chips, a six-figure amounts strip (Reclassified beside Recovered, Written off and Lost), the one-pass triage panel, the resolution panel with the registers' own evidence, and the Evidence / History tabs. `modules/defensio/COORDINATION.md` is the parallel-session contract from A37 on and declares the ownership: `denials` (taken over from Claima — the repository, the seed, the router and the root-cause list stay in `data/`, a repository never moves into a module) and the two stubs `appealCases` (`data/repositories/appeal-cases.js`, A38 takes it) and `tpaFeeAccruals` (`data/repositories/tpa-fee-accruals.js`, A42 takes it), each a table with `create` and reads under a TODO header. The design system names no module colour, so Defensio is its icon (`shield`) and its group, like the other three.
The takeover (the only non-additive edits, both logged in both COORDINATION files): Claima's A31 worklist and page are redirect stubs — "Denial management has moved to Defensio", the link, and the shell moved on after a moment, with the query and the deeper path carried across (`/denials/analytics` is still handed to Claima's analytics screen, which stays); the A35 home's Open denials card, Untriaged denials panel and Denials queue action link to `#/defensio/denials` (the data source is the same repository); the A30 timeline's Denied event links to `#/defensio/denials/<id>`; and the Claima manifest's A31 comment reads revoked, its nav entry and route kept since the badge is live and the route redirects. `createDenialRecord` in the amendment is `denials.create`, which the remittance engine calls from the same path it always did — nothing to re-export. Every other link into `#/claima/denials` (the activity trail, the DTR drills, the nullification route card, the write-off chips) resolves through the stub and is listed under Requests for the consolidation pass.
Data model calls: the one-pass triage adds a category (Clinical / Technical / Administrative), a tier (Hard / Soft / Underpayment) and a separation (True denial / Contractual adjustment / TPA fee) to the class and root cause; each root cause in `data/seed/root-causes.js` carries the category and tier it usually reads as, so picking a cause fills the form, and `TIER_CLASS = { Hard: Appealable, Soft: Corrigible, Underpayment: Appealable }` moves the class when the tier is moved by hand (the cause's own class wins when re-picked). A separation other than a true denial is `reclassify()`: what is open moves to `amounts.reclassified`, the denial reads Reclassified (resolved — it was never a denial), and the record the separation calls for is written — a contractual adjustment puts a `Reconciliation note` on the claim's own audit trail (ref `RN-<denial id>`, kept on the denial as `reclassification`) and a TPA fee creates an accrual (`TPA-0001`); the note is required, since it is the reconciliation. The identity gains the bucket: denied = recovered + lost + writtenOff + reclassified + transferred + open, asserted by `selfCheck()` and by the new `data/engines/denial-resolution.js` — which derives each denial's resolution from the remittance postings, the write-off postings, the repeats, the appeal case, the accrual or the note, reconciles it against the stored figures (the page's "Registers say" line), and `console.assert`s the ledger identity on load. `Appeal` joins the routes (RC-15's suggestion now; `DefensioHandoff` stays as "Contract hand-off", Pactum's underpayment register) and creates an `appealCases` row chained both ways; `resolveFromAppeal` is published for A38. Appeal windows read the Pactum payer record's `appealWindowDays` first through a resolver the repository pushes onto the router (which stays a leaf), then `CONFIG.claima.denials`, then 30 days. Two payer codes join the catalogue for the separations, `CO-45` and `OA-23`, and `SEPARATION_BY_CODE` pre-fills the form from them.
Seed: the A31 intents re-tagged for the one-pass triage, DN-0011 read as corrigible (auth rework is a correction), DN-0012 routed to an appeal case (AC-0001), DN-0014 routed to a write-off request raised once the write-off register has seeded (WO-2026-000058, so the first live request is 000059), two denials that were never denials — Bankers' CO-45 reconciled as contractual, Arope's OA-23 accrued as GlobeMed's fee — and a recode route on the oncology emergency visit's claim (ENC-2026-000401, the one claim with a coded chart behind it): the seed submits and denies that claim on its own trail, the route raises RCR-0002 on the chart and reopens the claim as a draft with `rejectedCycle`, and marking the chart coded is what sends it back out. Four untriaged, DN-0015 five days from its deadline. Intents that name a visit build once `claims.peersReady` settles (the assembly claims seed behind it), always after the generated ones, so the ids are the same whatever order the registers load in; the Bupa denial the remittance seed posts is DN-0001 on a fresh load and last after a reset fired from a Claima screen, since that seed runs from whichever register is read first.
Two things found in verification, outside the amendment's four edits and recorded in both COORDINATION files: `data/store.js` gained `batch(fn)` — notifications held until fn returns, then each reason committed once — because the seeds write through helpers that commit, and a badge redrawn on one of those commits fired the write-off seed against a half-built denial register (stand-ins on a reset from the Claima home); the denials seed runs in one. And `data/repositories/claims.js` `ensureAssemblySeed` reads a Primary as proof the seed ran, not any claim with a kind: the remittance seed activates a Secondary as it posts, and on a load where it ran first the eleven assembly claims never appeared (a race on every first load, lost on a Defensio route about half the time). Verified live: fresh loads on Defensio and Claima routes and Reset demo data from both — 20 denials, 13 assembly claims, WO-047 on the timely-filing denial, both self-checks pass; triage & route (Refresh, the claim reopened and resubmitted onto cycle 2 by A28's subscription), a TPA reclassification (TPA-0002, the note refused empty), the Claima stub redirect with its query, the timeline link; every module in the tree imported in the browser (418 files, 0 failures).
UI calls: the separation is a `.segmented` control, the system's primitive for one of a few, and the true-denial block is a plain wrapper carrying `hidden`; the one Save reads what it will do — Triage & route, Save triage, Update triage, Update triage & re-route (the reason asked in a dialog), Reclassify as contractual adjustment, Accrue as TPA fee; Claima's chip helpers and event shapes are copied into `denial-chips.js` and `denial-evidence.js` rather than imported across the module boundary; the worklist's Triage column is the tier over the class with the category in the tooltip, and a resolved row's Route column names the record that resolved it. `CONFIG.claima.denials` stays where it is and no role flag was added — `canResolveDenial` gates the manual close as before.

### 39 — Defensio: F4 Appeal Tracking & Resolution
Added the appeal's second half under `modules/defensio/features/appeal-tracking/` and the fourth Defensio nav entry, badge = payers past their response window plus conceded money aging unpaid. `#/defensio/appeal-tracking` is the worklist (five-card rail — In flight, Overdue, Decided MTD by outcome, Recovered MTD, Awaiting recovery — search over case, denial, claim and payer reference, Payer / Status / Outcome / Recovery state / submitted-date filters, response deadline soonest first with overdue painted, a same-payer bulk follow-up over the selection, Follow up · Decision on each row); `#/defensio/appeal-tracking/<id>` is the feature's own host for the **Tracking & outcome** tab — the response clock, the follow-up log, the Under Review setter, the decision (outcome, date, payer reference, rationale from the shared payer-code list, document, and an allocation editor with the sum checked live), a disposition on each lost share (escalate, write-off loop, accept with reason — the last gated on `canResolveDenial`), the recovery panel (the expected records, their states and ages, Chase, a shortfall's answer) and Close, gated on the ledger. The same tab is registered for amendment 38's case page through `modules/defensio/tabs.js`, the module's tab registry (`registerTab` / `tabsFor`, one line per session), so `#/defensio/appeals/<id>/tracking` draws it too. `appeal-tracking.html`/`.js`, `tracking-page.js`, `tracking-tab.js`, `tracking-dialogs.js`, `tracking-kpis.js`, `tracking-rows.js`, `tracking-chips.js` (the claim vocabulary imported from the denial feature's chips inside the module, not copied). Two entities Defensio owns: `expectedRecoveries` (`data/repositories/expected-recoveries.js`) and `escalationRequests` (`escalation-requests.js`, the fallback for a level-2 creator that is not on disk). `data/repositories/appeal-tracking.js` is the F4 half of the appeal case — every status after Submitted (Under Review, Won, Partially Won, Lost, Settled, Closed) and the fields `tracking`, `outcome`, `recoveryState`, `decidedAt`, `closedAt`, written onto amendment 38's rows additively — and publishes `getAppealOutcome`, `getLostAppeals`, `getF4HomeFlags` and `appealDetailOf`. `CONFIG.defensio.appealTracking` holds the response window (30 days, MOPH 45, a Pactum payer record's `responseWindowDays` first), the 5-day warning, the 45-day recovery aging mark and the accepting role.
Architecture calls: the amendment puts the outcome logic in the engine, but the layering rule says a repository imports only a leaf engine and this one has to read six registers — so `data/engines/appeal-resolution.js` is the leaf (the response clock, the outcome validation, the ledger, the close gate, the flags, over rows handed in) and `appeal-tracking.js` resolves the world and does the writing, the clearance engine's split; the repository is over the file cap on purpose, one entity's lifecycle in one file. The remittance hook is registered once, in `expected-recoveries.js` (the entity the posting moves), and tells the tracking repository which cases moved through `afterMatchHooks`. Defensio never posts cash: an expected recovery is Recovered or Shortfall only when Claima's `remittances.afterPostHooks` reports a posting on the appealed claim's lines, and a reversal puts it back to waiting. The escalation path is amendment 38's `createNextLevel`, feature-detected at the call — it was on disk, so the seeded level-2 case AC-0005 came through it and the request table stays empty.
Data model calls: the ledger every decided case holds — disputed = recovered + writtenOff + accepted + escalated + openShortfall (+ pendingDisposition) — is asserted on load and is the closure gate; a lost share nobody has dispositioned is its own bucket rather than a shortfall in disguise. On the denial, the conceded share reads as recovered the moment the payer concedes it (the cash state is the expected recovery's, and the resolution panel's chip now says which — awaiting, recovered on RMT-…, shortfall, or lost with its disposition), the lost share as lost once it has a disposition, and an escalated share stays open because the level-2 case is raised on an open denial and disputes it again; `denials.applyAppealOutcome` settles Recovered / Lost / Partially Recovered off the denial's own figures with no successor, so the remittance that pays the conceded share later finds nothing open and amendment 36's `resolveFromRemittance` never chains a successor for a share still in dispute — which is why the write-off loop raises a claim-residual request rather than a denial-sourced one (a denial-sourced request posts against `amounts.open`, which the appeal has already settled). A conceded amount puts the claim back with the payer as Acknowledged, since amendment 29 posts a remittance only against an in-flight claim, and the claim's trail gains an `Appeal outcome` line either way. Two clocks are named apart everywhere: the filing deadline is the denial's (the appeal window), the response deadline the payer's.
UI calls: the allocation editor fills the lost share as the remainder when a conceded share is typed and the outcome preset fills both for Won and Lost, so the sum badge is green before anything is corrected by hand; a disposition may be chosen on the form or later from the tab, and the accept option is disabled with the role in its label for a role that cannot; the chase button on an awaiting recovery records only that somebody asked, since the days waiting are read off the decision date; a restricted record's case is withheld outright on the host page and its cover and money on the row, the denial worklist's line. The worklist's overdue highlight is the clock's critical badge and the row's tooltip — the design system ships no row tint and a module writes no CSS.
Seed: four cases written as intents and driven through the repositories' own writes — a denial created on the largest free Denied generated claim of the first payer with one, triaged, routed to an appeal (which opens the case), submitted with the four amendment-38 fields flat and nested — then Won awaiting its remittance twenty days (two follow-ups, the next due in two days, and the payer's review reference); Partially Won with half conceded and paid through Claima's own manual remittance entry and `post()` (RMT-2026-000077, matched by the hook) and half in the write-off loop (WO-2026-000059, Pending Approval); Lost and escalated by the CMO to AC-0005 at level 2 (the RCA trigger amendment 37 reads); and Settled at sixty per cent, paid (RMT-2026-000078), accepted and Closed. Amendment 38's Submitted case is stamped with a response deadline twelve days out and left as it is. A partial payment past the payer's tolerance raises amendment 29's Pactum hand-off as its rules say; the two remittances sit on the workbench like any other. Verified live: Under review → a partial decision → accept → a manual remittance posted in Claima matching the recovery → Close, with `[defensio] appeal ledger`, `[defensio] resolution` and `[writeoffs]` self-checks passing before, after and after Reset demo data; every module in the tree imported in the browser (469 files, 0 failures). Ruled after review: a denial's resolution is the outcome-level truth and cash truth lives only on the expected recoveries — a downstream Recovery Rate (A41) reads expected recoveries in state Recovered, never the denial's `recovered`; a shortfall answered on the case never restates the denial, the resolution panel's line is the visibility; the appeal-loop write-off requests carry `origin: 'appealLost'`, `denialIds[]` and `appealCaseId` so written-off is summed from both sources; and the one real defect — amendment 29's underpayment hand-off raised on an appeal's partial payment, the same money in two dispute queues — is the consolidation pass's (suppress the hand-off when the claim carries an open expected recovery), the two seeded hand-offs left as they are and no more seeded.

### 38 — Defensio: F3 Appeal Preparation & Submission
Took over the `appealCases` stub (`data/repositories/appeal-cases.js` — over the cap on purpose, one entity, one file — seeded from `data/seed/appeal-cases.js` once `denials.seedReady` settles and again after a reset; the A36 `create` contract kept, the case now opening as a Draft) and added the feature under `modules/defensio/features/appeals/` with the nav entry **Appeals** (badge = reviews waiting on the signed-in role plus cases inside the warning window or past it). `#/defensio/appeals` is the workbench (four-card rail, Active | Submitted | All, search over case, denial, claim and patient, Payer / Status / Level / Tier / Deadline / Preparer / date filters, cases to file deadline-ascending with the warning window amber and past red, submitted rows read-only); `#/defensio/appeals/<id>` the case page — banner, Draft → In review → Approved → Submitted stepper, the case summary and the review rounds in the bounded pane, and the tabs Case (what the registers say: the denial, its triage, the claim's lines, the contract version, the linked records, the timeline excerpt — the denials feature's own evidence panel imported, and the root-cause conclusion when amendment 37's register holds one), Grounds & citations (a primary ground, secondary grounds, and the citation picker over the stamped contract version), Evidence bundle (what the registers suggest attached by reference in one click, uploads, include toggles, up/down order, and the grounds ↔ evidence warning map), Letter (template, generate, edit in a contenteditable panel, every generation and every hand edit a version flagged as such, locked at approval), History, and then whatever another Defensio feature registered for `appeal-case` in `modules/defensio/tabs.js` (amendment 39's Tracking & outcome, feature-detected); `#/defensio/appeals/<id>/package` (+ `/<ref>`) the printable package — cover sheet, locked letter, enclosures index — rendered from the frozen copy alone. `appeal-chips.js`, `appeal-kpis.js`, `appeal-rows.js`, `appeal-actions.js` (send for review, approve / return, package, submit, withdraw, level 2), `appeal-history.js`, `citation-picker.js` and the four tab files sit beside the three screens. Static: `data/seed/appeal-grounds.js` (nine grounds, the bundle types, the evidence each ground expects) and `data/seed/letter-templates.js` (two English templates, an Arabic placeholder, the merge fields and `merge()`); `CONFIG.defensio.appeals`; `canReviewAppeal` (coder, CMO), `canApproveSeniorAppeal` (CMO), `canOverrideAppealDeadline` (CMO) on `shared/roles.js`.
`data/engines/citation-renderer.js` is the only place a contract term becomes a sentence: given a pointer into the version stamped on the claim — the term, a methodology row, a coverage row on a plan, a pre-auth, referral or documentation rule, a billing rule — it reads `contracts.get(contractId)` by that id and nothing else (never `currentOf` or `contractForService`; `assertStamped` guards it), lists every citable row for the picker (`browse`) and renders "Contract CT-2026-002 v1, Coverage CV-001: …; in force on the date of service 15 Apr 2026" (`render`). The repository stores the rendered text the screen (or the seed) hands it, so a citation is what was read when it was cited and does not move with the agreement — which is also what keeps the engine out of the repository: it reads contracts, and a repository imports only a leaf.
Data model calls: the preparer is `createdBy` — whoever routed the denial — and never signs, whatever tier they hold; the tier is read off the disputed amount at the moment the case goes to review (`seniorReviewAbove` is $200, not the amendment's $5,000 — the largest denial on file is $614, so at $5,000 the senior path could never be reached; the ministry appeal crosses it and the rest do not). Approval locks the newest letter version and the package is frozen once per approval (`PKG-<case>-v<n>`, a second call hands the standing one back); a return keeps the round and reopens editing. Submission writes the four flat fields A39 reads (`submittedAt`, `method`, `reference`, `packageRef`) and the nested `submission` beside them, is late past the denial's own deadline (`filingDeadline` = the appeal-by date the route stamped) and then needs the CMO and a reason kept on the row, and talks to three registers through their published writers: `denials.markAppealSubmitted` (an A38 helper — a trail line, the route stays), `claims.setStatus(…, 'Appealed')` when the claim is Denied (the timeline's own Appealed event), and `followups.log` on the claim (the FollowUp event, since the lifecycle engine derives events from the trail and the follow-up register and no other writer reaches it). Withdrawal is before submission only and calls `denials.releaseAppealRoute` (A38) — the Appeal route ends, the denial goes back to the worklist as Triaged for a second look; a level-2 case (`createNextLevel`, also the door A39 calls on a lost share dispositioned escalate) copies the citations and the included bundle, pre-loads the parent's package as evidence, files `level2WindowDays` from the decision and re-points the denial's route at itself (`denials.repointAppealRoute`). A39's outcome is read through `outcomeHooks[]`, then `appeal-tracking.js` by dynamic import, then the row — and `denials.resolveFromAppeal` is theirs to call when the chain ends.
Hooks in A36's feature, the amendment's two: the denial page's banner wears the case chip (`getAppealCaseForDenial`) after A37's RCA chip, and the worklist gained the Has appeal filter (`?appeal=yes|no`, a post-filter beside A37's RCA one). One token fixed in `denials.js` `progressOf` and recorded under Conflicts: the Appeal branch read the stub literal `'Open'` as proof the case had moved, so a Draft would have painted its denial In Progress on creation.
UI calls: the stepper is the design system's, the stage after Submitted drawn as a current step named by A39's status; the Case panel's `.dl--narrow` and the review rounds' `.journey` are the denial page's; the citation picker is a `modal--lg` of `<details>` per section, in-force badges and a Cite button per row, with the argument asked in the same dialog once a row is picked; the bundle's include toggle is a bare checkbox in the table cell (the system ships none) and the secondary grounds are checkboxes in a `.rule-child-row`, the merge screen's call; the letter editor is a `contenteditable` div inside a `.panel` — no editor in the system, no CSS in a module — and the version switcher a `.segmented`; the package prints alone through `data-print="hide"` on everything else; the workbench's Ground column shows the primary and counts the secondary in a badge with the rest in the tooltip. A restricted record's case is withheld outright on the page and its patient, payer and money on the row, the denial worklist's line.
Seed: four cases on A36's appealable denials, found by rule (payer, code, state — a reset from a Claima screen shifts every denial id by one). AC-0001 (the ministry's protocol appeal the denials seed opened) upgraded in place to In review at the senior tier — grounds, three citations, seven enclosures including the protocol extract and the physician's statement as uploads, the medical-necessity letter generated then edited by hand, sent by Tarek Solh, so the CMO signs; a Draft on the ministry's CO-96 refusal, triaged and routed by this seed (the one denial inside the warning window — A36's untriaged-near-deadline row became the deadline-critical draft, the honest reading of a denial four days from its window), one citation, no letter yet; Approved to submit on the Army fund's documentation refusal (full bundle with two uploads, contract letter v1 locked by Tarek); Submitted by Portal three days ago on the Allianz remainder (DN-0008, for A39 to track). The last three were prepared by Hala Mansour, a denial analyst who never signs in, so both demo reviewers have cases to sign; A36's CO-197 pair stays untriaged for the bulk-triage demo. Verified live: the coder refused on the senior case, the CMO's approval locking v2, the package, a submission by email moving the claim Denied → Appealed with the follow-up on the timeline and the line on the denial's trail, a withdrawal putting DN-0015 back as Triaged with its route ended, `canEscalate` reading A39's Lost outcome (AC-0004 → AC-0005 already raised through `createNextLevel` from their seed), Reset demo data re-seeding all four, and every Defensio route loading with no console error.

### 37 — Defensio: F2 Root Cause & Accountability
Added the root-cause feature under `modules/defensio/features/rca/` and two Defensio nav entries: Root cause (badge = open cases past their target) and Accountability (badge = open cases, for the authorised roles alone). `#/defensio/rca` is the worklist (five-card rail — Open, Overdue, In analysis, Awaiting close, Individual — search, Status / Trigger / Nature / Analyst / Payer / opened-date filters, paging, overdue rows first, and New RCA case: a manual case over any set of denials picked from a searchable table); `#/defensio/rca/<id>` (+ a tab id) is the case page — banner, gated Conclude and Close carrying the register's own sentences in their tooltips, a five-card totals rail, and Evidence (the origin trail), Analysis (problem, repeatable why/because rows, confirmed cause off the one root-cause list, nature radios, configuration-gap toggle with its target and note), Causer (enabled only on an individual finding: person off the staff list, role in failure, the trail drawn with a checkbox per entry and at least one ticked, the person's prior cases underneath), Corrective actions (raise, edit, done, verify with a note, reopen, attach) and History (the case, its actions and — for the authorised roles — its accountability case, one trail). `#/defensio/accountability` is the register behind `CONFIG.defensio.rca.accountabilityRoles` (coder, CMO; everyone else gets the permission banner) and `#/defensio/accountability/<id>` the case: response → decision → appeal as three numbered panels, the deduction tracking as the design system's stepper (Recommended → Sent to HR → Outcome captured), the evidence the person was named on, and the history. Three entities Defensio owns, each seeded on first read behind `denials.seedReady` and re-seeded after a reset: `rcaCases` (`data/repositories/rca-cases.js`, over the cap on purpose), `correctiveActions`, `accountabilityCases`; `data/engines/rca-triggers.js` is the leaf that decides what opens a case; `data/seed/staff.js` is a static reference list like `reference.js` (the five demo roles keep their role ids, the coders theirs, and nine people who never sign in — clerks, a registrar, a pre-authorisation coordinator, a supervisor, the director, HR); `CONFIG.defensio.rca` holds the 14-day target, the $500 amount trigger, 3-in-90-days, the 7-day response window and the authorised roles. `data/repositories/denials.js` gained an A37 section, `retagRootCause`. The two A36 hooks: the denial page's meta line wears the RCA chip when a case covers it, and the worklist gained a Has RCA case select (`?rca=yes|no`).
Data model calls: the trigger engine is pure and idempotent — a denial in any case is covered and never re-cased, a repeat cluster with an open case is extended rather than doubled — and evaluates amount → appeal lost → repeat, so a denial worth the threshold or lost on appeal opens its own case even when it also sits in a cluster; a cluster is the confirmed-or-tagged cause plus its origin, the stage of the revenue cycle the cause's group names (the prevention feed's own word), never the payer — F2 asks what keeps breaking; amendment 40's pattern engine keys on payer + cause + service by its own spec, and "repeats per payer" is that screen's, which COORDINATION now records so it is not inherited here. `getLostAppeals()` is feature-detected on `data/repositories/appeal-tracking.js` by dynamic import and live in this tree. The seed and the trigger pass run once, after every peer seed has been forced to start and the store has been quiet for 250 ms (and again once per burst of `denials.*` / `appeal*` commits after that): read apart, a peer's seed that writes a denial and then the appeal it lost cased the denial once as a repeat and never as a lost appeal, and two demo resets showed two different worklists — read together, the same nine cases come back in the same order on every load and after every reset, whichever register the shell read first (verified in node over three load orders and two resets each). Concluding retags every covered denial whose tag differs from the confirmed cause through `denials.retagRootCause` — on a resolved denial too, audited old → new on the denial and listed on the case — and opens the accountability case when the nature is individual; closing needs every action Verified and the accountability decision recorded (an appeal reviewed if one was filed), and HR's outcome may still be pending. Evidence is never synthesised: a ref is an audit row another register wrote, stored as `{ auditId, entity, entityId, action, at, user, label }` and resolved by id first and by what it said when a reset dealt the ids again. The three role-separation rules are refused and `console.assert`ed on the save paths: a causer needs evidence, the decider is never the analyst, the appeal reviewer is senior to the decider (`staff.level`) — which is why the demo's concluded individual case has Nour Khalil as analyst, so the coder decides and the CMO reviews. A deduction is only ever recommended; nothing here posts to payroll. Trail entries live on the shared audit entity keyed on each id, not on an `audit[]` field.
Seed: the amendment's four cases plus two older closed individual ones the two decided accountability cases hang off (an accountability case needs a concluded individual finding behind it) — every denial found by rule, never by number: the lost cover appeal (retagged RC-03 → RC-04, registrar named on two denial-trail entries, warning appealed and upheld by the director, closed), the late filing (billing clerk named, $50 deduction recommended and sent to HR as HR-2026-0142, closed with HR's answer pending), the pre-auth matrix gap (systemic, Configuration action on `#/pactum/contracts/CTR-0001/preauth` verified, closed), the recode denial on the oncology emergency visit (the one denial with a chart and a coder behind it: Rita Saba named on her own Draft saved and Marked coded entries, a Training action open, ACC awaiting response inside the window), the RC-17 repeat cluster six days past its target with one why left unanswered, and the $585 amount case unassigned. The trigger engine then opens the rest on load — the recovered $614 denial nobody cased, the appeal lost on the appeal-tracking seed, and the RC-15 cluster the appeals seeds triaged into being — nine cases in all, the same nine after every reset; only the denial ids underneath shift by one when the Bupa remittance seed lands last, which A36 already records. UI calls: the why rows and the cause picker sit in `.toolbar`s (the one primitive that lets a field grow), the nature is bare radios in `.rule-child-row` and the gap toggle `.segmented`, the accountability page is `.split--even` with `.dl--narrow` since both halves carry prose, a masked role reads "Individual — case N" on the worklist, the case banner and the Causer tab and the register withholds outright, and the Causer tab of a concluded case lists its linked entries read-only with a critical badge on any that no longer resolve.

### 41 — Defensio: F6 Denial Analytics & Payer Scorecard
Added the analytics feature under `modules/defensio/features/analytics/` and two Defensio nav entries with no badge (a report is not work): `#/defensio/analytics` (+ a view id: `/overview|reasons|breakdowns|funnel|governance|misclassification`) is the analytics screen — a global period / compare / payer bar with Export CSV of the view on screen, and six views as `.sections` sub-tabs: Overview (the ten definitions as cards with their delta against the period before and the monthly trend of what landed against what came back in cash), Reasons vs causes (the payer's codes ranked beside the desk's root causes, and the divergence rows where the triaged cause is not the one the code usually comes down to), Breakdowns (a `.segmented` slice picker — payer, department, service line, doctor, encounter type — over a table whose every figure reconciles with the overview's, plus the top-8 by denied value), Appeal funnel (stages from landed to cash, leakage led by appealable-never-appealed, wins by ground and by level, turnaround by payer against its response window, shortfall aging), Governance (RCA throughput and overdue, the nature split, individual findings by job role only — asserted on every draw that no causer's id or name is in the HTML — actions by status and type, and amendment 40's plan verdicts, prevented value with an `estimate` badge and rule quality, feature-detected) and Misclassification (the separated-out rows' trend, the contractual / TPA split, by payer, the rows, and a plain link to `#/defensio/tpa-fees`). `#/defensio/scorecard` is the generate form (payer, a period or a calendar month, compare, notes) whose preview is the document; Save freezes it as `SC-2026-000n` — the same payer and period again is the next version, both listed; `#/defensio/scorecard/archive` lists every snapshot and `#/defensio/scorecard/<no>` is the print view with a version switcher, Export PDF (`window.print()` with the chrome under `data-print="hide"`) and Export Excel (a CSV from the snapshot). `denial-analytics.html`/`.js`, the six `view-*.js`, `analytics-charts.js` (perf-charts copied, plus month columns, ranked bars and funnel bars), `analytics-format.js`, `scorecard-doc.js`, `payer-scorecard.html`/`.js`, `scorecard-archive.html`/`.js`. One entity Defensio owns, `scorecards` (`data/repositories/scorecards.js`, seeded on first read from `data/seed/scorecards.js` — one hand-frozen card, NSSF for August 2026, v1), immutable: `create` is the only write. `modules/pactum/NOTES.md` carries the one-line dependency note the amendment asks for; no Pactum code changed.
Architecture calls: the definitions are one table in `data/engines/metric-definitions.js`, a leaf — `{ key, label, formula(world), dateBoundary, format, hint }` for the ten metrics — and `data/engines/denial-analytics.js` is the one engine every view and the scorecard call (it reads eleven repositories and is read by none, the billing engine's position); a view draws what it is handed and does no arithmetic, which is what "no view-local formulas" means here. The engine builds a world per period and per slice and runs the same formulas over it, so a breakdown row, the overview card and the scorecard headline are one function over subsets of one register. `getDenialMetrics(payerId?, period)` is the published definitions-table object Pactum's Performance denial columns read from here on. The archive imports no engine at all — a saved scorecard renders from `row.figures` alone, and the render asserts it holds a frozen document. The analytics views are drawn into a scratch node and moved under the shell panel as sibling regions of the page body, so the shell's rhythm spaces them and a view's listener rides on the panel it belongs to.
Data model calls: three date boundaries, one per metric family, named in a caption on every view — intake (the day the denial landed), decision (the appeal's decision date, the denial's resolution date), cash (the payment date of the remittance that carried the recovery). A Reclassified denial (a contractual adjustment or a TPA fee the desk separated out) is in no denial rate and is counted only by the misclassification rate; recovered is cash-confirmed only — an expected recovery in state `Recovered` — so a conceded amount nobody has paid is not recovered, the A39 ruling; written off is the denial's own `amounts.writtenOff`, which both doors (a denial-sourced request and the appeal loop's) post into, with the two sources split in the figure's detail so nothing is counted twice. A claim is "answered" on its payment date, else the day its first denial landed, else its submission — the generated dataset stamps no answer day on a refusal — which is why a denial rate over the last thirty days reads higher than Performance's year-to-date figure: the register's denials are dated by offsets from today while the claims they sit on were answered through the year. Individual findings are grouped by the staff title of the person named — the id is resolved to a title inside the engine and nothing else about them leaves it; what they did rides in the row's tooltip.
UI calls: the ten overview cards are two `.metric-rail`s of five, each an `<a>` whose href opens the worklist holding its records, with the arrow inline in the value (the Performance denial card's device) and the delta in the sub-line; a figure amendment 40 has not delivered reads "Pending" in the card's text variant rather than a made-up number, and the Governance panels wear a `pending A40` badge with a state view. A drill-through carries what the denials worklist reads (payer, status, code, root cause, class, separation) and loses the period, since that worklist reads no `from` / `to` from its query — a one-line request to A36 under Requests, and the non-payer breakdown cells say so in their tooltip. The scorecard's month field is a plain wrapper carrying `hidden` (a `.field` sets its own display); a change to the payer or period after a generate clears the preview and says why, so a snapshot is only ever saved from a document the desk has seen.
Verified live: the six views on the default period and by deep link; a scorecard generated for NSSF over August, saved as SC-2026-0002 beside the seeded v1, the v1 / v2 switcher, the archive list; Reset demo data re-seeding the one card; every new file `node --check` clean and every module manifest and every `data/` file imported in the browser — the one failure is A40's in-flight `data/repositories/denial-patterns.js`, which the engine's dynamic import tolerates.

### 42 — Defensio: F7 TPA Fee Accounting
Took over the `tpaFeeAccruals` stub (`data/repositories/tpa-fee-accruals.js` — the ids and the A36 `create` contract kept, since `denials.reclassify` is what accrues a fee; over the cap on purpose, one entity, one file) and added four entities Defensio owns beside it — `tpas` (the administrators and their dated payer links), `tpaFeeSchedules` (per link, versioned, append-only), `tpaDisputes` and `tpaAmendments` — each with its seed in `data/seed/`, nothing in `data/store.js`, plus two leaf engines: `data/engines/tpa-matching.js` (the version in force on the remittance date, the most specific scope, the caps, the tolerance, the state and the ledger buckets) and `tpa-amendment.js` (the impact, the tier off the write-off tiers' config, who may sign). The feature is `modules/defensio/features/tpa/` and one nav entry, **TPA ledger** (badge = overcharges nobody has disputed + unscheduled fees): `#/defensio/tpa` is the ledger — a five-card rail (withheld against expected, variance, open overcharge, in dispute, the Unscheduled chip) over three tabs, Accruals (largest open overcharge first, search and filters, a same-administrator bulk dispute, Open and Dispute per row, the accrual drawer with its basis, the version applied, its ledger line, the pre-amendment figures and the trail), Schedules (administrators, contacts, links, every version with Edit always blocked and Amend beside it) and Disputes (Raised → Acknowledged → Settled or Written off, the frozen evidence under each row); `#/defensio/tpa/accruals/<id>` opens the drawer; `#/defensio/tpa/amendments` is the archive and `/new`, `/<id>` the four-step flow — header, computed impact, tiered review, post. `CONFIG.defensio.tpa` holds the tolerance, the config path to the approval tiers and the period-cap unit. Two hooks: the denial page wears a "TPA accrual TPA-…" chip on a TPA-separated denial, and `data/engines/denial-resolution.js`'s TPAFee evidence links to the drawer (the A36 request).
Data model calls: an accrual is one remittance on one claim — a second separation on the same pair joins it, so `actual.separationRefs[]` lists the denials and `denialId` the first; the basis is read from the remittance Claima posted when there is one, else the claim's own payment stamps, else the separation itself, and the administrator is the one linked on the payer on that date. Expected is always the version in force on the remittance date (`versionOf` asserts it) — a posted retrospective restatement first, then the standing version, an unposted restatement invisible. `matchState` is always the matching's answer and `state` the overlay a dispute (Disputed, Settled) or an amendment (Amended) writes through `setDispute` / `applyAmendment`, with the pre-amendment figures snapshotted; the ledger every row holds — actual = agreed + recovered + writtenOff + openDisputed + openUndisputed + unscheduled ± amendmentNet — freezes an amended row's overcharge at the snapshot and carries agreed-before − agreed-after as the amendment's net, which is what `[tpa] self-check` asserts on load. A restatement runs from the period's start to the end of the version it restates: the period names the closed accruals the amendment posts corrections for, later accruals are matched afresh. Versions never overlap and never start in the past (a new one closes the open-ended one before it); "non-overlap per scope" is read as one row per service group inside a version. A dispute's remainder goes to a ClaimResidual write-off request through Claima's own creator on the dispute's claim with the largest payer balance (tagged `origin: 'tpaDispute'`, additive on A33's row) or is accepted with a reason — a Paid claim has no balance to write against, and the dialog says so. The tolerance floor is $1, not the amendment's $5: a three-per-cent fee on a $250 claim is $7.50 and a one-point rate error on it $2.50, so at $5 nothing on this dataset would ever read past a match — a config value, demo-scaled the way A38's senior threshold is.
Claima correction path: pending integration. Claima publishes no creator that reposts a remittance at a different amount (`reversePosting` reverses a whole posting, `claims.adjust` writes off a balance), so a posted amendment keeps each correction as a `postingRefs[]` record of kind PendingIntegration with a `Reconciliation note` line on the claim's trail, and the creator is asked for under Requests in `modules/claima/COORDINATION.md`. The separation hook is the A36 call itself — no hook array; the write-off creator is a dynamic import, feature-detected and live.
UI calls: the ledger's tabs are the design system's `.sections` under the panel header and each tab owns its node and listeners (the shell's freshBody rule one level down), the toolbar drawn once so a half-typed search survives a commit; the rail's two sums are plain numbers and the three slices controls; the schedules tab is a `<details>` per administrator with the panel header as `<summary>`, the billing breakdown's collapsible; the amendment stepper's steps are buttons (the system's stepper allows it) so a posted amendment's four steps can each be read; the settle dialog's remainder switch is `.segmented` with the write-off option disabled and explained when no claim carries a balance, and its hidden toolbar sits in a plain `div` because `.toolbar` sets its own display; a restricted record's claim is withheld on the row the denial worklist's way.
Seed: two administrators found on the payer master (GlobeMed × Arope and Cigna at 3% of paid, Lab 2%, $30 cap — Cigna's v2 is GlobeMed's July notice of 4% from August, restated to 3% by TAM-0001, reason TPA error, requested by Hala Mansour and signed by Tarek Solh at tier 1; NEXtCARE × Allianz, linked from June, flat $4 a claim from August), and five separations driven through the denial register's own writes on Paid or Partially Paid claims found by rule (every other Defensio seed picks Denied claims) beside A36's TPA-0001 — which is the Overcharged accrual in the Raised dispute, a fee withheld on a claim the fund refused outright. The six read Disputed, Matched, Matched, Amended (correction $3.03, TPC-0001 pending integration), Unscheduled and Settled ($10 credited, $5 to write-off request). Verified live: a separation → accrual → dispute → acknowledge → settle; a draft amendment on Arope's second quarter → review → approve → post (the accrual in the settled dispute restated with its snapshot, the ledger still adding up); a version appended and a past-dated one refused; Reset demo data from a Defensio and from a Claima screen; every module in the tree imported in the browser (524 files, 0 failures) and every Defensio route loaded with no console error but the standing `[performance]` one.
